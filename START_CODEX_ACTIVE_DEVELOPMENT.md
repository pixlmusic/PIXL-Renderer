# Start Codex — PIXL Active Development

Use this message when starting a new Codex development session from the PIXL Renderer repository root:

> Read `AGENTS.md`, `PIXL_ENGINEERING_CONTEXT.md`, `PIXL_DEVELOPMENT_WORKFLOW.md`, and `PIXL_ACTIVE_STATE.md` completely before making substantive changes.
>
> You are now working on PIXL Renderer in active development mode.
>
> Treat the current working build as the known-good baseline. Preserve existing PIXL systems and visual fidelity while implementing the newest task.
>
> For every task, inspect the relevant C++/HLSL/settings/build dependencies, check whether `Data\Shaders` contains the active runtime version, make the smallest coherent change, build/test early, diagnose failures rather than stopping at them, and maintain `PIXL_ACTIVE_STATE.md`.
>
> Do not push, publish, or deploy externally unless explicitly requested.
>
> After reading the project files, proceed directly with the development task I provide.
