# Generated build, staging, and local runtime mirrors
/build/
/bin/
/dist/
/Data/
/PIXL_AUTONOMOUS/
/checkpoints/

# Local reference projects used for reverse engineering are not renderer source
# and may retain their own licences/assets.
/_REFERENCE/

# Loose runtime/debug copies occasionally placed at the repository root.
# Canonical equivalents live under engine/, pipeline/, or distribution/.
/PIXLRenderer.dll
/Lighting.hlsl
/WindowLife.hlsli
/GlassGrime_1k.png

# Private neural-enhancement research is developed and licensed separately.
# It is not compiled, linked, loaded, or packaged by PIXL Renderer.
/tools/PixDiTEnhance/

# Local automation/browser state
/.playwright-cli/
.idea/
.vs*/
CMakeUserPresets.json
shadertoolsconfig.json
.vscode

# Folder view configuration files
**/.DS_Store
.claude/settings.local.json

# Python environments, caches, datasets, and trained model artifacts
**/__pycache__/
*.py[cod]
/.venv/
**/.venv/
**/.tartanair-env/
*.onnx
*.pt
*.pth
*.ckpt
*.safetensors

# Downloaded local compiler bundle; the renderer build does not consume it.
/extern/DirectXShaderCompiler/
