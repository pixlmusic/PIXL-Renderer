# Generated build, staging, and local runtime mirrors
/build/
/bin/
/dist/
/Data/
/PIXL_AUTONOMOUS/
/checkpoints/
/out/

# Accidental in-source CMake / Visual Studio output
/CMakeFiles/
/CMakeCache.txt
/cmake_install.cmake
/*.sln
/*.vcxproj
/*.vcxproj.filters
*.vcxproj.user
*.suo
*.VC.db
*.VC.VC.opendb
*.lastbuildstate
*.tlog

# Local reference projects used for reverse engineering are not renderer source
# and may retain their own licences/assets.
/_REFERENCE/

# Loose runtime/debug copies occasionally placed at the repository root.
# Canonical equivalents live under engine/, pipeline/, or distribution/.
/PIXLRenderer.dll
/Lighting.hlsl
/WindowLife.hlsli
/GlassGrime_1k.png

# Private neural-enhancement research is developed and licensed separately.
# It is not compiled, linked, loaded, or packaged by PIXL Renderer.
/tools/PixDiTEnhance/

# Local automation/browser state
/.playwright-cli/
.idea/
.vs*/
CMakeUserPresets.json
shadertoolsconfig.json
.vscode

# Folder view configuration files
**/.DS_Store
*.vortex_backup
*.tmp
*.bak
.claude/settings.local.json

# Python environments, caches, datasets, and trained model artifacts
**/__pycache__/
*.py[cod]
/.venv/
**/.venv/
**/.tartanair-env/
*.onnx
*.pt
*.pth
*.ckpt
*.safetensors

# Downloaded local compiler bundle; the renderer build does not consume it.
/extern/DirectXShaderCompiler/

# NVIDIA Neural Rendering runtime is installed/package-local proprietary binary data.
# The open integration source is tracked; the redistributable runtime must be supplied
# through the licensed release pipeline and is far above GitHub's normal file limit.
/pipeline/ImageReconstruction/Kernels/ImageReconstruction/Streamline/nvngx_dlssnr.dll
