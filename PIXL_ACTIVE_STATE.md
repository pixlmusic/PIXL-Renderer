# PIXL Renderer — Active Development State

> Live engineering ledger for ongoing PIXL Renderer development.
>
> Codex should update this file after meaningful development work.
> Keep it concise enough to remain useful across sessions.

## Project Status

**ACTIVE DEVELOPMENT**

The release-preparation phase has been completed separately. This file is now for normal ongoing engineering work.

## Current Active Task

- **2026-09-01 release-polish build is source-complete, packaged and deployed for owner runtime validation.** Fast camera rotation no longer reduces otherwise valid Hybrid GI history merely from motion-vector magnitude; reprojection remains guarded by world/depth/normal validity. Radiant Grid now keeps a bounded 20-frame particle-emitter submission history with a two-frame full hold, preventing candles/fires from losing clustered illumination during brief visibility changes while preserving per-owner energy deduplication. Actor Surface Effects now anchor events and GPU reconstruction to the live `NPC Root [Root]` skeleton transform (with safe scene-root fallback), and a contaminated actor payload retains its Dialogue Focus prefix instead of being replaced during conversation; this targets jump/gait/camera-dependent mask sliding. The GUI now exposes the real DLSS/NR scene input resolution (DLAA, Quality, Balanced, Performance, Ultra Performance), resets every affected temporal backend on change and reports actual input/output dimensions; Feature 18 output remains display-resolution because the installed runtime rejects an independent scaled output contract. Experimental Hair Reconstruction is retired from the registered/shipped module path and known-good Strand Shading is restored; its 128-byte shared-data reservation and source stub remain only to preserve cached-shader ABI and auditability. Release build PASS, strict HybridGI FXC base+temporal PASS, JSON validation PASS, source/package audit PASS. DLL is 19,666,944 bytes, SHA-256 `48E82C84578A0ACED9BD231A533766A30AFB0390A2BDFE0239227260C6F0CC64`. Live Data and current beta package/archive are synchronized; no shader cache was deleted. Visual verification of turns, DLSS/FG, actor jumping and dialogue remains pending.

- **2026-08-31 Crysis-inspired Atmospheric Visibility audit is source-complete and not deployed.** PIXL already uses a modern shadowed 3D froxel pipeline with conservative depth, cascaded directional PCF, terrain horizon visibility, Sky Veil cloud optical-depth visibility, Ambient Probe/SkyBounce/local-light scattering, temporal rejection, and depth-aware reconstruction. The bounded CPU-only fix now rejects invalid volume history after FOV/projection changes, exterior worldspace changes, or lighting-input availability transitions. No pass, resource, setting, shader register/define, module descriptor, HLSL, or CPU/GPU ABI changed. All 12 strict Atmosphere compute variants, canonical Release build, and the 38-module source/build audit pass; DLL SHA-256 is `8DD429F0B3CCE6F6D32AF947E454D33E07AA7AEED58D90B73F8E03699C69C57F`. Runtime transition validation and deployment are pending. Report: `docs/Crysis3Pipeline/ATMOSPHERIC_VISIBILITY_REPORT.md`.
- **2026-08-31 Crysis-inspired Material Intelligence audit is source-complete and not deployed.** PIXL already normalizes legacy Skyrim materials into bounded base colour, perceptual GGX roughness, dielectric F0, conservative conductor inference, specular AA, and rough-metal multi-scatter, so the known-good visual baseline was preserved. A `chair`/furniture false-positive in automatic hair evidence was fixed, and a collapsed developer-only Physical Material Registry panel now reports table/model/texture/fur counts plus non-finite/schema validation without copying table payloads. No GPU ABI, register, resource, define, permutation, or executable HLSL changed. Release `PIXLRenderer` build passes; only inherited FidelityFX MSB8028 warnings remain. Runtime validation and deployment are pending. Report: `docs/Crysis3Pipeline/MATERIAL_INTELLIGENCE_REPORT.md`. A global Lighting `AUTO_FUR` roughness/F0 write-order issue was documented but intentionally deferred to avoid broad shader invalidation during live testing.
- **2026-08-31 Crysis particle-derived local-light stabilization is source-complete and awaiting runtime visual validation.** Radiant Grid previously appended a local light for every eligible immediate particle submission, so repeated paths or layered fire/torch/brazier billboard geometry could multiply energy and consume cluster capacity according to draw count.
- `QueueParticleLight` now aggregates once per billboard owner per frame with expected O(1) lookup, retains the brightest non-additive tint and largest conservative bound, and rejects non-finite payloads. Separate billboard owners remain separate; no distance merge can fuse adjacent fixtures. Queue handoff also drains while disabled so re-enabling cannot replay stale emitters for one update.
- This is CPU-only and preserves `LightData`, HLSL, `t35`/`t36`/`t37`, module settings, shader defines, cache metadata, and `Radiant Grid` module version `3-2-0`. No shader cache invalidation, live deployment, staging copy, or Hybrid GI file was involved.
- Canonical Release `PIXLRenderer` build and `PIXL-Audit` pass with 38 integrated modules. DLL SHA-256 is `CAB88F2DD4F3332199C37CC49D9788639406DC73340C476065A38C6371243D4E` (19,609,088 bytes); only inherited FidelityFX `MSB8028` shared-intermediate warnings remain.
- Runtime acceptance: compare a candle, held torch, layered brazier/campfire, and dense interior while orbiting; verify stable brightness, distinct nearby fixtures, existing configured culling, and no stale flash after disabling/re-enabling particle lights. Detailed audit: `docs/Crysis3Pipeline/PARTICLE_RESPONSE_REPORT.md`.
- **2026-08-31 Crysis 3 cross-reference / Directional Visibility correctness pass is source-complete and awaiting owner runtime validation.** The audit proved PIXL already implements the Crysis SSDO/bent-normal concept inside HybridGI, so no duplicate pass, buffer, setting, shader define, or global cache-invalidating permutation was added. `pipeline/Hybrid GI/Module.ini` advances narrowly from `4-2-1` to `4-3-0`.
- HybridGI now derives observation confidence from usable angular coverage and the adaptive ray budget as well as distance; temporal resolve reacts to bent-direction, visibility, and confidence discontinuities; confidence is applied once instead of effectively squared; and motion reprojection decodes octahedral bent normals before filtering rather than bilinearly averaging packed oct coordinates. Half/quarter reconstruction now also weights the four candidates by receiver-normal similarity, preventing same-depth crease/thin-geometry bleed. These preserve the existing 400-byte C++/HLSL constant-buffer ABI, resource formats, and deferred t17 contract; upsampling additionally binds the normal pyramid HybridGI already owns at local `t6`.
- Strict FXC (`cs_5_0`, `/Ges /WX /O3`) passes eight representative variants: full-, half/adaptive-, and quarter-resolution GI; full-, half-, and quarter-resolution disocclusion; and half-/quarter-resolution upsampling. `git diff --check` passes. The combined canonical Release build now passes through the particle-response validation build; live deployment remains intentionally deferred while Skyrim PID 23912 is running, and live `Data\Shaders` is untouched.
- **Crysis-inspired live-water caustic focus prototype is source-complete and not deployed.** `Water.hlsl` now derives a bounded first-order refractive-footprint Jacobian from the final wave + flowmap + rain-ripple normal and reconstructed visible receiver distance, then modulates only exterior above-water refracted receiver colour. The authored receiver-caustic atlas remains unchanged. No pass, draw, texture sample, resource/register, cbuffer, or CPU/HLSL ABI was added; five strict Water pixel variants and the 38-module audit pass. Runtime rejection criteria include quad flicker, crest singularities, double-pattern contrast, or TAA/DLSS sparkle. Report: `docs/Crysis3Pipeline/DYNAMIC_WATER_CAUSTICS_REPORT.md`.
- **Shared world-wind sampling foundation is source-complete with no current visual delta.** New `FoliageDynamics/WorldWind.hlsli` owns a resource-free deterministic sampler and exact current/previous evaluation contract. `FoliageWind.hlsli` forwards through a compatibility wrapper; the active RunGrass Foliage vertex program is byte-identical before/after (28,284 bytes, SHA-256 `5E44A1BA03B12907A42AA63A935251D82253548C4975A03DC026A4357F40AB4A`). Strict grass/Ground Response/precipitation/Hair harnesses pass. Hair and precipitation are intentionally not connected until an authoritative renderer wind-state and previous-origin contract exists. Report: `docs/Crysis3Pipeline/WORLD_FORCE_FIELD_FOUNDATION.md`.
- The Crysis study is documented under `docs/Crysis3Pipeline/`: confirmed Crysis-vs-CryEngine provenance, a full PIXL feature cross-reference, unified pipeline architecture, implementation roadmap, and the Directional Visibility change report. It records that PIXL already has a thin auxiliary scene buffer, shadowed froxel atmosphere, physically normalized legacy materials, particle lighting, reflection hierarchy, modern restrained bloom, and advanced WindowLife glass; the release-safe priority is to consolidate these rather than duplicate them.
- Runtime acceptance queue for HybridGI 4.3.0: cave/eave/stair occlusion while orbiting; railings/foliage silhouettes; rapid camera motion and teleport/FOV reset; full/half/quarter GI; TAA and DLSS; interior/exterior transitions; SSR/probe fallback; and Contact Shadows/SkyBounce toggled independently. Do not extend bent visibility to direct sun/local lights until A/B evidence proves it will not double-shadow existing contact/shadow authority.
- Rollback checkpoint: `build/active-dev-checkpoints/20260831-crysis-directional-visibility-prepatch`.
- **2026-08-31 Hair Reconstruction v2 / directional screen-space shadows / farmhouse Auto-POM guard are built and deployed for live validation.** The owner stopped Skyrim before deployment. Release and `PIXL-Audit` pass with 38 integrated modules; final DLL SHA-256 is `5A44DE4514A1B2228DDE29672BB14AF3B0C493E9FA0E8E503A905A7960E77497` (19,607,552 bytes), and canonical/live hashes match.
- The 13:21-13:34 runtime log showed distinct demand-compiled Lighting pixel descriptors as previously unseen skin, FaceGen, native Hair, and material variants entered view. Each queue returned to `remaining=0`; there were no shader compile failures/fatals and no evidence of one permutation rebuilding repeatedly. This is cache repopulation after the prior rollback quarantine, not a compile loop.
- The supplied farmhouse screenshot's narrow blue strips are sky visible through alpha-test holes. Synthetic object Auto-POM could displace an opaque atlas texel into transparent padding before Skyrim's later alpha test. Alpha-tested Auto-POM now compares source/candidate alpha at the selected mip and cancels only displacements that cross out of a valid opaque island. Authored POM, opaque materials, and valid alpha relief remain unchanged. Runtime verification on the same farmhouse is pending.
- Hair v2 no longer appends to or expands the global FeatureData ABI. Its exact 128-byte payload replaces the historical eight-register reserved post-process block in place, preserving total buffer size and all following offsets. `HAIR_RECONSTRUCTION` is descriptor-scoped to native Hair, conservative auto-Hair, and diagnostics rather than emitted on every Lighting permutation. Strict FXC passes native/auto/candidate Hair variants; runtime appearance, motion, wetness, classification, and reconstruction stability remain pending.
- PIXL's existing Contact Shadows module is already the renderer-native directional sun/moon screen-space shadow system (Skyrim directional-light vector, depth reconstruction, Bend raymarch, R8 visibility, Lighting/Grass/DistantTree consumption). The Unity reference was not duplicated. A bounded `Directional Shadow Strength` (0-1, default 1) now controls the final refinement blend. Its module descriptor remains `2-1-4` because only the standalone compute/settings path changed, avoiding needless invalidation of every consumer permutation.
- Live config, user presets, and the existing pipeline cache were not manually modified. Hair's new descriptor `1-0-2` selectively invalidates encountered Hair variants. Material Layers `1-3-2` selectively invalidates applicable Lighting pixel variants so the Auto-POM alpha-island guard cannot be hidden by stale farmhouse shaders; unrelated shader families remain cached. Rollback checkpoints: `build/active-dev-checkpoints/20260831-hair-v2-ssshadow-live-predeploy` and `build/active-dev-checkpoints/20260831-hair-v2-pre-materiallayers-tag`.
- **2026-08-31 release baseline / FSR / actor accumulation / water / shadows / fur pass is built and deployed for live validation.** The user's live `UserGraphics.json` remains byte-identical at SHA-256 `827CDF4ACCE18B8438362A64B08C7D07EFAEB534E99BC1FE7CDB09868BC6726E` and is the Ultra/golden visual profile. Fresh-install defaults are Medium quality, FSR 3 Balanced, and frame generation off. Ground Response quality scaling changes only near/far tessellation.
- FidelityFX was advanced from the local FSR 3.1.4 snapshot to upstream maintenance commit `e65b253` (including the DX11 FSR 3.1.5 RCAS limiter and upscale dispatch-size fix) while retaining PIXL's required short-output CMake patch. FSR 4 through OptiScaler/D3D11on12 remains a future optional path: PIXL's current DX12 sidecar does not yet bridge the complete upscaler resource/state contract, so it was not made a release dependency.
- Actor Surface Effects now evaluates every actor draw in one shared camera-relative actor space instead of mesh-local bind origins. This targets the ghost-white head/waist discontinuity and equipment misses. Submersion immediately washes snow/mud, retains wetness, and decay contracts the upper contamination boundary downward. Runtime tests across head, skin, boots, armour, jumping, deep snow, and water are pending.
- Directional screen-space shadow consumption now matches the producer's stable 6144-12288-unit exterior range rather than fading at 2048-5120. Water received a bounded roughness-aware directional sun glint. Both require visual validation.
- Auto fur detection is now connected to live raster permutations using conservative fur/pelt/fleece and known furry-creature evidence with eye/claw/horn/armour exclusions. The release-safe implementation is a virtual fibre-shell/fuzz BRDF (no new draw calls/resources); true silhouette geometry shells remain future/high-risk work.
- Release build PASS: `build\PIXL-12C\Release\PIXLRenderer.dll`, SHA-256 `703940D67C0F2096586E2A068014A38E46F526DC5FC1BC499F1E40877B265495`. Representative AutoFur, MaterialForge, skin, water, actor-effect, and contact-shadow FXC permutations pass. `tools/AuditPixlRenderer.ps1` passes all 37 modules and the staged beta package. Canonical/live shaders match 264/264. Live Data and `PIXL-Renderer-v1.0-CURRENT-BETA` are deployed; visual success is not claimed until owner testing.
- Rollback material: `build\active-dev-checkpoints\20260831-release-baseline-fsr-actor-water-shadows-fur-prepatch` and `build\deploy-backups\20260831-fsr-actor-water-shadows-fur`.

- WindowLife `0.5.6` exact loose-mask integration is built, clean-Release validated, and deployed to live Data and beta for owner runtime validation as of 2026-08-25. Runtime comparison proved the external mask selected the correct pane but the mask-fit confidence gate suppressed the room, curtains, and occupants. Exact external masks now use appropriately relaxed world-size/aspect acceptance and retain a precisely pane-clipped procedural fallback if UV-to-world reconstruction is unavailable; noisy native glow maps keep the strict facade-safety gates.
- Private source-control preparation is active. Generated build/staging/runtime trees, datasets, model weights, Python environments, downloaded DXC, and local automation state are ignored; internal ledgers and the independent `tools/PixDiTEnhance` research workspace are `export-ignore`. `tools/ExportPixlPublicSource.ps1` audits a committed public GPL source archive. GitHub CLI and the uploaded Ed25519 key authenticate as `pixlmusic`; `origin` is `git@github.com:pixlmusic/PIXL-Renderer.git` and passed a read-only SSH test. The repository is still empty and nothing has been pushed. Historical Community Shaders `upstream` remains comparison-only.
- PixDiT Photo Mode Enhance engineering-validation foundation is complete as of 2026-08-25. The reproducible pixel-space teacher/distillation/export suite, cross-vendor DirectML validation, compositor shader, native bridge contract, model card, and measured build report are under `tools/PixDiTEnhance`.
- The generated checkpoints are deliberately **not** live or release models. The original synthetic artifact proves architecture/distillation/export, while `train_real_data.py` now provides bounded restoration/geometry pretraining from the completed Sintel/TartanAir RGB-depth corpus. Neither has been copied into `Data`, connected to the Photo Mode UI, enabled at runtime, or synchronized to beta.
- Production Photo Mode integration is a subsequent development task requiring licensed representative PIXL RGB/depth captures and reviewed targets, a compiled native DirectML bridge, real-image/temporal acceptance, and physical AMD hardware validation. Preserve the original Photo Finish capture and write neural output only to a separate sibling file.
- Auto-POM, foliage, runoff, cache, and benchmark items remain runtime-validation backlog. This iteration changed/deployed only WindowLife and added isolated PixDiT training/export tooling; it did not deploy a neural model or alter the other renderer systems.
- Refine object Auto-POM for the next runtime pass after owner screenshots showed diffuse/compression noise becoming pebbled displacement on road, roof, wood and stone meshes. Preserve authored height/POM as authoritative and retain the approved terrain/GroundResponse depth behavior.
- Auto-POM acceptance criteria: synthesize height only from coherent mid-scale material structure; reject broad painted colour, baked-light gradients, chroma-only changes and unsupported fine noise; keep shifts bounded at grazing angles; preserve stable DLSS/DLAA sampling; retain useful stone/brick/board depth even when the user exposes a high maximum-depth setting.
- Smart microdetail acceptance criteria: unsupported high frequencies remain shading-only, with bounded luminance-first albedo recovery, normalized normal reconstruction and subtle cavity/ridge roughness rather than literal displacement or amplified chroma/compression artifacts.
- WindowLife `0.5.5` and Material Layers `1.3.1` are now validated and deployed to `Data\Shaders` with Skyrim closed. Runtime visual/log validation is pending the next launch; do not promote this build to beta until that pass is accepted.
- Runtime-validate Foliage Dynamics `2.1.0` natural multi-scale wind deployed on 2026-08-24 while preserving the approved WindowLife v0.5 room-atlas and repaired complex-grass material baseline.
- Wind criteria: ordinary terrain grass must move even when Skyrim supplies negligible legacy wind; TREE_ANIM trees/plants must receive coherent broad gusts plus bounded branch/tip variation; enabling motion must not change vegetation roughness/specular/metallic response or fold cards into mirror-like facets; current/previous deformation must remain coherent.
- Grass alpha/cutout criteria: the dedicated override must work with enhanced vegetation lighting and wind independently disabled, without changing the existing grass population/distance fade.
- Grass criteria: preserve first-/third-person SSS parity, move/soften the packed-normal detail transition without introducing a new seam, and produce readable normalized GGX highlights under direct light.
- WindowLife criteria: the supplied Solitude facade must never expose a proxy/interior mesh or classify a whole building; authored masks must provide a stable common window centre, size, and floor; frames remain excluded; parallax, curtains, occupant variation, and indoor passers remain convincing.
- Runoff criteria: DLSS Balanced must align with DLAA, droplets must retain depth and continuity at close range while panning/walking, and the effect must not produce the supplied red/cyan spatial break-up.
- Cache criteria: one recovery compile is expected because the previous watcher bug had already reduced the persistent library to 60 stages; after recovery, edits to direct module kernels must preserve unrelated pipeline stages.
- World Benchmark v2 remains pending runtime smoke testing after these visual systems are accepted.

## Known-Good Baselines

### Overall

- The current working PIXL build is the baseline.
- Preserve existing PIXL visual functionality unless a task explicitly changes it.
- `Data\Shaders` may contain the newest runtime-tested shader copies and must be checked when relevant.

### GroundResponse 13BE

Protected known-good behavior:

- deformable snow/mud raised hull textures correctly;
- hull is fully opaque;
- hull correctly occludes player/objects using HS/DS raster depth;
- top face lighting is correct;
- directional/world-space shadows work;
- buried-player/contact-shadow bleed-through is fixed.

Do not regress this checkpoint.

## Recent / Important Development Areas

These are context, not necessarily unfinished tasks.

### Quality profiles

- Quality settings must be wired to real runtime/rendering parameters.
- Lighting quality grouping has been an active development area.
- Radiance Weave quality previously appeared to produce little/no effect and must remain meaningfully wired.

### GroundResponse interactions

Recent work has included snow/mud deformation, shout interaction, fire/spell interaction, dragon-fire behavior, and hull shadow quality.

### Precipitation

Recent design requirements include rain world-space behavior, snow world-space behavior, convincing precipitation distance, and snow integration with PIXL deformation and vanilla behavior.

### Vegetation

Known problem area from prior development:

- advanced wind could make vegetation appear shiny;
- wind behavior needed correction;
- RunGrass compilation/integration has required debugging.

### Director / Photo mode

DOF accumulation/history behavior has been an active system and should be considered when changing temporal/camera logic.

### PixDiT neural Photo Finish validation (2026-08-25)

- The external one-click TartanAir helper under `H:\sintel training data` now uses a compact seven-environment profile (AncientTowns, Antiquity3D, OldScandinavia and four OldTown variants), explicitly excludes the approximately 23 GB Ruins pair, requests only easy/lcam_front/image+depth archives, and performs resumable Windows-native Python extraction. Per-archive completion markers prevent re-download after verified ZIP removal. The download-only import and target selection pass self-check; the owner controls when to resume the external transfer.
- Added `tools/PixDiTEnhance/TRAINING_GUIDE.md`, a beginner-oriented guide covering aligned RGB/target/depth capture requirements, manifest layout, exact teacher/distillation/export commands, RTX 3060 Ti memory guidance, result interpretation, provenance, and the production acceptance checklist. It explicitly records that the real PIXL RGB/depth dataset capture utility is still required before production training.
- Added `tools/PixDiTEnhance`, containing deterministic synthetic RGB/depth generation, a compact 7,689,731-parameter pixel-space local-window transformer, conditional flow-matching teacher training, one-forward-pass student distillation, FP32/FP16/INT8 ONNX export, CUDA/CPU/DirectML execution tests, and a master retrying test runner.
- The student consumes fixed float32 NCHW `(1,4,512,512)` RGBD and produces bounded RGB `(1,3,512,512)` in exactly one invocation. It has no VAE and no iterative runtime scheduler. Arbitrary captures use registered overlapping RGB/depth tiles after existing PIXL temporal reconstruction, detail recovery, and Photo Lens processing.
- DirectML is the required AMD/NVIDIA/Intel runtime contract; CUDA/TensorRT remains optional. FP32 and FP16 were executed through ONNX Runtime `DmlExecutionProvider` on the available NVIDIA RTX 3060 Ti. Physical AMD hardware was not available and remains required before a production claim/release.
- Non-destructive contract: disabled by default, off the render thread, original image immutable, separate `_NEURAL` output, strict model metadata/hash and finite-output checks, bounded residual compositing, and immediate original-image fallback on any failure. The validation checkpoint is SDR sRGB only; HDR bypasses it.
- Authoritative 50-epoch/50-epoch run: teacher train loss `0.123044 -> 0.032718` (best validation `0.027479`); student train loss `0.021027 -> 0.019313` (best validation `0.032130`); student validation PSNR `28.231 dB` versus synthetic raw baseline `28.153 dB`; all values finite.
- 512x512 measured inference: CUDA FP32 `59.066 ms`, CUDA FP16 `33.418 ms`, DirectML FP32 `84.660 ms`, DirectML FP16 `34.900 ms`, and developer CPU INT8 `1016.334 ms`. These are development-machine pipeline measurements, not shipping performance claims.
- Final ONNX hashes: FP32 `62DB3204AE26CE91EF075E4F3DB12EFB81B0D0B90FF34745B5128F32F1C5AFD8`; FP16 `F41C3680E504E04DD6C89489EA4A9245F47AAC7409C391DAB6CC210CF7BBAEE3`; dynamic INT8 `91110A2C193F5E061B5E968ECA26EE2688784773D97248C4C5EC068A2444F30E`.
- Validation: full master runner PASS with hard convergence/parameter/PSNR/shape/finite/one-step/DirectML precision gates; all ONNX graphs passed full checker; deployment graphs contain no Sin/Cos operators; FP16 maximum delta from FP32 `0.000742`; compositor passed FXC `ps_5_0 /Ges /WX /O3`; all Python sources passed `compileall`.
- Detailed results and limitations are in `tools/PixDiTEnhance/BUILD_SUMMARY.md`, `MODEL_CARD.md`, and `hlsl_bridge_spec.md`. Generated weights and datasets remain under ignored `build/pixdit-validation` rather than public source or release staging.
- Added `tools/PixDiTEnhance/train_real_data.py`. It indexes the real extracted TartanAir front-left RGB/metric-depth layout and reads Sintel clean/final RGB plus `.dpt` depth directly from the downloaded ZIPs. Whole trajectories/scenes remain in one split; future true PIXL aligned pairs may be weighted only on the training side.
- Full inventory at stride 8: 8,389 samples total / 6,291 train / 2,098 validation (6,261 TartanAir plus 1,064 each of Sintel clean/final). Both Dunmer screenshots are SHA-256-recorded holdout aesthetic references and are never training entries.
- A 12-sample, 64-square CUDA smoke epoch passed in 3.0 seconds with finite loss/gradients and produced a compatible 7,689,731-parameter, one-forward-pass bounded-residual checkpoint. This is a plumbing test only: one epoch over a tiny subset does not establish visual quality.
- The real-data smoke checkpoint exported successfully to opset-17 FP32/FP16/INT8 ONNX. CUDA executed FP32/FP16 with finite correct-shaped output; FP16 also executed through `DmlExecutionProvider` with finite `(1,3,64,64)` output. Export metadata now identifies real-data pretraining separately from synthetic validation. Physical AMD validation remains pending.
- The capped real-data pilot trained for 20 epochs over 1,800 balanced auxiliary samples (1,282 train / 518 validation). Best loss was `0.044290`; validation PSNR improved from the degraded-input baseline `27.207 dB` to `27.985 dB`; gradients and outputs remained finite.
- Added safe continuation semantics: `--epoch-offset` advances deterministic augmentations, a resumed checkpoint is validated/copied as the incumbent before optimization, later epochs replace it only on a real holdout improvement, and metrics now distinguish best from final validation. A CUDA resume smoke test passed.
- Ten lower-rate continuation epochs produced the current best auxiliary checkpoint at absolute epoch 30: validation loss `0.043609`, PSNR `28.183 dB`, mean absolute residual `0.011967`, maximum pre-clip gradient norm `0.411967`, and no NaN/Inf. This is a useful restoration/geometry pretraining gain, not yet a production Skyrim enhancement model.
- The epoch-30 model exported at fixed `(1,4,512,512) -> (1,3,512,512)` to opset-17 FP32/FP16/dynamic-INT8. Hashes: FP32 `0F925BAF4326A71645C5FBC9D88F77CFAE5E27550759CBBE507D80CF55F6B115`; FP16 `D3CFB58DAE7D15CBABA2F156AD0E56FCE9E38D568E50A56A39BD056D56069B44`; INT8 `FCAA577D1525044E32D6C669B7F89EA041407C3C0CF2D76859D5D2DF1A24B6E6`. CUDA and DirectML FP32/FP16 outputs were finite and correctly shaped. Timings from this export are invalid for performance comparison because Skyrim was concurrently compiling its shader cache; repeat after the game/compiler are idle.

### Eye rendering

PIXL eye optics/rendering is an active high-fidelity subsystem.

## Current Build Status

- PixDiT work added isolated tooling/bridge specifications only and did not change compiled PIXL C++ targets, CMake inputs, live shaders, `Data`, or beta staging. The established renderer DLL baseline below remains authoritative; no redundant renderer rebuild/deployment was performed for the synthetic model-validation task.
- Build configuration: `PIXL-12C` Release baseline
- Last clean command: `cmake --build P:\build\PIXL-12C --clean-first --config Release --target PIXLRenderer -- /m /nr:false`
- Result: PASS; full clean Release compilation and link completed after correcting the bundled FidelityFX DX11 shader compiler's overlong output argument.
- DLL path: `build\PIXL-12C\Release\PIXLRenderer.dll`
- Latest clean Release build: PASS after WindowLife `0.5.6`; build/live/beta DLL SHA-256 `3C615F0DADD8C5718E63C01B3CEB7722D01CA3393207B0F2A2ADABBE733C416B` (19,399,680 bytes).
- Relevant warnings/errors: only inherited FidelityFX `MSB8028` shared-intermediate warnings; no PIXL compiler/linker errors. `tools/AuditPixlRenderer.ps1` passes for all 36 modules.
- FidelityFX build finding: `FidelityFX_SC.exe` exited with `0xC0000409` after receiving the long absolute H: output path, and MSVC later failed to open generated includes through the same long tree. The committed top-level patch now routes both generation and include lookup through optional `PIXL_FFX_SHORT_BINARY_ROOT`; the validated build used `P:/build/PIXL-12C/ffx-dx11-shaders`.

## Current Shader Status

- Live shader root: `H:\The Elder Scrolls - Skyrim - Special Edition\Data\Shaders`
- Source shaders changed in this iteration: `distribution/Shaders/RunGrass.hlsl`, both Foliage Dynamics kernels, WindowLife, and all four Rain Response runoff compute shaders.
- Last permutations tested: RunGrass PS with Foliage alone and with SkyBounce/ContactShadows/AmbientProbe/WorldProbes, RunGrass VS with Foliage, WindowLife base/Deferred/Envmapped/MaterialForge/Specular Lighting PS variants, and all four runoff CS stages.
- Compile result: all targeted stages pass Windows SDK 10.0.26100 FXC `/Ges /WX /O3` using valid production permutations.
- Module versions: Foliage Dynamics `2.1.0`; WindowLife `0.5.6`; Rain Response remains `3.2.0` because its changed runoff files are direct compute kernels and a version bump would unnecessarily invalidate every family receiving its global define.
- Runtime shader-cache status before this deployment: only 60 stages / 27,240,954 bytes remain under `Data\PIXL\PipelineLibrary`. The previous direct-kernel watcher path had already deleted the full library before the repair could take effect. No cache files were manually deleted for this deployment.
- Canonical/live reconciliation: PASS, 243/243 matching; zero different, source-only, live-only, backup-like, or overlay-collision entries.
- New watcher behavior: direct module `.hlsl` changes release only that module's owned kernels; tracked/owned `.hlsli` changes invalidate only affected pipeline families; `RunGrass.hlsl` maps to Grass; only genuinely unowned shared includes can request a full persistent-library clear. Full and selective invalidations now log an explicit reason.

## Current Runtime Validation

- Last owner evidence: grass SSS works in both first and third person (PASS), but a detailed near-grass transition remains visible; normalized GGX highlights remain too weak; WindowLife `0.4.0` can place/tile occupants inconsistently and one Solitude facade still exposes invalid interior geometry; runoff has severe DLSS Balanced spatial break-up compared with DLAA.
- Pre-deployment log review: the 14:31:59 run is 28,758 bytes / 393 lines with zero error/critical, shader-failure, exception, or assertion matches. It reports a valid disk cache and saves metadata at 14:32:25. This predates the new DLL and cannot validate the new runtime paths.
- Deployment: PASS while Skyrim was closed. The clean build, live game, and beta DLLs are byte-identical at SHA-256 `3C615F0DADD8C5718E63C01B3CEB7722D01CA3393207B0F2A2ADABBE733C416B`; all 243 assembled live shaders match canonical source. `UserGraphics.json` was not modified.
- Audit: targeted `git diff --check` passes; `tools/AuditPixlRenderer.ps1` passes for all 36 modules.
- New rollback checkpoint: `build\active-dev-checkpoints\20260824-153811-pre-grass-window-runoff` preserves the prior live DLL, the ten replaced live shader/descriptor files, and all 60 remaining pre-run cache stages (71 files / 46,688,089 bytes total).
- Runtime validation of this build is pending the owner's next launch/cache recovery and scene tests.

## Latest Active-Development Checkpoint — 2026-08-25 WindowLife 0.5.5 Exact Pane Masks

- Owner runtime result from `0.5.4`: representative Solitude windows still reported the procedural/fallback layout, some facade geometry remained lit, and closed shutters were treated as windows.
- The renderer log proved `textures\architecture\solitude\swindow01.dds` and `swindow03.dds` were explicit candidates but had no game glow texture bound (`glow=0`). Exact optional masks already installed at `Data\Textures\masks\swindow01_mask.dds` and `swindow03_mask.dds` therefore never reached HLSL. The same log showed arbitrary Solitude roof, wall, door, trim, floor, and detail materials being accepted merely because another texture slot contained glow data.
- WindowLife now scans only the optional loose `Data\Textures\masks\*.dds` directory, canonicalizes `_mask`/`noalpha` suffixes, loads valid DDS resources, and matches them by diffuse basename. Missing or invalid optional masks preserve the existing native-glow/procedural fallback; no external mask asset is copied into PIXL source or staging.
- Lighting PS slot `t125`, previously unused, now carries the per-draw exact pane mask. Existing room atlas `t126`, per-draw structured data `t127`, 176-byte C++/HLSL payload size, and all payload offsets remain unchanged. `Class0.z` uses reserved bits to distinguish native glow from an exact external mask.
- Exact masks take precedence over native glow and bypass the old flat-normal pane guard because the authored bitmap already separates glass from frame/leadwork. Native-glow and conservative procedural paths retain their prior guards.
- Classification now requires strong diffuse-path window evidence or an exact mask match; generic architecture plus an unrelated glow slot no longer activates WindowLife. This directly targets lit shutters, roofs, walls, and facade stencils without globally excluding legitimate stained/leaded windows.
- Strict FXC `/Ges /WX /O3` passes base, vertex-colour, environment/specular, and model-space-normal alpha-test Lighting pixel permutations. Incremental Release link, the 36-module audit, and canonical/live reconciliation all pass; source/live shaders are 243/243 exact.
- Deployed with Skyrim closed. Rollback checkpoint: `build\live-checkpoints\windowlife-v055-predeploy-20260825-163846`. Existing pipeline cache was preserved so `0.5.5` should selectively rebuild only eligible WindowLife Lighting pixel stages. Beta staging remains untouched.

## Previous Active-Development Checkpoint — 2026-08-25 WindowLife 0.5.4 Authored-Fit Repair

- Owner runtime finding: `Fit Interiors to Authored Window Masks` appeared to do nothing and manual Room Cell Width/Height remained finicky across window styles. The atlas itself was healthy; the runtime log reports a 12-mip authored atlas and ready t126/t127 resources with no WindowLife shader failures.
- Root cause: failed mask confidence silently reused the same procedural room grid as the toggle-off path. Manual RoomWidth/RoomHeight also participated in strict/background acceptance bounds, so tuning the procedural fallback changed whether automatic fitting was accepted.
- Added a mask-local 3x3 coarse-guide seed recovery only for narrow panes lost by the normal one-sample fast path. Normal panes retain the prior sampling cost.
- Replaced manual-slider-dependent fit bounds with geometry-relative limits plus conservative absolute ceilings. Strict four-edge confidence remains required for people/curtains; broader background confidence remains available for arches and mullioned panes.
- When authored fitting is enabled on a glow-authored window, a rejected fit now remains honest architectural glass instead of drawing a repeated procedural interior. Procedural width/height remains available only for non-glow materials or when authored fitting is disabled.
- Added fit-state diagnostics to the existing developer overlay: red = rejected authored fit, cyan = background-safe fit, green = occupant-safe fit. Developer sliders are relabelled `Fallback Room Width/Height` with their actual scope documented.
- Reduced the accepted-room source diffuse remnant from 14% to 8%. Independent glass reflection, dirt, roughness and refraction remain intact while the flat opaque/yellow vanilla window texture contributes less over a trustworthy room.
- Four representative Lighting pixel permutations pass strict Windows SDK FXC `/Ges /WX /O3`: base, vertex-colour, environment/specular, and model-space-normal alpha-test. A manually over-composed all-feature invocation exposed unrelated Ambient Probe/Atmosphere warnings because descriptor-specific constants were absent; it was not counted as a production permutation.
- Incremental Release build and link pass with only inherited FidelityFX `MSB8028` warnings. The 36-module audit passes. Build/live DLL SHA-256 is `FD0B840B9483A455ECCC978019C491D2D32EB7F6DC8CBBB48A0A86F3C0F83D07`.
- Deployed while Skyrim was closed. Canonical/live reconciliation passes 243/243 with zero differences or collisions. The full 3,434-stage cache was preserved; WindowLife `0.5.4` should selectively invalidate only eligible static Lighting pixel stages on next launch.
- Rollback checkpoint: `build/live-checkpoints/windowlife-v054-predeploy-20260825-144648`. Beta staging and `UserGraphics.json` remain untouched.
- Runtime validation pending: inspect the large Solitude stone facade and representative small/arched/leaded windows first. The debug overlay should distinguish a genuine mask rejection from frame classification. Confirm room fitting no longer depends on Fallback Room Width/Height and that the reduced source diffuse reveals the room without losing glass character.
- Follow-up candidate after this pass: some genuinely closed/shuttered windows still appear bright at night. Add a conservative transmissive-glass discriminator from authored glow, pane-normal continuity and diffuse/roughness evidence only after runtime evidence, so shutter rejection does not suppress valid stained/leaded panes.
- A signed pane offset was requested for consideration. Do not expose negative `ParallaxDepth` directly: it would reverse layer motion rather than place a surface in front of Skyrim's depth-tested mesh. If still needed after the opacity/mask test, implement a separately named bounded optical sample/composite bias with explicit semantics.

## Previous Active-Development Checkpoint — 2026-08-25 WindowLife 0.5.3 + Auto-POM 1.3.1

- Owner runtime result for WindowLife `0.5.2`: the authored room was only readable immediately beside the pane and did not approach the supplied parallax-window reference at normal street distance. The atlas loaded successfully and the runtime log contained no WindowLife/shader errors, so the defect was in sampling/compositing rather than resource availability.
- Root-caused a concrete atlas defect: the 2048x2048 PNG was uploaded as a single-mip SRV even though the asset contract required a full mip chain. Full-resolution room detail therefore collapsed into unstable dark noise when a window occupied only a few pixels. `WindowLife.cpp` now generates a full cubic, sRGB-aware mip chain at setup, retains a safe base-level fallback, and logs the uploaded mip count.
- Added separate close and distance pane confidence. Close glass still uses the strict full-resolution glow erosion and flat-normal guard that excludes frames; minified windows can retain filtered authored glow coverage without relaxing the candidate/material classifier. Oblique street views now fade only near genuinely edge-on angles.
- Rebuilt the authored interior as a four-cue depth stack: deep mip-stable room back plane, nearer high-contrast furniture/detail plane extracted from the room atlas, shallow seeded coloured curtains, and a dark parallax side/reveal. Each layer moves at an independent depth while the real glow mask remains the final coverage authority.
- Raised room visibility to a confident reference baseline using time-of-day-aware exposure, mip-preserved local contrast, near-total suppression of the flat source diffuse in accepted pane pixels, and a nonlinear composite weight that keeps partially minified windows readable. No user configuration values were overwritten.
- Refined synthetic object Auto-POM so the configured depth is a maximum gated by coherent mid-frequency structure. The march now rejects broad paint/light gradients, chroma-only changes and unsupported fine compression noise, uses normal/luminance agreement, and leaves fine residuals to bounded luminance-first albedo/normalized-normal/signed roughness microdetail instead of treating them as geometry.
- Material Layers cache ownership is now limited to applicable Lighting pixel techniques. No cbuffer size/offset, register, SRV slot, descriptor, GPU format, or per-draw WindowLife payload changed; the payload remains 176 bytes and the atlas remains at PS `t126` beside per-draw `t127`.
- Strict Windows SDK FXC `/Ges /WX /O3` passes WindowLife base, model-space-normal, and the exact production-sized Lighting feature stack. Auto-POM base/model-space/envmap, authored POM, multilayer POM, terrain, alpha-test exclusion, and Material Forge production permutations also pass.
- Incremental Release build and link pass. Only inherited FidelityFX `MSB8028` shared-intermediate warnings remain. `tools/AuditPixlRenderer.ps1` passes all 36 modules.
- Deployment completed with Skyrim closed. Live DLL SHA-256 is `5BF43C0CF3D5E2756877E91E653284F5E2BF53E0624BD2B7670C49C234E2C13E`; canonical/live shader reconciliation passes 243/243 with zero differences or collisions. `UserGraphics.json`, the pipeline cache, and beta staging were not modified.
- Rollback checkpoint: `build/live-checkpoints/windowlife-v053-20260825-032304` preserves the prior DLL/PDB and every replaced shader/descriptor. The next launch should selectively rebuild affected Lighting pixel stages for WindowLife `0.5.3` and Material Layers `1.3.1`, not clear the whole library.

## Latest Active-Development Checkpoint — 2026-08-24 Foliage Dynamics 2.1.0

- Root-caused the terrain-grass no-motion defect: the previous PIXL enhancement was only a percentage of Skyrim's legacy displacement. Draws with negligible `WindVector.z`, or current user gust/flutter values at zero, therefore received no meaningful added movement.
- Root-caused the apparent wind/material coupling: grass alpha override was gated by `EnableEnhancedVegetation`, and Terrain/Vegetation quality presets forcibly enabled that material-lighting switch while selecting wind quality. The wind shader itself uses `EnableEnhancedWind`, but the preset made motion selection opt animated foliage into a different lighting path.
- Rebuilt grass motion as a world-stable multi-scale field: transformed absolute instance anchors, travelling eased gust cells, directional meander, a calm ambient floor, weather-dominant response, tip-weighted cross drift/flutter, exact current/previous evaluation, and a hard bound on PIXL-added displacement. `Wind Response = 0` remains exact vanilla motion.
- Rebuilt TREE_ANIM enhancement with independently sampled current/previous broad noise, branch-scale variation, tip flutter, a restrained calm floor for smaller plants with zero tree amplitude, and bounded added deformation. Tree gust anchors restore `SharedData::CameraPosAdjust`, keeping the field stable across Skyrim camera-relative origin shifts without requiring FrameBuffer b12. Static Lighting geometry without TREE_ANIM metadata is deliberately not moved because it has no safe root/tip mask and could include buildings.
- Grass alpha/cutout now has an independent runtime gate. Material alpha shaping no longer multiplies the already separately dithered population/distance coverage, preventing the cutout controls from shifting the grass fade boundary.
- Quality profiles no longer force `EnableEnhancedVegetation` while selecting Terrain/Vegetation motion quality. The UI now labels lighting and motion explicitly as separate systems.
- FeatureData b6 remains exactly 80 bytes and Foliage tuning b13 remains exactly 96 bytes; no cbuffer fields, offsets, registers, SRV slots, formats, or descriptor ABI changed. Foliage Dynamics module version is `2.1.0` so only Grass and Lighting families are expected to invalidate.
- Strict Windows SDK 10.0.26100 FXC `/Ges /WX /O3` passes RunGrass base/GroundResponse VS, RunGrass base/full/depth PS, TREE_ANIM Lighting VS, and TREE_ANIM deferred Lighting PS.
- Incremental Release build passes; only inherited FidelityFX `MSB8028` warnings remain. The 36-module audit passes. Build/live DLL SHA-256 is `FAADB16A4D11D24F0D7F3AE543AC9DEFD5F8D3E9E40B53BE3C10E0A67B8A733A`.
- Deployment completed while Skyrim was closed. Canonical/live reconciliation passes 243/243. `UserGraphics.json`, the existing 3,435-file / 2,086,188,526-byte pipeline library, and beta staging were not modified.
- The preserved cache metadata still records WindowLife `0.4.1` and Foliage Dynamics `2.0.5`, while live descriptors are `0.5.0` and `2.1.0`. The next launch must therefore selectively invalidate the 589 cached Lighting stages and eight RunGrass stages once; the other 2,837 cache files should remain. Lighting cannot be narrowed further on this launch because the pending WindowLife v0.5 room-atlas integration changed its pixel path as well as this wind update changing TREE_ANIM vertices.
- Rollback checkpoint `build/checkpoints/20260824-foliage-wind-v21-predeploy` preserves the previous live DLL and affected live shader/descriptor files. The prior `20260824-windowlife-v5-mirrorgrass-predeploy` checkpoint already preserves all 589 Lighting and eight RunGrass stages, avoiding a redundant ~986 MB cache copy.
- Runtime visual validation pending: enable `Natural Multi-Scale Wind` with the current `Wind Response 2.0`, `Gust Strength 0`, and `Leaf Flutter 0` first to prove ordinary terrain-grass coverage; then test moderate gust/flutter values on grass, shrubs, and TREE_ANIM trees. Confirm material appearance is identical in a stationary before/after wind toggle and that alpha override still changes grass coverage with both lighting and wind disabled.

## Latest Active-Development Checkpoint — 2026-08-24 18:24 +10:00

- Owner runtime result: WindowLife `0.4.1` now works well and the supplied Solitude test is materially improved. Preserve that shader as the current window baseline; no WindowLife runtime HLSL, descriptor, module version, resource binding, or cbuffer was changed in this iteration.
- Completed post-run log/cache review. The recovered persistent cache contains 3,435 files / 2,086,133,326 bytes. The latest 806-line `PIXLRenderer.log` saved metadata at 17:08:42 with zero shader/compiler failures, criticals, assertions, device removals, exceptions, or out-of-memory matches. The only warning remains the expected optional `PIXL-TerrainField.esp` absence.
- The watcher selectively invalidated only five affected stages during the prior deployment; unrelated shader families were preserved. The roughly 51-minute compile was recovery from the earlier already-deleted library, not a continuing invalidation loop.
- Root-caused the forced-complex-grass ring: `RunGrass.hlsl` faded packed-normal amplitude to zero over an `AlphaParam1`-derived world-space radius. Vanilla grass has no corresponding packed normal, explaining why it did not show the same material/lighting boundary.
- Replaced that amplitude transition with a distance-progressive `SampleBias` filter. The complex normal remains present for the complete card lifetime; distance now removes high-frequency bandwidth without changing the mean material normal or producing a lighting ring.
- Root-caused tip-only grass reflection to two compounding inputs: some complex packs author their packed alpha strongest at blade tips, and wind-deformed derivative card normals allowed individual triangles/tips to dominate the lobe. Packed alpha is now a tunable low-influence variation channel (`0.15` default), passed blade interiors use near-binary material coverage, and the coherent per-card/instance normal remains authoritative.
- Foliage tuning payload version is now 3 at the existing dedicated `b13` register and remains exactly 80 bytes on C++ and HLSL. The former c4 padding float is now `GrassComplexSpecularMapInfluence`; no descriptor slot, register, buffer size, or FeatureData b6 layout changed.
- Renamed the misleading distance UI to `Complex Normal Filter Distance` / `Complex Normal Filter Softness`, and added `Complex Specular Map Influence`. These settings now correspond to real filtering/masking behavior rather than implying a generated-grass population LOD control.
- Moved grass GGX normal-variance evaluation outside the variable-length RadiantGrid loop and evaluate it once per pixel. Sun and local lights consume the same normalized anti-aliased roughness; this repaired strict FXC X3595 derivative-in-varying-loop failures without modifying the shared FoliageDynamics include, so Lighting permutations are not invalidated by this iteration.
- Strict FXC `/Ges /WX /O3` validation passes the base complex-grass pixel shader, the full SkyBounce + ContactShadows + AmbientProbe + WorldProbes + RadiantGrid + NaturalLighting pixel permutation, and the Foliage Dynamics vertex shader.
- Incremental Release `PIXLRenderer` build passed. New build/live DLL SHA-256 is `86D1ECDA635E5B356701B55B0FD18DAA163D7853926BBD18D51E4936785EFACC`; only inherited FidelityFX `MSB8028` shared-intermediate warnings remain.
- Deployment completed with Skyrim closed. Only the DLL, `RunGrass.hlsl`, and `FoliageDynamics/FoliageTuning.hlsli` were replaced. The shared `FoliageDynamics.hlsli` remains byte-identical to the prior live file, deliberately avoiding broad Lighting invalidation.
- Canonical/live shader reconciliation passes 242/242 with zero different, source-only, live-only, backup-like, or overlay-collision entries. `tools/AuditPixlRenderer.ps1` passes all 36 integrated modules.
- Rollback checkpoint `build/checkpoints/20260824-grass-complex-v3-predeploy` contains the previous DLL, three relevant live foliage files, and all eight existing RunGrass cache stages (12 files / 23,334,629 bytes). No cache file was manually deleted.
- Added `pipeline/WindowLife/ASSET_SPEC.md` for a future authored layered-interior path: ten separate 1024x1024 lossless room sources packed into a 4x4 4096x4096 BC7 sRGB atlas; separate 512x512 straight-alpha curtain and occupant sources packed into 2048x2048 atlases; 16-pixel gutters/edge duplication; curtain, occupant, and room layers at independent depths. Missing assets must preserve the current procedural fallback.
- Generated non-runtime concept references locally: a 4x4 semi-coloured blurred Nordic occupant atlas at `C:\Users\PIXL STUDIO PC\.codex\generated_images\01a02f8d-45f1-73d2-9ae4-82449f6e00d2\exec-7d91bb0e-357f-43f9-a665-7d28b55a861b.png` and a 4x4 sixteen-room Nordic medieval interior atlas at `C:\Users\PIXL STUDIO PC\.codex\generated_images\01a02f8d-45f1-73d2-9ae4-82449f6e00d2\exec-a45d4045-2313-43a5-a10e-dd12d154ae84.png`. They are concept/source images only, not yet packaged, bound, licensed as release assets, or copied into canonical/live shaders.
- Beta staging remains intentionally untouched pending owner runtime approval.
- Next runtime expectation: only RunGrass-owned cache stages should be invalidated/recompiled. Test forced complex grass at the previous boundary, then inspect full blade length under the same sun angle while varying `Complex Specular Map Influence` between `0.0` and `0.15`. Also confirm SSS parity remains intact.

## Latest Active-Development Checkpoint — 2026-08-24 WindowLife v0.5 + mirrored grass specular

- Owner runtime result for the preceding grass build: PASS. Selective compilation was "insanely fast", the forced-complex material boundary is gone, full-blade grass response looks good, and SSS parity remains intact. Preserve this repaired grass material path as the new known-good baseline.
- Integrated the generated PIXL room concepts as `pipeline/WindowLife/Kernels/WindowLife/RoomAtlas.png`: a 2048x2048 sRGB, fully opaque PNG containing sixteen 512x512 cells. Runtime path is `Data/Shaders/WindowLife/RoomAtlas.png`; GPU allocation is a single-mip 2048 RGBA sRGB texture.
- WindowLife now loads the atlas with DirectXTex/WIC, binds it at Lighting PS `t126`, and rebinds it alongside the existing per-draw structured SRV on `t127` for every Lighting draw. Missing, failed, disabled, or incorrectly sized atlas input logs one actionable warning and retains the procedural room/reveal fallback.
- Expanded `WindowLife::PerGeometryData` and matching HLSL `PerDrawData` from 160 to 176 bytes with one aligned `Asset0` float4. It carries regional room family, atlas-ready/enabled state, authored-room blend strength, and tile count. FeatureData b6, all constant-buffer registers, existing payload offsets c0-c9, and t127 are unchanged.
- Added default-on `Authored Room Backgrounds` and `Authored Room Visibility` (`0.78`) settings with JSON persistence. WindowLife is now version `0.5.0`.
- The room atlas is sampled on a recessed `1.34x` parallax back plane beneath the existing shallow analytic curtains and independently moving occupants. Sampling flips texture V to world-up, clamps within a 2.5-texel cell inset, fades outside the physical room rectangle, remains clipped by Skyrim's full-resolution pane/glow mask, and is exterior-only; indoor passer behaviour is unchanged.
- Authored room colour is exposure-matched to the source window emission instead of replacing its brightness blindly. Legacy and Linear Light Core output paths receive the proper colour-space conversion.
- Added deterministic regional material families in native classification: general Nordic/Whiterun, Solitude/castle/noble, Riften/canal timber, Windhelm/dark stone, Markarth/Dwemer, and inn/shop/trade. Each family selects from a curated stable subset of the sixteen rooms per resolved logical window.
- Added `pipeline/WindowLife/ROOM_ATLAS_PROVENANCE.md`. The atlas was generated from an original PIXL text prompt without external images or named-franchise source assets, then mechanically cropped/resized/repacked. It remains subject to the normal release/publisher licensing review.
- Added `Mirror Complex Normal Y (Specular)`. Foliage tuning payload version is now 4 and expands its dedicated b13 block from 80 to 96 bytes with aligned c5 storage; C++ and HLSL sizes match. The option is default-off and JSON-persisted.
- Mirrored complex-grass Y is specular-only: diffuse normals and SSS remain on the already-approved primary normal. When enabled, PIXL evaluates a second tangent-Y-mirrored normalized GGX lobe for sun and RadiantGrid local lights, averages it with the primary lobe, and filters both roughness values outside variable light loops. This broadens orientation coverage without doubling energy; existing Specular Strength and Normalized GGX Response remain the intensity controls.
- Strict FXC `/Ges /WX /O3` passes WindowLife Lighting base, deferred, environment-map, Material Forge, and specular permutations. It also passes complex RunGrass base PS, full SkyBounce + ContactShadows + AmbientProbe + WorldProbes + RadiantGrid + NaturalLighting PS, and Foliage Dynamics VS.
- Release `PIXLRenderer` build passed after CMake regeneration; the 36-module audit passes. Build/live DLL SHA-256 is `907A2EB94E1201FA7B9B3805DACEBA0BB188B16E4398702865575F21D46FF4AB`.
- Deployment completed after Skyrim closed. Canonical/live shaders and assets reconcile 243/243 with zero different, source-only, live-only, backup-like, or overlay-collision entries. Beta staging remains untouched.
- Focused rollback checkpoint `build/checkpoints/20260824-windowlife-v5-mirrorgrass-predeploy` contains the prior DLL, six affected live files, all 589 prior Lighting cache stages, and all eight prior RunGrass stages (603 files / 986,443,418 bytes).
- Expected next-launch invalidation is deliberately limited to Lighting (WindowLife v0.5/source/interface change) and RunGrass (tuning v4/source change). All unrelated persistent shader families must remain present. The room PNG itself does not create shader permutations.
- Next visual test: verify the log reports the room atlas as `ready`; inspect Solitude, Whiterun/general Nordic, Riften, Windhelm, Markarth, a tavern/shop, small panes, large panes, and oblique camera movement. Then enable `Mirror Complex Normal Y (Specular)` and lower Normalized GGX Response/Specular Strength as needed while comparing distant sheen against wet ground.

## Latest Active-Development Checkpoint — 2026-08-24 Natural Skin / Tissue Diffusion 3.3

- Completed a full C++/HLSL trace across Skin Optics, Tissue Diffusion, Dialogue Focus, Lighting direct/indirect composition, deferred mask output, compute dispatch, and live runtime copies before changing the skin path.
- Root-caused a major SSS contract defect: deferred `MASKS` uses R11G11B10 and Lighting writes x=SSS amount, y=human-profile blend, z=directional ambient luminance; the texture's implicit w is coverage/1. Earlier Tissue Diffusion code misread z/w as keratin/fur, so ordinary opaque skin was treated as hardened tissue and its warm diffusion path was shortened to roughly 32%.
- Restored the real mask contract without changing its format, register, or C++ layout. Base/Human selection remains in y; z/w are no longer interpreted as species/material masks. Cross-profile samples now receive continuous rejection to prevent diffusion between unlike adjacent actors/materials.
- Burley SSS now treats mask x as contribution strength rather than multiplying mean-free path. Physical radius remains stable at antialiased face edges; the final scattered result blends by coverage, sample coordinates are safely clamped, mask strength is applied once instead of squared, and empty neighborhoods fall back to the center irradiance instead of darkening.
- Separable SSS now blends the complete Base/Human kernel continuously per tap, rejects non-skin/profile-mismatched samples, and applies SSS coverage once in the final vertical pass. It retains the existing 21-tap ABI and cbuffer layout.
- Rebuilt skin micro-detail composition from a unit reconstructed tangent-space normal. The old fixed z=0 vector plus hand-restored base z produced brittle pore slopes; the new path clamps XY safely, reconstructs z, performs normalized RNM composition, and samples AO at the same biased mip as the normal.
- Replaced resolution/distance-dependent normal-derivative curvature with geometric-normal change divided by the matching world-position footprint. Facial macro curvature is now stable across dialogue distance and resolution while pores remain excluded from curvature broadening.
- Direct skin lighting keeps both normalized GGX lobes, uses terminator/back-light-only tissue transmission instead of a constant red front-light floor, and consumes detailed/soft shadow visibility once rather than squaring local shadows. This targets natural ear/nostril transmission without a waxy diffuse fill.
- Indirect skin lighting now preserves the two roughness lobes separately, applies multi-scatter compensation per lobe, no longer deletes probe specular on curved cheeks/noses/ears, and adds a restrained energy-layered grazing peach-fuzz response.
- Dialogue Focus keeps its existing private b13 ABI and conservative mask/quality boosts. The corrected tissue amount semantics make the existing focused-NPC SSS lift operate as a real strength adjustment rather than shrinking the blur radius.
- Skin Optics is version `0.3.0`; Tissue Diffusion is version `3.3.0`. `TissueDiffusion::HasShaderDefine` now truthfully reports Lighting only. This fixes the previous whole-library invalidation behavior for Tissue profile/version changes; its direct compute kernels remain owned by `ClearShaderCache()`.
- Strict Windows SDK 10.0.26100 FXC `/Ges /WX /O3` passes five valid skin Lighting PS permutations (FaceGen and RGB-tint, model-space and tangent normals, base/full rain/SkyBounce/contact/probe lighting) plus DiffuseExtraction, Burley, separable-horizontal, and separable-vertical compute shaders. Invalid MaterialForge+skin and incomplete ENVMAP test combinations were identified as non-production permutations and were not counted as failures.
- Release build initially exposed a FidelityFX generated-header dependency race after CMake regenerated for the module versions: the generator crashed once with `0xC0000409`, then the backend observed one header before generation completed. Serial generation/retry repaired the build tree; the following Release build and incremental header rebuild both pass. Only inherited FidelityFX `MSB8028` shared-intermediate warnings remain.
- The 36-module audit passes after the dependency fix. Live deployment completed with Skyrim closed. Build/live DLL SHA-256 is `87901917872F3B4B9C02752BD6CBEAB5EDAEB5676E830018A2AE7FAB5ED5BA61`; canonical/live shader reconciliation passes 243/243 with zero different, source-only, live-only, backup-like, or overlay-collision entries.
- The 3,435-file / 2,086,188,526-byte pipeline library was preserved byte-for-byte during deployment. Its metadata still records Foliage `2.0.5`, WindowLife `0.4.1`, Skin Optics `0.2.0`, and Tissue Diffusion `3.2.0`; live versions are `2.1.0`, `0.5.0`, `0.3.0`, and `3.3.0`. The next launch should invalidate the union of 589 Lighting stages and eight RunGrass stages once, preserving the other 2,837 compiled stages rather than rebuilding the whole library.
- The latest pre-deployment runtime log contains no shader/compiler failure, exception, assertion, device-removal, or out-of-memory evidence. It predates this skin build and therefore cannot validate the new visual/runtime path. The optional Terrain Field plugin warning remains unrelated.
- Rollback checkpoint `build/checkpoints/20260824-skin-optics-v3-predeploy` preserves the previous live DLL and affected skin/tissue/Lighting shader files. The earlier WindowLife checkpoint already contains all 589 pre-change Lighting cache stages, so no redundant near-gigabyte cache copy was made.
- `UserGraphics.json` and beta staging were not modified. Existing user tuning remains authoritative; visual validation should compare the same NPC/settings before changing F0, roughness, tissue distance, or skin colour.

### Natural Skin Runtime Validation Pending

1. Let the one-time selective Lighting/RunGrass compile finish and confirm the log reports selective invalidation, not a full cache deletion.
2. Test the same human NPC in dialogue under diffuse daylight, hard sun, warm fire/local light, interior ambient light, and strong ear back-light. Check for stable cheek/nose highlights, visible but restrained pores, warm thin-feature transmission, and no red front-lit wax layer.
3. Pan and move between close and medium dialogue distances; curvature/specular width and SSS radius should remain stable rather than popping with resolution or distance.
4. Inspect face silhouettes against bright and dark backgrounds for SSS bleed, dark mask-edge halos, temporal crawl, and DLSS/DLAA differences.
5. Check at least one Argonian/Khajiit and one body-skin material. Base/Human profiles must remain distinct without the previous false opaque-skin hardening.
6. Review `PIXLRenderer.log` for Skin Optics/Tissue shader compile failures and Dialogue Focus `ACTIVE`/GPU-bind confirmation after the test.

### Permutation-Granular Cache Policy — 2026-08-24

- The first Natural Skin/Foliage/WindowLife launch proved that startup validation no longer deleted the whole library: the runtime log reported `Selectively invalidated 597 cached shader stages (966807244 bytes) for 4 changed module(s); preserved unrelated pipeline families`.
- Direct filesystem inspection during compilation confirmed 2,837 old stages remained intact. The long wait came from all 589 expensive Lighting stages plus eight RunGrass stages being removed at family granularity, while the compiler card did not distinguish disk hits from actual compilation.
- Added `RenderModule::AffectsCachedShader(type, descriptor, stage)` as a conservative cache-ownership refinement. Default behavior remains whole-family invalidation; only audited modules opt into narrower scopes.
- Audited scopes are now: Foliage Dynamics = all RunGrass plus Lighting TREE_ANIM; Tissue Diffusion = FaceGen/FaceGen-RGBTint pixel shaders; Skin Optics = FaceGen, hair, and other skinned-actor pixel shaders; WindowLife = eligible static-object Lighting pixel shaders only.
- Startup invalidation now parses the existing eight-hex-digit descriptor filenames, removes only entries matched by changed modules, and re-stamps explicitly unaffected entries in shared top-level families. This prevents `Skip Unchanged Shaders` from immediately overriding the granular decision through coarse `Lighting.hlsl` modification time.
- The compiler card now displays `DISK CACHE` and `BUILT NOW` counts separately.
- Against the preserved 597-stage checkpoint, individual expected scopes are approximately 51 Foliage stages, 64 Tissue stages, 128 Skin/actor stages, or 272 WindowLife stages. The current four-module union would be about 451 rather than 597; normal single-feature iterations receive the larger reduction.
- Release build passed. Audit passed with 36 integrated modules; canonical/live shaders remain 243/243 identical. Built DLL SHA-256: `62CAE03949D3C7C9F4BECBE0C2B971C041C4CA225E742823D7B6EC7727CF381F`.
- Skyrim was subsequently closed and the permutation-granular Release DLL was deployed to the live hard-linked Vortex/game target. Source/live DLL SHA-256 is `62CAE03949D3C7C9F4BECBE0C2B971C041C4CA225E742823D7B6EC7727CF381F`.

## Checkpoint - 2026-08-25 WindowLife 0.5.1 Optical Interior Repair

- Diagnosed the supplied arched/cathedral-window capture. The authored room atlas was loaded and bound, but the shader discarded every interior layer when the coarse glow-layout scan could not prove all four rectangular edges. Arches, curved tops, and dense mullions readily fail that strict gate. At oblique/upward views the unconstrained back-plane offset could also move a successfully resolved room completely outside its atlas cell.
- Split layout eligibility into a tolerant background confidence and the existing strict occupant confidence. Irregular windows now receive a pane-clipped room atlas, while people and curtains still require the strict boundary result that protects the known Solitude facade/helper-mesh case.
- Shallow tier-2 windows now receive the recessed background and edge layer without people. Tier-3 windows receive occupants only when their authored layout is safe.
- Added a UV-tile-bounded two-texel glow-core erosion, tightened flat-normal evidence, and added restrained diffuse support so luminous outer trim/frame halos are rejected before any glass/interior work.
- Added room-wall parallax constraints for the curtain, depth, atlas, and occupant layers. The atlas back plane now uses a deeper `1.58x` separation without disappearing at upward/grazing views.
- Changed the final optical composite so the exposed room atlas dominates pane pixels: room exposure can recover the deliberately dark atlas, flat source diffuse is attenuated, and only a faint source stained-glass/emissive tint remains. This specifically targets the reported opaque yellow-paint appearance.
- Bumped WindowLife runtime metadata from `0.5.0` to `0.5.1`. With the deployed granular-cache DLL, the next startup should invalidate only eligible static Lighting pixel permutations, not the complete pipeline library.
- Representative WindowLife pixel permutations compiled successfully with FXC: base, vertex-colour, environment-map, parallax, and Linear Light Core variants. `git diff --check` passed; the PIXL audit passed with 36 integrated modules.
- Canonical/live shader reconciliation passed at 243/243 exact matches after deployment. The desktop beta package remains untouched pending visual approval.
- Runtime validation remains required: inspect the supplied cathedral windows, the original Solitude facade, representative small/round/leaded windows, and oblique camera travel. Confirm room detail is legible, frames remain excluded, parallax reads as depth, and no proxy mesh reappears.

## Checkpoint - 2026-08-25 WindowLife 0.5.2 Contrast-Preserving Exposure

- Owner runtime capture proved the recessed room atlas is present, correctly clipped to the supplied narrow leaded window, and no longer exposes surrounding wall/frame geometry. The remaining defect was insufficient room visibility and weak interior contrast.
- Reviewed the renderer log. Hybrid GI was disabled at boot at `00:57:04`, the pipeline cache completed successfully at `01:29:41`, and Hybrid GI was manually enabled at `01:46:32`. No Hybrid GI compiler failure is present; the owner confirmed the feature is working. Hybrid GI source/runtime integration was therefore left unchanged.
- Diagnosed the low-contrast room composite: exposure was normalized independently from each sampled room texel. That pushed dark furniture and bright room lights toward the same target luminance, suppressing the very contrast that sells depth.
- WindowLife 0.5.2 now derives exposure from the known authored RoomAtlas mean instead. It preserves the atlas's native lighting/furniture contrast and uses a restrained visibility floor interpolated from the existing day/night window activity state. Linear Light Core and legacy output paths use corresponding linear/sRGB reference values.
- Source and live WindowLife metadata were bumped from `0.5.1` to `0.5.2`. Five representative Lighting pixel permutations (base, vertex colour, environment map, parallax, and Linear Light Core) compiled successfully with FXC. PIXL audit passed; canonical/live shader reconciliation remains 243/243 exact.
- Skyrim restarted while validation was finishing. File watching is disabled in the current user configuration, so the already-running process retains its prior compiled shader; WindowLife 0.5.2 takes effect on the next normal restart/selective cache validation. Do not mutate additional live shader/DLL files until Skyrim closes.

## Active Dependencies / ABI Notes

Record task-specific sensitive interfaces here.

Examples:

- C++ struct ↔ HLSL cbuffer;
- SRV/UAV register;
- descriptor slot;
- CommonLib type/API version;
- shader define dependency;
- module initialization ordering.

- `WindowLife::PerGeometryData` is shared between C++ and HLSL as a structured buffer element; this task extends both sides from 128 to 160 bytes with matching `Interior0` and `Geometry0` fields.
- WindowLife binds its per-draw structured buffer at `t127`; resource slot and descriptor lookup are unchanged.
- WindowLife `Interior0.zw` now use the previously reserved floats for authored-mask layout and interior passer enable flags. The payload remains exactly 160 bytes on both C++ and HLSL sides; no register, descriptor, resource-format, or downstream offset changed.
- SkyBounce common-buffer camera anchoring feeds `ShadowVisibilityProbeArray` sampled by enhanced vegetation SSS; first-person correctness depends on `frameBufferCached.GetCameraPosAdjust()` rather than stale `shadowState::posAdjust`.
- Grass/tree enhanced wind is compile-time present but runtime-controlled through `foliageDynamicsSettings.EnableEnhancedWind`; vanilla displacement remains authoritative.
- `FoliageTuningData` is a dedicated C++/HLSL b13 payload and is now 80 bytes on both sides. New c4 values are detail-distance scale, transition softness, normalized-GGX response, and padding; FeatureData b6 is unchanged.
- `RoofRunoffTuning` is now 32 bytes on both C++ and HLSL sides, carrying active render size and inverse size. Runoff resources are allocated in the active DLSS render domain; current depth, history, emitters, drops, and composite all use that same domain.
- WindowLife retains the existing 160-byte `PerGeometryData` payload and t127 binding. The `0.4.1` repair is shader-only and does not change its C++/HLSL ABI.
- WindowLife v0.5 supersedes that historical 0.4.1 note: its C++/HLSL per-draw payload is now 176 bytes, preserving c0-c9 and adding aligned `Asset0` at c10. RoomAtlas occupies Lighting PS t126; the structured payload remains t127. Both are rebound together by WindowLife's final per-draw Lighting hook.
- Foliage tuning v4 uses a 96-byte dedicated PS b13 payload. c0-c4 are unchanged from the approved grass build; c5 adds the mirrored-specular-Y flag plus three padding uints.
- Direct Rain Response kernels are module-owned COM resources. `RainResponse::ClearShaderCache()` releases all four kernels and resets their retry state without touching the persistent Skyrim pipeline library.
- Tissue Diffusion's deferred mask contract is x=SSS amount, y=Base/Human profile blend, z=directional ambient luminance, and w=implicit coverage/default from the R11G11B10 mask target. z/w are not keratin/fur classifications.
- Skin Optics still uses the existing seven-float4 (112-byte) shared payload, PS b7 per-geometry wetness buffer, skin detail t72, RFAOS t71, and wetness t74/t75. No cbuffer size/offset, register, resource format, descriptor slot, or ABI changed in the natural-skin pass.
- `TISSUE_DIFFUSION` is a Lighting-only global define. The module's compute shaders are compiled directly and released through `TissueDiffusion::ClearShaderCache`; they are not Skyrim pipeline-library families.
- Persistent cache layout remains `PIXL.StageShard.v1`. Permutation-granular startup validation relies on the existing cache filename contract beginning with the eight-hex-digit descriptor and conservatively deletes an affected-family entry if that descriptor cannot be parsed.
- WindowLife 0.5.1 is shader/settings-metadata only: the existing 176-byte C++/HLSL per-draw payload, t126 room atlas, t127 structured buffer, formats, and descriptor slots are unchanged.
- WindowLife 0.5.5 adds optional Lighting PS SRV `t125` for an exact per-material pane mask. `t126`, `t127`, the 176-byte structured payload, all payload offsets, and GPU formats remain unchanged. Reserved `Class0.z` bits identify native glow versus external exact mask so Lighting never samples an unbound source.
- WindowLife 0.5.6 is HLSL/settings-metadata only. It does not change t125/t126/t127, the 176-byte payload, any register, format, descriptor, or C++/HLSL ABI.

## Files Changed in Current Task

- `engine/ShaderCache.h`
- `engine/ShaderCache.cpp`
- `engine/RenderModule.h`
- `engine/Menu/OverlayRenderer.cpp`
- `engine/Utils/UI.cpp`
- `engine/Modules/FoliageDynamics.h`
- `engine/Modules/FoliageDynamics.cpp`
- `engine/Modules/RainResponse.h`
- `engine/Modules/RainResponse.cpp`
- `engine/Modules/WindowLife.h`
- `engine/Modules/WindowLife.cpp`
- `distribution/Shaders/RunGrass.hlsl`
- `pipeline/Foliage Dynamics/Module.ini`
- `pipeline/Foliage Dynamics/Kernels/FoliageDynamics/FoliageDynamics.hlsli`
- `pipeline/Foliage Dynamics/Kernels/FoliageDynamics/FoliageTuning.hlsli`
- `pipeline/WindowLife/Module.ini`
- `pipeline/WindowLife/ASSET_SPEC.md`
- `pipeline/WindowLife/Kernels/WindowLife/WindowLife.hlsli`
- `pipeline/Rain Response/Kernels/RainResponse/RoofRunoffDetectCS.hlsl`
- `pipeline/Rain Response/Kernels/RainResponse/RoofRunoffResolveCS.hlsl`
- `pipeline/Rain Response/Kernels/RainResponse/RoofRunoffGenerateCS.hlsl`
- `pipeline/Rain Response/Kernels/RainResponse/RoofRunoffCompositeCS.hlsl`
- `extern/FidelityFX-SDK/sdk/src/backends/dx11/CMakeLists.txt`
- `engine/Modules/TissueDiffusion.h`
- `distribution/Shaders/Lighting.hlsl`
- `pipeline/SkinOptics/Module.ini`
- `pipeline/SkinOptics/Kernels/SkinOptics/SkinOptics.hlsli`
- `pipeline/Tissue Diffusion/Module.ini`
- `pipeline/Tissue Diffusion/Kernels/TissueDiffusion/SSSCommon.hlsli`
- `pipeline/Tissue Diffusion/Kernels/TissueDiffusion/Burley.hlsli`
- `pipeline/Tissue Diffusion/Kernels/TissueDiffusion/SeparableSSS.hlsli`
- `tools/PixDiTEnhance/train_real_data.py`
- `tools/PixDiTEnhance/export_and_test_onnx.py`
- `tools/PixDiTEnhance/README.md`
- `tools/PixDiTEnhance/TRAINING_GUIDE.md`
- `.gitignore`
- `.gitattributes`
- `README.md`
- `PRIVATE_COMPONENTS.md`
- `SOURCE_DEPENDENCIES.md`
- `tools/ExportPixlPublicSource.ps1`
- `tools/PixDiTEnhance/PRIVATE_LICENSE.md`
- `cmake/FidelityFX-SDK.cmake`
- `cmake/patches/FidelityFX-DX11-Short-Output.patch`
- `PIXL_ACTIVE_STATE.md`

## Root Cause / Engineering Findings

- WindowLife classified any texture path containing architectural tokens as a candidate. Runtime evidence includes `sroofbluemetal01.dds`, `swoodbeam01.dds`, `smetaldoorl01.dds`, `stoneedgetrim01.dds`, `sgravestone01.dds`, and other non-window materials.
- The shader debug mask retained an 18% whole-material tint even when its pane mask was zero, exaggerating the false-positive geometry.
- Pane selection relied heavily on diffuse/gloss brightness and weak glow evidence, so raised/decorative frame areas could survive the mask.
- Window rooms used a fixed world-space extent and depth; small panes could intersect only a narrow part of an occupant, visually collapsing the silhouette into a line.
- SkyBounce centered its shadow-visibility probe field from `shadowState::posAdjust`, already documented elsewhere in PIXL as stale in first person. Enhanced grass SSS consumes this probe visibility, explaining the first-/third-person discontinuity.
- Enhanced wind multiplied the entire authored Skyrim bend by the shipped `WindStrength=2.0`, over-deforming foliage cards and exposing unstable reflective facets.
- Fifty-two installed Solitude NIFs reference Window Shadows-style helper textures under `textures\masks\swindow##_mask.dds`. Because those proxy paths contain `window`, the first revision still classified helper proxy geometry as a real pane and exposed it through the building.
- The original Solitude `_g.dds` glow maps already encode clean glass-only masks with dark frames/background. For glow-authored windows, diffuse/normal fallback thresholds were unnecessarily widening the pane selection onto frames or surrounding geometry.
- In complex grass PS permutations, SkyBounce probe visibility completely replaced detailed raster shadow visibility. A low/coarse or camera-displaced probe could therefore zero otherwise lit first-person grass transmission.
- Grass directional transmission was highly dependent on the presented side of a two-sided/card normal. Small first-/third-person view and deformation differences could flip the thin-sheet hemisphere and remove the bright backscatter term.
- The existing World Benchmark was a 20-location COC/wait/single-screenshot loop: it had no camera path, feature-focused route, benchmark quality contract, frame-time distribution, clean promo framing, settings snapshot, or resilient fallback for uncertain exterior COC names.
- Roof runoff treated output-resolution logical pixels as dynamic-resolution UVs twice: reconstruction multiplied normalized output UV by inverse render scale, while current/previous projection mapped through dynamic-resolution scale before indexing output-sized runoff buffers. Under DLSS this displaced world anchors/history and made tiny camera motion invalidate the effect.
- Roof detection used fixed 8/16-pixel eave samples and fixed 6/12-pixel roof-plane samples. Close geometry therefore represented too few world units to pass the physical size gates, explaining the missing near-camera effect.
- Runoff history sampled one nearest half-resolution texel and required a tight seed match. Sub-pixel camera movement could miss that texel, erase accumulated roof water, and leave emitters invisible until they slowly recharged.
- Real architectural glow atlases contain multiple window styles, mullions, and many disconnected bright sub-panes. Treating every connected bright island as a separate window would repeat tiny occupants in every pane; a coarser mask representation is needed to find the logical window group while the original full-resolution glow still clips every layer to the actual glass.
- WindowLife explicitly returned before evaluation whenever `SharedData::InInterior` was true, so the previous effect could never represent people passing outside an interior window.
- The old occupant was a nearly symmetric capsule figure translated rigidly across the pane. On small windows its limbs collapsed into a vertical line, and on larger windows its static outline read as a restroom/signage icon sliding sideways.
- Cold-start cache validation was all-or-nothing. A single module-version mismatch deleted all 3,434 cached stages even when that module only affected one shader family; validation also short-circuited after the first failed module.
- The live-file watcher still had a second full-clear path after cold-start invalidation was repaired: any nested `.hlsl` not present in the pipeline dependency map set `clearCache=true`. Rain runoff uses direct module compute kernels, so editing `Data\Shaders\RainResponse\RoofRunoff*.hlsl` deleted the entire persistent library even though those kernels are not library permutations.
- The watcher did not map top-level `RunGrass.hlsl` back to Skyrim's Grass shader type, and its duplicate-event predicate used AND across changed fields, which could discard distinct filesystem events.
- The apparent grass “LOD” boundary includes a shader-side packed-normal detail fade driven by `AlphaParam1`, not only Skyrim's mesh/card population distance. Moving Skyrim grass generation distance alone would not correct the lighting/material transition.
- Grass GGX was heavily attenuated twice: normal-variance filtering broadened the lobe aggressively and the final authored macro-specular response was multiplied by an additional `0.12`, making normalized highlights effectively unreadable.
- The DLSS runoff defect was a resolution-domain mismatch. Runoff UAVs were display-sized while depth and jittered scene rendering occupied the active dynamic-resolution rectangle, so Balanced mode reprojected/detected/composited different physical pixels; DLAA hid the bug because active and display dimensions matched.
- Window glow atlases split one logical window into disconnected bright islands around mullions. A one-island component fit could shrink occupants into individual subpanes, while failed fitting on a large candidate facade fell back to a repeated procedural room grid and exposed the Solitude proxy/interior mesh.
- The bundled FidelityFX shader compiler has a fixed-size path-handling defect: an absolute H: build output argument triggered `0xC0000409`, while the identical invocation succeeded with a short relative output path.
- Tissue Diffusion's documented mask z/w contract did not match the actual Lighting/G-buffer contract. Because the R11G11B10 mask target effectively returns w=1, nearly all ordinary skin was being treated as maximally hardened/fur-covered tissue.
- Burley applied the SSS amount both to mean-free-path distance and twice to neighboring sample energy, then always selected the scattered result for every positive mask value. This created inconsistent radius, edge darkening, and over-strong partial-coverage diffusion.
- Skin micro-detail decoded the shared normal texture with z=0 after remapping and restored only the base tangent z after RNM. This was not a unit-normal composition and destabilized close-up pore highlights.
- Skin indirect lighting averaged the two specular roughness lobes and then multiplied the result by `1 - Curvature`, contradicting the direct path and making curved facial landmarks unnaturally matte in probe/ambient light.
- Tissue Diffusion advertised `HasShaderDefine=true` for every Skyrim shader type even though only Lighting consumes the define, so any Tissue module-version change could invalidate the whole persistent library.
- Shader-family invalidation was still too coarse for Lighting: a changed skin, window, or tree feature removed every Lighting vertex/pixel stage even when compile-time technique guards made most permutations unable to execute that feature. Coarse `Lighting.hlsl` mtime validation would also have recompiled retained entries unless they were explicitly revalidated.
- WindowLife's authored-layout safety gate conflated a trustworthy moving-person boundary with background visibility. A missing coarse edge therefore suppressed the room atlas entirely on valid arched/mullioned windows. Independently, unconstrained view parallax could push the back plane outside `[0,1]`, reducing its weight to zero at oblique camera angles.
- The final WindowLife composite retained up to 12% flat source emission, only allowed a 2.2x exposure for the deliberately dark room atlas, and left the lit source diffuse largely intact. Even when a room sampled successfully, those choices favored the original opaque yellow texture over interior contrast.
- WindowLife 0.5.5 treated exact external pane masks as though they were noisy native glow atlases. Tall/narrow Solitude pane groups could fail geometry-radius/aspect confidence, and enabling authored-mask fitting then deliberately zeroed the room background, curtains, depth reveal, and occupants. The mask was correct; the fit trust policy was not.
- FidelityFX shader generation originally moved only the generator command onto `P:`. MSVC still opened nested permutation headers through the long canonical build path and failed at a deeply named include despite the file existing. Both generation output and the backend include directory must use the short alias.

## Changes Implemented

- Restricted native WindowLife candidates to explicit window/glass/glow-atlas evidence; generic architecture tokens alone no longer activate the effect.
- Added flat-pane normal evidence and stricter diffuse/glow gates so raised frames/decorative trim are rejected at pixel level.
- Removed whole-material debug tint outside the pane mask.
- Anchored adaptive rooms to each geometry bound, bounded depth for small panes, and added separately controlled shallow curtain and recessed-room layers.
- Added `CurtainStrength` and `RoomDepthStrength` settings/defaults while keeping descriptor/register bindings stable.
- Re-anchored SkyBounce probe data to the cached frame-buffer camera position used by other first-person-safe PIXL systems.
- Changed grass/tree enhanced wind to scale only PIXL's bounded gust/crosswind/flutter delta; zero is exact vanilla motion and the shipped value no longer doubles base deformation.
- Explicitly reject Window Shadows/helper proxy-mask material paths (`/masks/`, `_mask.dds`, `windowshadow`, `window_shadow`) before WindowLife activation, even when the filename contains `window`.
- When a real glow texture exists, use its authored luminance as the authoritative pane mask; diffuse and normal heuristics now remain fallbacks only for windows without glow maps.
- Increased the safe small-pane depth cap and added a derivative-based pane-boundary reveal so recessed layers read more clearly without extending the effect beyond glass.
- Preserve valid detailed raster illumination with `max(detailedShadow, skyProbeVisibility)` instead of allowing the coarse SkyBounce probe to replace it.
- Added a grass-only, modest normal-sign-invariant thin-sheet multiple-scatter term while retaining the existing physical backlight response, directional light color, albedo tint, SSS controls, and real shadowing.
- Bumped WindowLife to `0.3.1` and FoliageDynamics to `2.0.4` so runtime cache metadata reflects these shader changes.
- Rebuilt World Benchmark as `PIXL.WorldBenchmark.v2` with a nine-scene cinematic feature route and retained 20-scene full sweep; feature intent covers all 36 renderer modules.
- Added deterministic Catmull-Rom camera rigs, unpaused native free-camera ownership, clean HUD handling, three reference captures per scene, raw frame-time/GPU distributions, pass timing, settings snapshot, COC fallbacks, and manifest/video cue metadata.
- Added an optional temporary benchmark quality contract: DLSS/DLAA, frame generation off, and optionally frame limiter off. All modified runtime settings and HUD/camera state restore on completion/cancellation.
- Kept video recording external by design for now: in-process encoding would contaminate the benchmark workload. The deterministic clean route and manifest are ready for lossless OBS capture and promo editing.
- Corrected roof-runoff depth reconstruction/projection to keep output pixels, active-resolution depth sampling, and current/previous camera transforms in their proper coordinate spaces.
- Made runoff roof/eave tests adapt their pixel footprint to a stable world-space size near the camera, modestly increased close-drop anti-aliasing coverage, searched a 3x3 reprojection neighbourhood by stable world seed, and added a small current-frame reservoir so disocclusion/motion does not blank the effect.
- Bumped WindowLife to `0.4.0` and added `UseAuthoredMaskLayout` and `EnableInteriorPassers`, with default-on serialization and UI controls. Existing JSON files safely acquire both defaults.
- Added a two-resolution authored-mask layout: a coarse glow mip finds the logical window-group extents for scale/placement while the full-resolution glow mask remains authoritative for panes, mullions, frames, curved edges, and final compositing. Manual procedural width/height remains only as a fallback for materials without usable glow masks.
- Reconstructed WindowLife room axes from material-UV derivatives so mirrored/flipped atlas UVs do not turn occupants upside down, and applied consistent view-parallax offsets to the room, curtain, and occupant layers.
- Replaced the icon-like occupant with phase-animated segmented limbs, gait/bob, profile detail, and seeded cloak, robe, pack, and carried-object variants. Paired occupants use independent variants.
- Enabled subtle exterior passers when viewed from interiors. Indoor evaluation uses shorter/faster walking-biased events with reduced shadow strength and deliberately omits the curtain/recessed-room layers, so it reads as outside movement briefly attenuating window light rather than an interior room projected onto the glass.
- Implemented conservative per-module/per-shader-family cold-start cache invalidation. Plugin/layout changes and ambiguous mappings still force a full clear, while a known WindowLife mismatch now removes only Lighting stages; every module is validated even after an earlier mismatch.
- Moved high-frequency GroundResponse projectile, magic-event, and utility-focus telemetry from info to debug while preserving warnings, errors, and actionable diagnostics.
- Completed a Release build, strict shader compiles, source/live shader reconciliation, rollback checkpoint, and deployment while Skyrim was closed. The beta package remains intentionally untouched pending owner visual approval.
- Added reasoned persistent-cache operations and family-specific invalidation. Direct module kernels now hot-release only their owning module resources; module includes invalidate only declared dependent families; `RunGrass` maps to Grass; full clears are reserved for plugin/layout or genuinely ambiguous shared dependencies.
- Fixed watcher event de-duplication so a change in path, action, or directory is enough to retain a distinct event.
- Added Foliage Dynamics tuning payload version 2 with real-time `Grass Detail Distance` (default `1.35x`), `Detail Transition Softness` (default `1.0x`), and `Normalized GGX Response` (default `1.0`) controls. The RunGrass packed-normal transition now uses an independently scaled centre and softened half-width.
- Restored a readable normalized grass GGX response by reducing excessive specular anti-alias broadening, removing the hidden `0.12` energy loss, strengthening authored blade masks, and retaining the existing macro gloss/specular controls as authoritative user controls.
- Rebuilt runoff in active render pixels from allocation through detection, history, emitter projection, drop generation, and composite. Projection now uses the jittered camera matrix that produced current depth, and the composite queries the runoff mask dimensions rather than assuming the display target size.
- Added runoff bead depth cues from the drop-mask gradient: a restrained dark rim/core and cool lens highlight improve convexity without broad dilation or debug-colour leakage.
- Upgraded WindowLife to `0.4.1`: glow evidence must also pass flat/continuous pane-normal evidence, guide scanning tolerates narrow mullions, all subpanes derive a shared provisional centre/seed/rescan layout, and explicit size/aspect confidence gates reject facade-sized components.
- WindowLife now keeps glass optics but suppresses uncertain interior occupants when an authored glow layout fails, rather than falling back to a repeated building-wide room grid. Occupants use a lower common floor anchor, deeper seeded room placement, shallower curtains, and the existing articulated/variant/indoor-passer system.
- Changed the FidelityFX DX11 generator to a portable short relative output argument while preserving CMake's full dependency graph, allowing a genuinely clean Release build to complete.
- Corrected the Tissue Diffusion deferred-mask interpretation and rebuilt both Burley and separable sampling around stable physical radius, single-application coverage, continuous profile blending, and depth/normal/material edge rejection.
- Reconstructed unit skin micro normals, normalized their RNM composition, and made curvature distance/resolution stable from geometric derivatives.
- Preserved dual normalized-GGX skin lobes in direct and indirect light, restored curved-feature ambient reflectance, added restrained peach-fuzz IBL, and removed front-lit/double-shadowed transmission artifacts.
- Narrowed Tissue Diffusion's cache dependency to Lighting, preventing future Tissue profile/version changes from deleting unrelated shader families.
- Added stage/descriptor-aware cache ownership for the four active shader features, permutation-filtered startup deletion, explicit revalidation of unaffected shared-family entries, and visible disk-hit versus built-now compiler counters.
- Upgraded WindowLife to `0.5.1` with separate background/occupant layout confidence, shallow-tier room backgrounds, constrained room-wall parallax, stricter glass-core/frame rejection, a deeper atlas plane, and room-dominant emission/diffuse compositing.
- Upgraded WindowLife to `0.5.6`: exact external masks receive trust-appropriate world-size/aspect gates, and an exact-mask fit failure retains the existing room/silhouette system while the full-resolution authored mask clips every contribution to glass. Native glow maps retain strict fit requirements, preserving facade/helper-mesh safety.
- Added a reproducible top-level FidelityFX DX11 patch with optional `PIXL_FFX_SHORT_BINARY_ROOT`. A genuine clean Release rebuild regenerated all DX11 permutations under `P:/build/PIXL-12C/ffx-dx11-shaders` and completed successfully in 194.4 seconds. Only upstream-style MSBuild MSB8028 shared-intermediate warnings remained.
- Replaced the root GitHub README with a first-person PIXL project page covering architecture, the 36 integrated modules and renderer services, installation, build/staging, Community Shaders v1.8.3 ancestry, GPL-3.0/source obligations, third-party notices, and PIXL music links.
- Synchronized the clean Release DLL to live Data and beta. Final DLL SHA-256 is `3C615F0DADD8C5718E63C01B3CEB7722D01CA3393207B0F2A2ADABBE733C416B`; WindowLife 0.5.6 HLSL SHA-256 is `E7F6575B043C52FB65E924FD2990C54A480C2A77B306B0FCD5FFC0EAAEA04683` across source/live/beta. The audited beta contains 36 modules, the room atlas, no PixDiT/models, and no shader pipeline cache.
- Re-ran the renderer/package audit successfully and reconciled all 243 canonical shaders against live Data: 243 matches, zero differences, zero source-only/live-only files, and zero overlay collisions.
- Created local private-development checkpoints `c5ce2a0c`, `3bf6a6e2`, and `61477e8d`. The audited public-source export is `dist/PIXL-Renderer-v1.0-Source.zip` (75,827,696 bytes, 808 entries, SHA-256 `88727530520660A6255406FF6EC805433CDD10F6BCAEEEB259C1159DA3723060`); it excludes the independent private PixDiT workspace, internal ledgers, generated builds, runtime Data, model weights, and shader cache.
- Final beta inventory is 287 files / 162,588,035 bytes, includes the WindowLife room atlas, and contains zero `.pixlbin`, model-weight, or PixDiT files. Authenticated SSH read access to the new PIXL `origin` passes; no refs or source have been pushed.

## Launch Candidate Test / Fix Checklist — 2026-08-25

### 1. Cold-cache and runtime health

- [ ] Let the current permutation build finish without deleting the library again. Record total, disk-cache, built-now, failed, and elapsed counts.
- [ ] Inspect PIXLRenderer, game, and shader-compiler logs for compile failures, initialization errors, missing resources, repeated invalidation, and non-actionable spam.
- [ ] Restart once against the completed cache and confirm a normal warm launch does not rebuild the library.

### 2. Photo Mode state safety

- [ ] Reproduce the Photo Mode button being active from World Map, Inventory, and other vanilla/modal UI menus.
- [ ] Establish one shared gameplay-state eligibility function and use it both to disable the button and to reject activation at the runtime entry point. A UI-only disable is insufficient.
- [ ] Block activation whenever World Map, Inventory/Magic/Favorites, Container/Barter/Crafting, Journal/Pause, loading/transition, or another incompatible vanilla menu owns input. Keep PIXL's own Photo Mode controls usable after a valid gameplay activation.
- [ ] If an incompatible menu opens while Photo Mode is active, exit or suspend safely and restore camera, player visibility, HUD, input, time state, FOV and post-process state exactly once.
- [ ] Verify first/third-person transitions and near-camera movement cannot expose the player body clipping through the Photo Mode camera.
- [ ] Test mouse/keyboard and controller activation, cancellation, save capture, repeated entry/exit, fast travel/loading, death and save reload.

### 3. Advanced-menu accessibility and layout

- [ ] Reproduce the difficult `Radiance Weave -> Vanilla SSAO` toggle and determine whether the fault is hit-box size, overlapping widgets, disabled-state logic, scrolling, input capture, or insufficient padding.
- [ ] Audit every module panel for minimum row height, full checkbox/button hit regions, consistent horizontal/vertical padding, readable labels/tooltips, unclipped controls, predictable disabled states, and scroll access.
- [ ] Validate at 1080p, 1440p and 4K/UI scaling, including long translated labels, narrow menu widths, mouse and controller navigation.
- [ ] Ensure dangerous/debug controls are visually separated from normal tuning and that defaults/reset actions are clear and recoverable.

### 4. Main quality-menu wiring

- [ ] Build an explicit mapping table from every main quality preset/tier to every affected module field and expected runtime cost/visual result.
- [ ] Trace each mapping end-to-end: main UI -> profile application -> persisted JSON -> C++ runtime setting -> GPU constants/resources/dispatch -> visible shader behavior. Treat every no-op or shadowed value as a defect.
- [ ] Verify Off/Low/Medium/High/Ultra (or the final exposed tier names) apply immediately, persist across restart, and report `Custom` when an Advanced-menu override diverges.
- [ ] Confirm dependencies are coherent: a main preset must not silently enable unrelated material systems, overwrite user choices outside its scope, or select unsupported combinations.
- [ ] A/B each tier at fixed benchmark scenes and record frame time, VRAM, cache impact and screenshots. Preserve the current default PIXL visual baseline; performance tiers must make intentional, documented trade-offs.
- [ ] Make the main quality menu sufficient for ordinary users, leaving Advanced controls for deliberate per-module tuning rather than required setup.

### 5. Contact Shadows distance/depth stability

- [ ] Reproduce the owner-supplied distant-object shimmer/overlap in Whiterun and A/B Contact Shadows at the identical camera to distinguish shader self-intersection from genuine mesh/decal z-fighting.
- [ ] Trace Contact Shadows depth reconstruction, reversed-Z/linear-depth precision, ray-start and normal bias, thickness, maximum distance, depth-pyramid mip selection and off-screen rejection. Far geometry must not alternate between foreground/background samples or darken both surfaces at a depth discontinuity.
- [ ] Add a stable world-/view-distance and projected-size fade before depth precision becomes unreliable; the fade must be temporally smooth under camera motion and must not create a visible circular boundary.
- [ ] Validate stationary/pan/walk behavior under native/DLAA and DLSS quality modes at near, medium and far distance. Preserve useful near contact while eliminating distant flicker, crawling and false double surfaces.

### 6. Visual acceptance and release package

- [ ] Validate WindowLife 0.5.6 exact masks, fallback windows, frames/facades, closed shutters, interior depth and silhouettes.
- [ ] Preserve the owner-tested WindowLife fallback calibration of Room Width `110` / Room Height `140` as the current visual comparison point. Do not change the shipped default until it has been checked across small, large, round, arched and atlas-packed window families.
- [ ] Replace the single global WindowLife room dimensions with automatic, distance-invariant small/medium/large aperture families derived from stable material/geometry/mask extents, plus conservative per-material overrides where automatic classification cannot represent a unique landmark window.
- [ ] Separate the full-window layout domain from the pane clipping mask: mullions and crosshatch leadwork may divide glass visibility into small components, but all components belonging to one physical window must share one outer-aperture centre, floor anchor, room seed and parallax coordinate system.
- [ ] Repair large-window UV mismatch and mid-pane room starts by anchoring room boundaries to the reconstructed outer aperture rather than raw repeating material UVs. Verify mirrored/flipped UV islands and atlas seams do not restart rooms inside a glass pane.
- [ ] Specifically validate Whiterun crosshatch windows: retain glass-only clipping/refraction per small panel while spreading a coherent room across the entire framed window instead of fitting a separate miniature room into every panel.
- [ ] Replace the current dark procedural occupant shapes with an authored, provenance-recorded silhouette atlas: multiple poses, profiles, body proportions, clothing/headwear and one-/two-person arrangements; softly coloured by room light, depth-blurred and varied without becoming recognisable repeated "UI icon" figures. Retain the procedural path only as a missing-asset fallback.
- [ ] Create a separate provenance-recorded curtain atlas with multiple medieval fabric cuts, open/closed arrangements, folds, colours and opacity profiles. Sample curtains as their own parallax layer between glass and room background, with window-family-aware selection, edge-safe atlas gutters/mips and restrained movement; retain the procedural curtain path only as fallback.
- [ ] Validate authored occupants and curtains from exterior and interior views, including people passing outside interior windows, without projecting onto frames, facades or closed shutters. Measure cost and keep the implementation atlas-based/batched with graceful asset-failure fallback.
- [ ] Regression-check grass SSS/GGX/LOD, foliage wind, DLSS runoff, skin/tissue, Auto-POM and benchmark restoration paths.
- [ ] Add only the final validated shader cache to beta, rerun the 36-module/package and 243-shader reconciliation audits, and preserve rollback/checksums.
- [ ] Perform a final clean game launch, representative save/load/location traversal, screenshot comparison and log review before declaring the Nexus archive ready.

## Remaining Issues

- The primary Photo Mode Enhance aesthetic benchmark is the owner-supplied Dunmer close-up `C:\Users\PIXL STUDIO PC\Desktop\8c3566e3-263c-4349-86d0-9432c9a325ab.png`. It is a quality reference, not an assumed pixel-aligned training target. The protected comparison baseline is the current working PIXL render/build. Any neural result must remain a bounded, reversible residual blend that preserves identity, facial geometry, expression, eye direction, tattoos/markings, pose, composition, and existing PIXL lighting/material fidelity while adding restrained skin microdetail, eye/hair definition, material separation, and local contrast. Failure or low confidence must return the untouched source capture.

- WindowLife `0.5.6` requires owner visual validation across large, small, round, leaded, arched, and atlas-packed windows. With authored-mask fitting enabled, verify that the exact pane boundary remains as clean as 0.5.5 but the room, silhouettes, curtains and parallax now remain visible. Confirm the Solitude facade has no proxy/stone stencil and closed shutters/roof/wall materials remain neutral.
- Grass SSS camera parity passed the previous build but must be regression-checked. The new detail-distance control moves and softens the shader material transition; it does not change Skyrim's generated grass-card population distance. If a remaining boundary is geometric/card density rather than shader detail, adjust the relevant Skyrim grass generation/fade settings separately after an A/B isolation.
- Grass normalized GGX and the new default `1.35x` detail-distance scale require runtime evaluation under sun and overcast lighting. Tune from the new Foliage Dynamics controls rather than compensating global grass brightness until the seam source is confirmed.
- Rain runoff active-resolution and bead-depth changes require DLSS Balanced versus DLAA tests at the same camera, followed by stationary, pan, walk, and near/far checks.
- The permutation-granular clean-Release DLL is live with WindowLife metadata `0.5.6` and Material Layers `1.3.1`. The next startup is the runtime proof: its compiler card/log should show a selective WindowLife-eligible Lighting-pixel rebuild with most unrelated stages loaded from disk, not another whole-library rebuild.
- `student_1step_real.pt` from the 12-sample smoke run is validation-only and must not be integrated or judged visually. A capped 256-square pilot is now documented, but its output remains experimental until it beats the degraded-input validation baseline and passes holdout review. Production still needs co-registered PIXL raw/target/depth capture tooling, licensed targets, temporal/tiled validation, and physical AMD testing.
- World Benchmark v2 is native-build and live-deployment complete, but route framing/COCs, unpaused actors and precipitation, HUD restoration, quality restoration, captures, metrics, fallback behavior, and cancellation still need a runtime smoke test.
- The authored-mask search materially increases WindowLife shader work on candidate pane pixels. Measure Lighting/WindowLife cost during the benchmark and tune only if the observed GPU cost is disproportionate; do not discard mask correctness based on instruction count alone.
- The beta package is synchronized and audited without a shader pipeline cache. If runtime validation leads to another shader/DLL change, restage and re-audit beta before release; the owner will add the final validated cache separately.
- Natural Skin/Tissue 3.3 requires owner dialogue-scene validation. Pay particular attention to human facial warmth without waxiness, stable pore/specular response with distance, ear/nostril back-light, silhouette bleed, beast-race profiles, and DLAA versus DLSS temporal stability.

## Next Actions

1. On the next restart, record the compiler card's `DISK CACHE` and `BUILT NOW` counts plus elapsed time. WindowLife `0.5.6` should produce a selective eligible-static Lighting-pixel rebuild; stop and review the compiler log if it begins rebuilding the entire library.
2. Revisit `swindow01`/`swindow03`, the narrow leaded test, and the supplied cathedral view first. Confirm the exact loose mask is selected in the log, room structure is readable with camera-driven depth, and the flat source texture is reduced. Then inspect the original Solitude facade and closed shutters to confirm they remain neutral.
3. Repeat WindowLife validation on small, large, leaded, round, atlas-packed, and indoor-view windows; pay particular attention to atlas-cell edges and upward/grazing views.
4. A/B grass in first and third person with `Grass Detail Distance` at `1.0x` and `1.35x`, then check `Normalized GGX Response` at `0` and `1.0` under the same sun angle. Use `Detail Transition Softness` only to widen/narrow the blend after identifying its centre.
5. Compare rain runoff at the same camera under DLAA and DLSS Balanced, then test close/medium distance while stationary, panning, and walking.
6. Review the resulting PIXLRenderer/game/compiler logs and repair any demonstrated visual, cache, compilation, or resource-lifetime defect.
7. Smoke-test the nine-scene benchmark feature route and verify camera/COC framing, unpaused scene activity, captures/metrics, cancellation, and exact HUD/DLAA/frame-generation/limiter restoration.
8. After owner visual approval, keep the already synchronized beta unchanged unless a later patch is required; if it is, restage, preserve the no-cache boundary, and rerun the package audit.
9. Run the Natural Skin validation matrix above after compilation, then review the log before tuning existing user roughness/F0/MFP values or promoting the build to beta.
10. For PixDiT, run the documented capped 256-square auxiliary pilot only after reviewing the external dataset terms. Do not integrate that checkpoint; first compare validation PSNR/loss and previews, then implement aligned PIXL RGB/target/depth capture before production refinement.

## Session Recovery

If resuming after Codex restart/context compaction:

1. read `AGENTS.md`;
2. read `PIXL_ENGINEERING_CONTEXT.md`;
3. read `PIXL_DEVELOPMENT_WORKFLOW.md`;
4. read this file;
5. inspect current Git/worktree changes;
6. continue from the recorded active task.

Do not assume unfinished source modifications are disposable.

## Checkpoint - 2026-08-26 Launch Checklist Engineering Pass

- Reviewed the completed-test `PIXLRenderer.log` (201,591 bytes, final entry 2026-08-25 23:51:50). It contains no shader/compiler failure, exception, assertion, device-removal, or out-of-memory evidence. WindowLife 0.5.6 selectively invalidated 272 stages and revalidated 317 unaffected shared-family stages; all four Rain Response runoff kernels compiled successfully. Repeated `SaveTheme()` compatibility messages were the only clear non-actionable spam and are now debug-only.
- Photo Mode now has one shared eligibility contract used by both the UI and runtime entry point. It rejects world-map, inventory/magic/favorites, barter/container/crafting, pause/journal/tween, dialogue/book/console/lockpicking/message-box, loading/main-menu, invalid player/camera, death, and transition states. An incompatible menu opening during a session exits safely; camera/HUD/player alpha/FOV state is restored idempotently. Near-camera alpha fade prevents the player's body entering the photo camera.
- Replaced the difficult Radiance Weave `Vanilla SSAO` widget with the shared full-row `PIXLUI::LabeledToggle`, including a 28-pixel minimum hit row, non-overlapping label/latch layout, and controller-friendly activation. Shared toggle sizing/padding now supplies the same accessibility behavior to other module rows using that component.
- Added complete runtime quality-profile detection/application contracts for Lighting, Materials, Atmosphere, Water, Terrain & Vegetation, Characters, and Camera. Applying a tier updates live module state, GPU feature data/history/recompile flags where required, and persisted JSON; divergence reports `CUSTOM`. `docs/QUALITY_PROFILES.md` records the field-level contract. Natural Multi-Scale Wind remains an explicit user choice, while the Terrain & Vegetation tier controls wind quality without silently enabling motion.
- Contact Shadows 2.1.1 now fades Bend ray work before distant depth precision becomes unreliable, rejects cross-surface Gaussian taps using relative linear-depth derivatives, and applies a smooth long-range discontinuity fade. Cache ownership is limited to the Lighting, Grass, and DistantTree pixel families that consume it. Strict FXC passed raymarch, terrain-seam, Lighting, RunGrass, and DistantTree representative permutations.
- WindowLife 0.6.0 adds generated/provenance-recorded 2048-square RGBA occupant and curtain atlases with independent parallax depths, edge-safe 4x4 tile selection, mip-aware insets, subdued room-light colouring, and analytic missing-asset fallbacks. The final assets are `WindowLife/OccupantAtlas.png` SHA-256 `D563E38AF70E2D3EFD0A869C9AF13DC32F90E2CBAA2AB27F6D9FF9A36CD0EADF` and `WindowLife/CurtainAtlas.png` SHA-256 `CF64FA529A13CDF96B785E71CF6171CC8C27FE2982615274AD87E95395107522`.
- WindowLife exact-mask fitting now reconstructs one coarse outer-aperture layout while retaining the full-resolution mask only for pane clipping. The wider exact-mask guide bridges Whiterun crosshatching/mullions; undersized surviving subpanes fall back to a stable geometry aperture rather than restarting rooms per pane. Quantized small/medium/large/grand families derive from geometry radius around the owner-approved `110 x 140` baseline. Closed/shuttered/boarded filename families are hard rejected, and the shipped defaults/preset now use `110 x 140`.
- Foliage Dynamics 2.1.1 keeps the known-good grass GGX/SSS/material path unchanged while repairing the remaining wind envelope. The former unconditional grass `2.0` and tree `1.25` ambient-energy floors were replaced by restrained `0.32`/`0.30` floors; forward, cross-wind, flutter, and maximum added displacement are independently bounded. This prevents calm cards/branches from rotating into mirror-like sun-facing facets while retaining world-stable breathing motion, travelling gusts, and prior-frame coherence. Strict grass and TREE_ANIM vertex-shader compiles passed.
- The previously implemented conservative Auto-POM multi-scale/chroma/normal structural classifier, DLSS render-domain roof runoff repair, runoff bead depth cues, natural Skin/Tissue pass, and World Benchmark v2 cinematic nine-scene feature route remain unchanged for regression testing; their implementation was already present in the known-good baseline and was not duplicated speculatively.
- A genuine clean Release build completed successfully in 184.5 seconds. A subsequent log-level-only `State.cpp` change rebuilt and linked successfully. The only build diagnostics are the inherited FidelityFX `MSB8028` shared-intermediate-directory warnings. The 36-module source/package audit passes and `git diff --check` reports no whitespace errors (only expected Git line-ending notices).
- Live deployment and beta reconciliation completed with Skyrim closed. Build/live/beta DLL SHA-256 is `512D079B409A918E17CC342657F7C8114C5CE907A4DFC7FF3A3E6A7C7DA46F97`. Canonical/live shader reconciliation is 245/245 exact with zero differences, source-only/live-only files, backup-like files, or overlay collisions. The existing runtime pipeline library was preserved exactly at 3,436 stages / 710,762,904 bytes.
- The beta was rebuilt from the audited no-cache staging graph: 289 files / 170,069,948 bytes, zero `.pixlbin` stages, and the new WindowLife atlases are included. No user runtime configuration was overwritten. `extern/FidelityFX-SDK` remains the pre-existing dirty submodule state and was not modified or reset.

### Runtime Validation Required For This Patch

1. Restart once and confirm selective invalidation for WindowLife 0.6.0, Contact Shadows 2.1.1, and Foliage Dynamics 2.1.1 rather than library deletion. Record `DISK CACHE`, `BUILT NOW`, failed count, and elapsed time; restart again to prove a warm launch does not rebuild.
2. WindowLife: compare exact masks and fallback at narrow/arched/round/large/Whiterun-crosshatched/Solitude windows, then closed shutters. Verify one coherent room spans the outer aperture, occupants/curtains are visible but blurred, no facade/helper mesh is lit, and indoor passer silhouettes remain pane-clipped.
3. Foliage: enable `Natural Multi-Scale Wind` with the current vegetation lighting settings. Check ordinary terrain grass, small plants, and TREE_ANIM branches while still/panning/walking under calm and windy weather. Confirm natural variation without silver facets, card folding, or motion-vector trails. The master quality preset must not switch wind on or off.
4. Contact Shadows: repeat the supplied Whiterun distant-camera test under DLAA and DLSS Balanced while stationary and moving. Near contact should remain, while far overlap/crawling should fade smoothly.
5. Photo Mode/UI/quality: test blocked menus, valid entry/exit, body clipping, controller/mouse activation, the Radiance Weave SSAO row, tier persistence, and `CUSTOM` detection. Run the World Benchmark feature route if time permits and verify exact quality/HUD/camera restoration.
6. Regression-check Auto-POM roof/stone surfaces, runoff at close/medium distance under DLAA/DLSS and camera motion, grass SSS/GGX, and dialogue skin/tissue. Review the new runtime log before promoting the cache into beta.

## Checkpoint - 2026-08-26 Private Source Publication And Beta Package

- Committed the launch-candidate development tree locally as `2bb2a2ac6723fcd2ad39c98aeb1739d0cd595676` (`Prepare PIXL launch-candidate renderer patch`). The pre-existing `extern/FidelityFX-SDK` working-tree patch remains untouched.
- `tools/PixDiTEnhance/` and `PIXL_AUTONOMOUS/` are local/private and ignored. PixDiT was removed from the tracked index without deleting the local files. The public export also excludes build products, runtime data/configuration, shader caches, trained models, internal agent/development ledgers, and machine-specific state.
- Generated and audited `dist/PIXL-Renderer-v1.0-Source.zip` from the committed development tree: 812 archive entries, SHA-256 `BC3CBC2E8232EFF2B566764B5F3E52AAA4583514D0974F9167BABB36054C9E30`. The audit found zero forbidden private/generated paths and no credentials or personal absolute paths.
- Published the audited source to the private repository `git@github.com:pixlmusic/PIXL-Renderer.git` as the new default `main` branch. Remote root commit is `d768fe6816a4928689a56e6c6ce25da5962c680a`. It contains one clean source-only history root and the exact CommonLibSSE-NG, FidelityFX-SDK, and Streamline submodule pins; earlier local history was deliberately not pushed so removed private tooling is not remotely recoverable.
- Rebuilt beta staging with the current game `UserGraphics.json` copied to `SKSE/Plugins/PIXL/Config/UserGraphics.json`; game/beta config SHA-256 is `4619DD7C2A4D7C3F7CACCD6A8708FE51637DFB0E78BA01F86793D6964F2C34B3`. The optional user configuration is excluded from product-identity scanning because legacy compatibility keys may legitimately remain false.
- Final beta staging contains 290 files and zero `.pixlbin` shader-cache stages. The package audit passes all 36 modules and canonical/live shader reconciliation remains 245/245 exact.
- Built `PIXL-Renderer-v1.0-CURRENT-BETA.zip` (81,429,490 bytes), SHA-256 `C791B44C1F734C1B9E49CAF93F925329D77767B6390472F2AE81B4C8892B7EC6`, with a companion `.sha256` file. The owner will add the final runtime-validated shader cache later.
- GitHub accepted the source push. It emitted only a large-file advisory for the required redistributable `nvngx_dlss.dll` at 52.24 MiB; this is below GitHub's 100 MiB hard limit. No tag, GitHub release, upload, or other external deployment was created.
- Runtime acceptance for the newly versioned WindowLife, Contact Shadows, Foliage Dynamics, Photo Mode safety, quality contracts, and accessibility changes is still required before the beta ZIP should be treated as the final Nexus release archive.

## Checkpoint - 2026-08-26 Autonomous Tasks 01-09 And First Runtime Compile Repair

- Completed the private `PIXL_AUTONOMOUS` Tasks 01-09 sequentially with per-phase backups, reports, patches, changed-file hashes, static HLSL checks, Release builds, and live/source reconciliation under `PBRPipeline\PIXL_AUTONOMOUS_RUNS\20260826-040218`. No phase has been claimed visually successful.
- Retained build-clean changes for SkyBounce toroidal probe history, vector-aware Flowmap cache generation, optional RCAS confidence inputs, CameraSuite lazy optional shader compilation, WaterOptics cache ownership/include repair, WorldProbes texel-centred cubemap mapping, bounded SkinOptics macro curvature, and FoliageDynamics projected-footprint detail retirement. Hybrid GI adaptive ray allocation is retained strictly off by default pending live validation.
- The final clean Release build produced `PIXLRenderer.dll` SHA-256 `9E8CD7F0B5A8D197468F7D1BD92D0D5546D2A343BC64F1940C7FFB5400AEE45C`; the live DLL was backed up and synchronized to that exact build. The 36-module audit passed. The inherited dirty `extern/FidelityFX-SDK` state remains untouched.
- The first live cold-cache attempt exposed an active vanilla-compatible Grass pixel permutation missed by the original enhanced-only representative matrix: `RunGrass.hlsl` called `PixlApplyGrassDistanceDither` from outside its `FOLIAGE_DYNAMICS` declaration scope. Shader IDs `7`, `10001`-`10008` failed with X3004. The source and live shaders were backed up under `RUNTIME_FIX_GRASS_PERMUTATION` before repair.
- Moved the stable distance-dither helper outside the feature guard so both enhanced and fallback pixel paths own the declaration. Also made existing fallback world-position derivatives explicitly `.xyz`, eliminating strict-mode float4-to-float3 truncation warnings without changing the intended calculation.
- Corrected source/live `RunGrass.hlsl` now match. Strict FXC `/Ges /WX /O3` passes four direct paths: fallback pixel, fallback depth pixel, enhanced pixel, and enhanced depth pixel.
- The second interrupted startup contained no shader/compiler errors. PIXL theme, menu icons, WindowLife atlases, and renderer hooks initialized correctly. The normal settings UI remained unavailable because startup was still inside the full pipeline compile gate; the game was closed with 3,418 tasks queued.
- The prior pipeline library has already been deleted as incompatible (`no plugin version found` / pipeline-layout metadata changed). One uninterrupted full rebuild is now unavoidable; subsequent launches should be warm only after that run finishes and writes valid metadata.

### Immediate Runtime Validation

1. Launch once and leave the startup/loading gate uninterrupted until all shader tasks complete. Do not judge the ordinary PIXL hotkey/UI while the compile gate is active.
2. Stop only if the log records a fresh `[E]`, shader failure, device removal, assertion, or crash; otherwise allow cache finalization.
3. Once the main menu becomes usable, verify the PIXL toggle opens the normal UI, load a save, perform a short stability smoke test, and exit cleanly.
4. Review `PIXLRenderer.log` for failed count, completion time, cache metadata write/finalization, and module initialization. Then restart once to prove the library is warm and is not deleted/rebuilt again.

### Clean-Rebuild Superseding Checkpoint

- At the owner's request, backed up the current build/live DLLs, ran `cmake --build build\PIXL-12C --config Release --target clean`, then rebuilt the complete `PIXLRenderer` Release target from fresh objects. Clean exit code: 0. Build exit code: 0. Elapsed build time: approximately 176 seconds.
- `tools/AuditPixlRenderer.ps1` passed all 36 integrated modules against the rebuilt DLL. Only the inherited FidelityFX shared-intermediate-directory `MSB8028` warnings were emitted.
- Rebuilt and deployed DLL SHA-256 is `177C7EB982F26F5E0842D2FF90CF8546D9BDE9D1EA96E47B367BA6CF6299994D`; build and live copies match exactly at 19,406,336 bytes.
- Reconciled all ten shaders touched by Tasks 01/03/05/06/07/08/09. Canonical and live hashes match for every pair, including the corrected `RunGrass.hlsl`.
- Runtime status remains pending. The next launch must complete one uninterrupted cold pipeline compile before normal UI availability, visual evaluation, or warm-cache validation can be judged.

## Checkpoint - 2026-08-26 Frame-Generation UI, Lighting Permutation, And Contact-Depth Repair

- Reviewed the completed runtime log at `Documents\My Games\Skyrim Special Edition\SKSE\PIXLRenderer.log`. Renderer startup, theme/icon initialization, WindowLife resources, disk-cache metadata, and deferred `DataLoaded` finalization all completed. The log exposed exactly three failed Lighting vertex stages (`C008001`, `C000003`, `C000001`) because vanilla `TREE_ANIM` permutations without `FOLIAGE_DYNAMICS` entered enhanced-wind code while the corresponding include was correctly absent.
- Guarded the enhanced TREE_ANIM implementation with `FOLIAGE_DYNAMICS`; the legacy tree shift remains available to every vanilla permutation. Strict FXC `/Ges /WX /O3` now passes both the previously failing base TREE_ANIM path and the enhanced Foliage Dynamics path.
- Traced the missing PIXL compiler panel/menu/hotkeys to the active frame-generation presentation path. `DXGISwapChainProxy::Present` called the D3D12 interop Present directly and bypassed the native D3D11 Present detour that owned `Menu::DrawOverlay()`. Camera Suite now exposes one shared `DrawRendererUIForPresent()` path; native D3D11 and D3D12 frame-generation presentation each invoke it exactly once. The D3D12 call occurs before UI brightness conversion and fence handoff so PIXL UI is included in the same wrapped UI surface as Skyrim's interface.
- Corrected a concrete reversed-Z defect in Contact Shadows. Skyrim near/far depth constants are now sent as `1/0` rather than conventional-Z `0/1`, and the point-border sampler now returns reversed-Z far depth (`0`) instead of a false near-plane blocker (`1`) for off-screen ray samples. The shader's invalid-pair fallback was corrected to the same convention.
- Contact Shadows `2.1.2` now retires the complete near-field screen-space term smoothly from 2,048 to 5,120 view units, with earlier discontinuity rejection, instead of retaining unstable continuous-surface contacts into distant low-precision geometry. Ordinary shadow maps remain authoritative beyond that range. Strict FXC passes Lighting, RunGrass, DistantTree, base Raymarch, and Terrain Seam Raymarch contact-shadow permutations.
- Canonical/live hashes match for `Lighting.hlsl`, both Contact Shadows kernels, and `ContactShadows.ini`. Release compilation passed, the 36-module audit passed, and the deployed build/live DLLs match at 19,411,456 bytes with SHA-256 `1F0867CC6E7A3AAA46C07B3EF792B5CE020C9C360800D55C58E3B92FF62AECEE`.
- Reconciled all 36 live `Data\Shaders\PIXL\Modules` descriptors against canonical source. Nine autonomous descriptors had remained one revision behind and were disabling their freshly built modules: Camera Suite, Foliage Dynamics, Hybrid GI, Image Reconstruction, Skin Optics, SkyBounce, Waterbody, Water Optics, and World Probes. Their prior live files are backed up under `RUNTIME_FIX_UI_LIGHTING_CONTACT`; canonical/live descriptor mismatches are now zero. Hybrid GI adaptive allocation remains disabled by its default runtime setting despite the module loading correctly.
- The completed 3,430-stage pipeline library was preserved (1,860,553,936 bytes). The next launch will selectively invalidate consumers owned by the corrected autonomous module versions, Contact Shadows' Lighting/Grass/DistantTree pixel families, and the three previously failed TREE_ANIM vertex stages. Because several corrected modules touch shared shader families, the selective pass may still be sizeable, but unrelated cached stages remain available and the library should not be deleted wholesale.

### Immediate Runtime Validation For This Repair

1. Launch with the existing forced frame-generation setting. Confirm the PIXL compiler card remains visible while selective compilation runs and PageDown opens/closes the normal renderer menu afterward. Confirm Numpad `*` toggles renderer effects.
2. Check the new log for `[DX12SwapChain] PIXL overlay/input routed through frame-generation Present`, zero failed shader stages, cache metadata write, and `Renderer DataLoaded finalization complete`.
3. Repeat the supplied distant Whiterun contact-shadow view while stationary, panning, and walking. Near contact should remain; distant crawling/double-surface artifacts should fade without a visible circular boundary. A/B Contact Shadows once to distinguish any remaining genuine mesh/decal Z-fighting.
4. Enable Natural Multi-Scale Wind and verify tree/plant motion now compiles and renders without reflective/mirror-like facets. No visual success is claimed until these live checks pass.

## Checkpoint - 2026-08-26 Runtime Acceptance Repair And CameraSuite Optical Ownership

- Backed up the runtime-acceptance source/live set before modification under `PBRPipeline\PIXL_AUTONOMOUS_RUNS\20260826-040218\RUNTIME_ACCEPTANCE_REPAIR_20260826-150652`. Skyrim was closed for every DLL/shader deployment.
- Reverted WindowLife's rejected 0.6.0 atlas/family renderer to the exact owner-accepted 0.5.6 optical implementation from checkpoint `c5ce2a0c`. The calibrated fallback remains `110 x 140`; existing occupant/curtain assets remain available for a future true depth tier but are not bound by the accepted renderer. The module descriptor is `0-6-1` so runtime cache ownership records this acceptance rollback without misrepresenting it as the old deployed revision.
- Restored Contact Shadows' runtime-proven Bend depth convention after the reversed-Z experiment caused the screen-space term to shadow the whole frame. CPU near/far constants, point-border depth, and raymarch fallbacks are back to the known-good `Far=1 / Near=0` contract. The complete contact term still retires smoothly from 2,048 to 5,120 view units so distant shadow maps remain authoritative. Module version is `2-1-3`.
- Repaired inert Foliage Dynamics sliders at the real draw boundary. Grass tuning uses PS `b13`, which RainResponse/GroundResponse could overwrite after Foliage's Prepass; `FoliageDynamics::BindGrassTuning()` is now called again from the existing grass setup hook after those consumers bind their data. The shared FeatureData ABI and register layout are unchanged.
- Upgraded Foliage Dynamics to `2-2-0`: deterministic world-space macro/gust/turbulence/flutter wind now drives root/stem/tip grass response and trunk/branch/leaf tree response with coherent current/previous timers. Grass lighting uses an energy-budgeted two-sided thin-surface model with Oren-Nayar correction, normalized GGX reflection, Beer-Lambert transmission tint, wet diffuse absorption, complex-map influence, and the retained mirror-Y lobe. Runtime visual approval remains pending.
- Strict FXC validation passed 15 representative WindowLife, grass, tree, and Contact Shadows permutations under `build/shader-validation/runtime-acceptance-20260826-160354`. Release compilation and the 36-module audit passed. The first acceptance DLL was deployed as SHA-256 `F050B6298CA3C90DB78DB1F6CC9245F7EE7F6B596EBFCE56E19A5919304BD9DF` before CameraSuite work superseded it.
- Backed up CameraSuite source and live predeployment files under `PBRPipeline\PIXL_AUTONOMOUS_RUNS\20260826-040218\CAMERASUITE_DOF_BLOOM_20260826-163034`. Existing lazy-compilation and shared frame-generation UI/present changes in the dirty worktree were preserved rather than overwritten.
- Upgraded CameraSuite to `1-3-0`. Removed the stale `state->inWorld` DOF gate so exterior and interior gameplay share the same pause/menu-safe contract. CameraSuite now forces native DOF strength to zero whenever PIXL DOF is inactive or Photo Mode owns the camera. When active, Skyrim's image-space pass is retained only to schedule/provide sharp color and depth; near/far participation is requested, and `ISDepthOfField.hlsl` bypasses the legacy blurred texture completely.
- Replaced the prior conservative 6-12 sample, approximately 6.5-pixel native-blur enhancement with a PIXL-owned 8/12/16/20-sample Vogel aperture. The new path computes signed CoC directly from the PIXL focus plane, supports near/far/sky defocus, applies dynamic-resolution-correct sampling, cat-eye/anamorphic aperture shaping, depth-layer rejection, foreground coverage, restrained highlight weighting, and a maximum 36-pixel footprint at the existing `2.0x` radius limit. No C++/HLSL shared-data layout changed.
- Modernized PIXL Bloom without adding buffers or bindings. Prefiltering now uses a quadratic soft knee, coherent-highlight-aware Karis firefly suppression, and retained scene-linear/exposure-aware thresholding. Pyramid upsampling attenuates broad octaves instead of adding every mip at equal energy, preventing the low-resolution full-frame veil associated with legacy bloom; final energy was recalibrated for the weighted pyramid.
- Strict FXC `/WX` validation passed all six DOF variants plus Bloom prefilter/downsample/upsample and HDR output (`10/10`) under `build/shader-validation/camerasuite-dof-bloom-20260826-164125`. Release compilation passed with only inherited FidelityFX MSB8028 warnings, and `tools/AuditPixlRenderer.ps1` passed all 36 modules.
- Deployed and hash-reconciled the Release DLL and five changed CameraSuite runtime files. Build/live DLLs are 19,406,336 bytes with SHA-256 `0B906F17AF1BD60EF598B1C798C811E067F208A1CC3E78216720963EDD509F82`. Live `ISDepthOfField.hlsl` is `A8A6E9CEC4B78A1A05A3D90839DEE923E32E42F50CB4A0FA80AACCDC7FFBFE02`; CameraSuite descriptor is `9C919DA0115A42B188D2B01FD32678333078B2F2D71D92A2BA8D0DC9414B7036`. Beta staging remains intentionally unchanged until this visual pass is owner-approved.

### Immediate Runtime Validation For The Acceptance + CameraSuite Patch

1. Confirm the compiler card/normal PIXL UI remains visible and the log completes with zero failed stages. CameraSuite/WindowLife/Contact/Foliage changes should selectively invalidate owners; do not delete the whole cache unless metadata explicitly reports a genuine global incompatibility.
2. A/B Contact Shadows at the supplied distant Whiterun view. Direct light must remain intact when enabled, near grounding must remain visible, and distant crawling/double-surface artifacts should fade. Disable Contact Shadows to distinguish any remaining genuine mesh/decal Z-fighting.
3. Recheck WindowLife on the previously accepted fallback windows at `110 x 140`, including the Solitude facade/helper-mesh failure and closed shutters. This deployment intentionally restores the accepted optical renderer before any future true RoomColor/RoomDepth/RoomNormal/Emissive tier.
4. Enable Natural Multi-Scale Wind and exercise ordinary grass, complex grass, plants, and TREE_ANIM vegetation while still/panning/walking. Verify the tuning sliders now respond at draw time, motion remains world-stable, and no cards become silver/mirror-like. Recheck GGX, mirror-Y, SSS, wetness, and first/third person.
5. In CameraSuite, enable PIXL DOF and start with Strength `0.55`, Focus Distance `2200`, Focus Range `1600`, Bokeh Radius `1.0`, Highlight Response `0.28`, Edge Protection `0.85`, Foreground Coverage `0.70`, Cat-Eye `0.20`, Anamorphic `1.0`. Verify both interior and exterior focus, near foreground/background separation, sky/terrain edges, bright bokeh, camera movement, DLAA and DLSS. Disable PIXL DOF and confirm no legacy Skyrim blur remains.
6. Enable PIXL Bloom and compare bright windows, fire/magic, sunlit speculars, and dark interiors at Strength `0.8`, Threshold `1.2`, Radius `1.0`. Verify compact sources retain color, isolated fireflies do not explode, and broad glow no longer becomes a uniform grey veil. Check SDR and HDR if available.
7. Exit cleanly and review `PIXLRenderer.log` for compile/runtime/resource failures before reconciling CameraSuite into beta staging. No live visual success is claimed by this checkpoint.

## Checkpoint - 2026-08-27 Ultra Expansion, Camera, Foliage, Snow, Assets, And Test Deployment

- Preserved the owner-approved live `UserGraphics.json` as the Ultra visual baseline. Its live SHA-256 remains `99A9A7B5D2DA91C896B76889FC602A92CD75F0006F8788F7D7F3C82EA28DCE1F`; deployment did not alter it. `SettingsDefault.json` and `PIXL-Renderer-Live-Tested.json` now contain an exact deep overlay of all approved values plus the new CameraSuite cold/elemental optics, GroundResponse weather-snow, and Interior Daylight compatibility defaults. The obsolete false-valued `TruePBR` migration key was removed only from packaged defaults so the source identity audit remains clean.
- Rebalanced the real Low/Balanced/High/Ultra quality mappings around the approved Ultra endpoint. Materials scale POM/specular-AA work; Atmosphere scales the volumetric grid without overwriting SkyVeil art controls; Water retains the enhanced renderer while scaling SSR distance/edge/dispersion; Terrain and Vegetation retain snow/deformation and scale material/wind cost; Characters retain SSS at every tier while scaling samples/detail/hair; Camera scales sample quality. Tier detection follows the same contracts and reports custom divergence.
- Added a primary `OPEN PIXL DIRECTOR` action to the Camera page using the existing authoritative eligibility/entry path. The advanced Tuner entry remains available, but ordinary users no longer need to discover it there.
- CameraSuite now owns depth-aware final-composite aperture DOF and forces Skyrim's native DOF contribution off. It also includes restrained environmental frost edges from real snowfall/exterior altitude, confirmed-player fire/frost impact optics, and a bounded inverse-radial bodycam lens with edge-only chromatic aberration and highlight halation instead of the former destructive edge smear. Bloom remains the modern soft-knee/weighted-pyramid implementation. Runtime visual validation is pending.
- Added six deliberately varied optional LUTs only: `Bleach`, `Winter`, `Sunset`, `FantasyGreen`, `Nightfall`, and `Cinematic`. Their archive member names and hashes are recorded under the CameraSuite Looks provenance file. No `.rar` or wholesale LUT folder is present in source, stage, or live shaders.
- WindowLife `0.6.2` binds the existing 4x4 authored occupant and curtain atlases at independent room depths with alpha-aware shading and analytic missing-asset fallbacks. The accepted mask/room fitting path remains protected. Runtime checks are required for visibility, facade rejection, shutter rejection, pane coherence, and performance.
- Foliage Dynamics `2.3.0` discards the broken enhanced tree deformation path and preserves authored Skyrim tree wind. Ordinary grass keeps vanilla structural motion plus a compact deterministic world-space gust layer with exact current/previous evaluation. Non-complex grass receives a restrained world-up normal blend for Hybrid GI, and normalized GGX is exposed to `4.0x`. Runtime validation is pending for calm/gust motion, first/third person, complex/non-complex GI, wetness, mirror-Y, and silver-facet regressions.
- GroundResponse weather snow accumulation slowly raises only exact snow-classified terrain during snowfall and settles back to the configured base in clear weather. The identical added depth feeds movement resistance and current/previous terrain positions. Confirmed player fire/frost hits feed CameraSuite elemental optics; existing projectile/beam/concentration/breath/shout geometry remains differentiated. A safe general steam-particle spawn was not fabricated and remains future work.
- GroundResponse `3.3.1` adds the owner-confirmed copyright-free `Internal SnowTex.tif` as one lossless `SnowMicro.png` asset only. It is loaded as linear data at PS `t110`; four finite-difference samples reconstruct world-stable snow microsurface slope without assuming an engine-specific normal swizzle. Exact snow classification, near-distance fade, and compaction attenuation prevent application to mud/rock or destruction of tracks. Provenance and source/distributed hashes are recorded beside the asset; the containing external asset directory is not distributed.
- Added a bounded incandescent response to warm additive particle and strip-particle Effect permutations. Bright cores approach a temperature-appropriate near-white, cooler edges retain chroma, and HDR energy uses a soft-knee rolloff; non-additive smoke, opaque effect meshes, and non-particle effects are unchanged.
- MaterialForge schema `3` now detects explicit fur/pelt/fleece/sheepskin tokens and corroborated fur-bearing species/body paths with boundary-aware matching that rejects words such as `furniture`. It records `FurShell`/`Fuzz` semantics and reserved descriptor parameters without changing the 208-byte GPU descriptor. This is classification infrastructure only; true multi-shell geometry is not falsely claimed and still needs a dedicated draw path.
- Interior Daylight `1.1.0` automatically enables existing sun/moon/weather and portal/shadow handling in interior cells that explicitly show/use sky lighting even if an older cell omitted the sunlight-shadow extension flag. Fully enclosed interiors remain untouched. General exterior-light transport through sealed arbitrary windows remains a larger future system.
- Strict FXC `/Ges /WX /O3` passes representative CameraSuite, WindowLife, foliage, terrain/GroundResponse, snow-microsurface vanilla/MaterialForge landscape, and three additive fire particle permutations. The final Release DLL builds successfully with only inherited FidelityFX `MSB8028` shared-intermediate warnings. The 36-module source and staged-package audits pass.
- Clean local test stage is `dist/PIXL-Renderer-v1.0-Dev-Test`. It contains the six selected LUTs, the one snow asset, no `.rar`, and a byte-identical approved live user config. The tested package was deployed with Skyrim closed without deleting the shader cache. Build/live DLL SHA-256 is `DB4D4EA347F80F4D2E25B14EDC34F182E147548837E51A6C6D1D8F31998E0004`.

### Runtime Validation Required For The 2026-08-27 Test Deployment

1. Let selective compilation finish and record `DISK CACHE`, `BUILT NOW`, failures, and elapsed time. Restart once afterward to confirm a warm launch. No visual success is claimed yet.
2. Camera: verify PIXL DOF inside/outside and native DOF absence when disabled; inspect near/far occlusion, focus transitions, DLAA/DLSS stability, modern Bloom, bodycam edges during movement, snowfall/altitude frost, and confirmed-player fire/frost hit optics. Confirm HUD/menu pixels remain unwarped.
3. Foliage: compare ordinary and complex grass in first/third person, verify non-complex Hybrid GI, natural gusts with no silver/mirror tree regression, stable motion during camera movement, mirror-Y response, GGX up to `4.0x`, SSS, and wetness.
4. Snow/fire: inspect untouched and compacted snow at near/medium distance for useful microdetail without rock/mud contamination or noisy tiling; verify weather accumulation/settling and movement resistance over time; inspect warm additive flame cores/edges and unrelated magic for regressions.
5. WindowLife: inspect fallback and authored masks across small/large/round/arched/leaded/Whiterun/Solitude/closed-shutter cases. Confirm room depth, authored people/curtains, pane clipping, no facade projection, and indoor silhouettes.
6. UI/presets: verify the Camera-page Director button, each quality tier's visible/performance scaling and persistence, `CUSTOM` detection after an advanced edit, the six distinct LUTs, and restoration of the approved Ultra endpoint.
7. Review `PIXLRenderer.log` after a clean exit for shader, texture, resource, device, assertion, or cache-finalization errors before updating beta or pushing this checkpoint.

## Checkpoint - 2026-08-27 Window Sizing, Autofocus, Elemental Lens, And Spell Footprints

- Backed up every relevant source file before modification under `build/runtime-repair-20260827/before` and the pre-deployment live set under `build/runtime-repair-20260827/live-before-deploy`. Skyrim was closed for deployment. The live `UserGraphics.json` was not edited and remains SHA-256 `8F4437FEAFB2B7D441BDF61224EE0EFDB3C44167F27F636A6ED667FFA21982DE`.
- WindowLife `0.6.3` no longer scans, loads, classifies, or lays out rooms from external `Data/Textures/masks` files. The procedural path now derives an aperture scale above and below the owner-calibrated room reference from geometry bounds and a conservative geometry-name hint. Dedicated window/pane geometry receives one clamped room rather than repeated cells; facade/wall geometry retains a stable grid. Closed, shuttered, and boarded filename families remain rejected. Final glass clipping still uses the live replacement material's native diffuse/glow/normal evidence, preserving texture-mod compatibility without an external authored mask requirement.
- CameraSuite `1.6.1` adds default-on gameplay autofocus without changing the 336-byte CPU/HLSL constant-buffer size or offsets. Centre depth is used when valid, four nearby guard samples rescue thin crosshair subjects, invalid/cleared depth remains sharp, and manual focus remains available. Autofocus uses a `480`-unit source default and an adaptive `300`-to-`1800` tolerance that grows with subject distance. Legacy saved manual ranges above `4000` are ignored only while autofocus is active; manual mode continues to honour the exact saved value.
- Fixed the whole-frame DOF defect by treating zero/unbound scene depth as invalid instead of near plane. `HDROutputCS` is now the sole enhanced-DOF owner; Skyrim's scheduled `ISDepthOfField` pass supplies a sharp source when PIXL DOF is active instead of applying a second, differently focused blur.
- Added owner-supplied RGBA `CameraSuite/Lens/FrostMask.png` and `FireMask.png` as optional authored lens artwork, loaded through sRGB WIC SRVs at compute slots `t7`/`t8`. Authored alpha defines the border coverage; RGB supplies frost/fire detail; bounded procedural crystal variation, heat motion, and sub-pixel refraction keep the response optical rather than a static pasted frame. Missing assets retain the prior procedural fallback. Public-release provenance confirmation remains recorded beside the assets.
- GroundResponse `3.3.2` separates continuous magic into beam, narrow flamethrower ribbon, cone, and true dragon/voice-breath profiles. The concrete Flames bug was a `Flamethrower` projectile flag being promoted to the same five-ray, 18-degree, 34-unit-radius footprint as dragon breath. Flamethrowers now use one near-constant-width ribbon; beams use a thinner line; authored cones retain three rays; explicit breath alone retains the broad five-ray footprint. Discrete projectile impacts and the existing GPU stamp ABI are unchanged.
- Strict FXC `/Ges /WX /O3` validation passed WindowLife Lighting PS, `HDROutputCS`, all-base `ISDepthOfField`, and `SurfaceDeformationUpdateCS`. Release C++ compilation and linking passed; only the inherited FidelityFX `MSB8028` shared-intermediate warnings remain. `tools/AuditPixlRenderer.ps1` passes all 36 modules.
- Build/live DLLs match at 19,424,768 bytes, SHA-256 `DCE249BB6A9A2D6298706AB668F6E2059000B730C95A1EB7A44966F7EAB8DA69`. The changed DLL, shaders, lens assets, and three descriptors were deployed and hash-reconciled with zero mismatches. The existing shader cache was not deleted; module revisions should selectively invalidate CameraSuite, WindowLife, GroundResponse, and affected DOF stages.

### Immediate Runtime Validation For This Patch

1. Let selective compilation finish and record `DISK CACHE`, `BUILT NOW`, failed count, and elapsed time. A subsequent warm restart should not rebuild these stages again.
2. WindowLife: leave `Automatic Window Room Sizing` enabled and test the same small Solitude window, broad/larger window, Whiterun crosshatch, facade/helper-mesh case, and closed shutters without touching Room Width/Height between buildings. Confirm one coherent interior per dedicated aperture, no mid-pane tiling, and no external mask loading in the log.
3. DOF: enable enhanced DOF with Gameplay Auto Focus on. Test a dialogue face, a nearby object against distant scenery, a landscape, sky edges, movement, DLAA, and DLSS. The centre subject must remain sharp and the full frame must never become uniformly blurred. Then disable Auto Focus and verify the manual distance/range controls respond normally; disabling enhanced DOF must leave the image sharp.
4. Elemental lens: test snowfall/high altitude and confirmed frost/fire player hits. Confirm the new authored frost/fire borders are detailed but leave the centre readable, do not affect HUD/menu pixels, and move/refract subtly without looking like compression artifacting. Confirm the log reports both authored lens textures loaded.
5. Snow magic: cast Flames across snow and verify a narrow continuous melted ribbon rather than a shout cone. Compare Frostbite/beam, a true cone, discrete fire/frost projectiles, and dragon breath to verify visibly different footprints without terrain contamination beyond exact snow receivers.
6. Exit cleanly and review `PIXLRenderer.log` for shader, texture, resource, device, assertion, cache, WindowLife-classification, and GroundResponse diagnostics before beta reconciliation. No runtime visual success is claimed by this checkpoint.

## Checkpoint - 2026-08-27 Exact Window Clipping And Snow-Hull-Safe DOF

- Backed up the affected source/default files under `build/runtime-repair-20260827/followup-before-20260827-172758` and the immediately previous live DLL/shaders/descriptors/config under `build/runtime-repair-20260827/live-before-followup-20260827-180752` before deployment.
- Runtime evidence was clean rather than a compile/resource failure: WindowLife had automatic sizing enabled but still inherited the saved `150x150` manual calibration, while CameraSuite autofocus inherited `0.0` edge protection and `1.5` foreground coverage. GroundResponse reported its authoritative displaced-depth synchronization active, so the snow-hull DOF defect was isolated to gather/edge policy rather than missing final terrain depth.
- WindowLife `0.6.4` restores optional diffuse-stem-matched masks as an exact final glass stencil only. A mask can no longer promote a material into a window, control room coordinates, resize/retile the room atlas, change occupants, change seeds, or reactivate helper/proxy geometry. Existing closed/shuttered/boarded rejection remains intact. Full-resolution external mask coverage is now authoritative for room, curtain and occupant clipping, preventing the former mip-expanded facade/frame halo.
- Automatic room sizing is now genuinely independent of the saved manual sliders and always begins from the owner-validated `110x140` calibration before applying dedicated-geometry scaling. Facade geometry retains a stable grid; manual width/height remains available only with automatic sizing disabled and is visibly disabled in the developer UI while automatic mode owns the layout.
- Added the default-on `UseExactGlassMasks` setting to C++ serialization, the live-tested/default source profiles and the WindowLife menu. Missing or replacement-texture-specific masks safely fall back to native procedural pane evidence. Runtime confirmation of the installed mask matches is pending; no visual success is claimed.
- CameraSuite `1.6.2` quantizes autofocus depth modestly for spatial stability, enforces safe minimum edge protection and bounded foreground coverage only while gameplay autofocus is active, and leaves manual Photo Mode values authoritative. A four-neighbour depth-continuity guard now keeps discontinuous terrain/snow/actor silhouettes sharp. The aperture gather rejects invalid depth, suppresses opposite-CoC taps, preserves rather than erases depth rejection during foreground coverage, and increases the sharp centre contribution near partial blur.
- Strict FXC `/Ges /WX /O3` validation passed four representative WindowLife Lighting pixel permutations plus CameraSuite SDR and HDR compute variants under `build/sv-wd-20260827`. Release compilation/linking passed; only inherited FidelityFX `MSB8028` shared-intermediate warnings remain. The 36-module renderer audit and both packaged-default JSON parses passed.
- Deployed with Skyrim closed without deleting the shader cache. Build/live DLLs are 19,437,056 bytes with SHA-256 `F7E879114CBF9FE4CB95E9B7E69E56BFA76B7223BAA79F0CA55ECE2C9B82B198`. Live WindowLife HLSL is `D83A17DF74A7BFFE5D2FCF657BE35368BC11C8B67DF74BAC6CC9A17AF939F64D`; live CameraSuite HDR output is `731A763964F7E12742DA0ADE0FE4EACD79FEEA5C0F94E79BED2FD8DEE27A61C7`. Source/live copies match. The current live `UserGraphics.json` was preserved at SHA-256 `1EBC95572FFD966CC12FA065BE43312BE69F3FE68A7434E8EED7CD826E6CAF6E`.
- Beta staging remains unchanged pending owner visual approval. Expected selective work on next launch is WindowLife-owned Lighting stages plus CameraSuite direct kernels; a deliberate full shader-cache clear was not performed.

### Immediate Runtime Validation For The 0.6.4 / 1.6.2 Patch

1. Confirm the startup compiler reports selective work rather than a global cold-cache rebuild, and note disk-hit/built-now/failed totals.
2. Leave `Automatic Window Room Sizing` and `Exact Glass Mask Clipping` enabled. Recheck the exact Solitude facade/frame case, small and large windows, Whiterun crosshatching, one replacement-texture window without a matching mask, and closed shutters. Room size must no longer follow the saved `150x150` manual values; exact masks must clip frames without flattening or moving the interior.
3. Enable enhanced DOF with gameplay autofocus. Test a dialogue face, close ground against distant scenery, untouched and displaced snow-hull edges, sky/terrain silhouettes, motion, DLAA and DLSS. Depth boundaries must remain stable and sharp rather than bleeding through the hull. Then disable autofocus and confirm manual controls still respond.
4. Exit cleanly and review `PIXLRenderer.log` for the exact-mask load count, module versions, shader failures, resource warnings and cache behavior before beta reconciliation. Lens effects and all other nominal systems were intentionally left unchanged.

## Checkpoint - 2026-08-28 Accepted Ultra Baseline, Lighting, Camera, Window Presentation, Runoff, And Contact Repair

- Treated the owner's current working build as the new known-good visual baseline before this pass. Backed up the accepted DLL, live configuration, relevant C++/HLSL sources, module descriptors, and runtime shader copies under `build\active-dev-checkpoints\20260828-025845-accepted-ultra-lighting-camera`. The accepted pre-patch live DLL was 19,437,056 bytes with SHA-256 `F7E879114CBF9FE4CB95E9B7E69E56BFA76B7223BAA79F0CA55ECE2C9B82B198`.
- Promoted the current live `UserGraphics.json` as the Ultra baseline without rewriting the user's live file. Live, beta, and developer-stage user configurations now match at SHA-256 `B874DFBB0A13CE6F6F13BE0DBE909B217773942F890E583D46DCE800964E23CC`. `SettingsDefault.json` and `PIXL-Renderer-Live-Tested.json` are a matching deep overlay with the new neutral WindowLife and disabled-by-default motion-blur fields; both parse successfully and have SHA-256 `6A9A29723562859D97EAE483C33DBC7247EBF38D8AF628EDC2B02DC8902953A8`.
- Prevented sealed-interior directional GGX from using the hidden exterior directional light. Directional diffuse remains compatible with Skyrim's authored interior lighting, while directional specular is permitted only outside or when Interior Daylight has activated the controlled portal/shadow path. This avoids sun-specular bleeding through walls without disabling genuine sky-lit interiors.
- Extended Natural Lighting's inverse-square retirement radius by `2.4x` without changing attenuation at a given distance. RadiantGrid now recognizes conservative candle, wick, torch, sconce, brazier, bonfire, campfire, hearth, flame, fire, ember, and burning particle families when no authoritative attached Skyrim light exists. Their synthesized practical lights use the normal physical local-light/GGX path and existing screen-space local contact handling. Existing attached `BSShadowLight` data remains authoritative; true shadow-map allocation for arbitrary particle-only emitters was not fabricated, so every emitter is not falsely claimed to own a full shadow map.
- CameraSuite `1.6.3` removes the persistent per-pixel random aperture rotation that presented as full-screen DOF noise. The aperture remains deterministic and depth-aware. Eye adaptation now measures a 99.5th-percentile highlight and applies a fast protection path when a bright window, snow field, flame, or sky transition would otherwise produce a large exposure blowout; manual exposure remains unchanged.
- Added disabled-by-default modern camera motion blur to the Camera panel. It reconstructs camera motion from scene depth and current/previous unjittered transforms, rejects sky/invalid depth and cross-silhouette samples, ignores sub-pixel temporal jitter, scales from 5 to 11 samples by Camera quality, clamps maximum motion, and composites before UI so menu pixels remain sharp. The CameraSuite CPU/HLSL constant buffer was appended at `c21` and is statically checked at 352 bytes; no prior offsets were changed.
- Replaced CameraSuite's over-limit settings serialization macro with an explicit field-list serializer using default-preserving deserialization. Existing settings remain compatible, and previously omitted cold/elemental optics plus the new motion settings now persist correctly.
- WindowLife `0.6.5` appends neutral `Interior Contrast` and `Interior Emission` controls to the structured GPU payload (`c11`, 192 bytes with matching CPU/HLSL checks). The controls affect recessed authored-room colour only, not pane detection, exact-mask clipping, the original glass, curtains, occupants, or room placement. Defaults of `1.0` preserve the accepted appearance.
- Tightened Rain Response roof runoff without changing detection or timing: low-energy mask halos are shaped out, the near shoulder band is narrower, side intensity is lower, and the two-pixel vertical companion is restrained. This targets the reported large/fuzzy falloff while preserving the visible runoff core.
- Contact Shadows `2.1.4` replaces its unweighted transition cross with a scene-depth-bilateral resolve and considers the full four-neighbour depth discontinuity. Screen-space directional visibility now has a conservative `0.12` floor, preventing an under-resolved ray field from erasing all direct light while retaining near-field grounding and the accepted 2,048-to-5,120 distance retirement.
- PIXL Director no longer deliberately fades the player at close camera distances. It restores the exact player alpha captured on entry every Director frame and again on exit, allowing close character photography. Director translation now consumes Skyrim's native TFC input through a smoothed velocity: normal movement runs at `0.30x` for deliberate framing, while Shift/native run input uses a faster `0.82x` gear. Teleports and camera-boundary clamps reset velocity to prevent snaps or accumulated drift; native rotation remains unchanged.
- Strict FXC `/Ges /WX /O3` validation passed representative Lighting, MaterialForge, Natural/RadiantGrid, Contact, Deferred, WindowLife, CameraSuite HDR/exposure, and both runoff compute paths. The final Release build completed successfully in 101.4 seconds; only inherited FidelityFX `MSB8028` shared-intermediate warnings remain. `tools/AuditPixlRenderer.ps1` passes all 36 modules for source, beta, and developer-stage packages, and scoped `git diff --check` reports no whitespace errors.
- Deployed with Skyrim closed and without deleting the shader cache. Build, live, beta, and developer-stage DLLs match at 19,449,856 bytes with SHA-256 `894E2DF1E2FD871BD9D5B3B943A533938C80E166DC75E112F616530154F96560`. All eleven changed live shaders/descriptors are hash-reconciled. Beta contains 302 files / 193,283,973 bytes and zero `.pixlbin` stages; its manifest correctly reports compile-on-device.

### Immediate Runtime Validation For The 2026-08-28 Patch

1. Launch without clearing the shader cache. Confirm CameraSuite `1.6.3`, WindowLife `0.6.5`, and Contact Shadows `2.1.4` perform selective invalidation only, compilation finishes with zero failures, and the normal PIXL UI/hotkeys remain available. Restart once to confirm a warm launch.
2. PIXL Director: approach the character closely and confirm the player never fades. Compare ordinary WASD/free-camera translation with Shift held; the base gear should be slow and smooth for framing, Shift should travel substantially faster, and releasing input should settle without a snap or long drift.
3. CameraSuite: confirm DOF no longer produces persistent noise across the whole frame and recheck dialogue faces, close terrain/snow hulls, distant scenery, motion, DLAA, and DLSS. Enable Modern Motion Blur and inspect slow pans, rapid turns, silhouettes, sky edges, UI, and menus; it remains off in the accepted Ultra baseline until explicitly enabled.
4. Exposure: transition between a dark interior and bright doorway/window, snow, sky, and large fire. Adaptation should remain responsive without a full-frame white blowout or pumping. Verify manual exposure still responds exactly when selected.
5. Lighting: in a sealed interior, verify exterior sun direction no longer creates GGX highlights through walls. Recheck a valid Interior Daylight/sky-lit interior. Inspect candles, torches, hearths, campfires, braziers, and flames for local diffuse/GGX reach and existing shadow behaviour; report any unrecognized emitter asset so its classification or authoritative light can be traced.
6. WindowLife: start with Interior Contrast and Interior Emission at `1.0`; adjust each independently and confirm recessed room art becomes clearer/brighter without changing window detection, frame clipping, glass opacity, curtains, occupants, or room sizing.
7. Rain/contact: compare roof runoff close/medium distance under DLAA and DLSS for a tighter core with less fuzzy falloff. A/B Contact Shadows at the supplied distant Whiterun case; direct light must remain intact, near contact must remain, and shadows must not bleed across depth-discontinuous objects.
8. Exit Skyrim cleanly and review `PIXLRenderer.log` for shader failures, resource errors, cache finalization, module revisions, and emitter diagnostics before accepting this visual checkpoint. No runtime visual success is claimed yet.

## Checkpoint - 2026-08-28 Final GUI And Control Readiness Audit

- Completed a static end-to-end audit of the public renderer shell, seven grouped quality controls, advanced tuner and all 36 active final-build modules plus Material Forge. The durable evidence and per-control disposition are recorded in `docs/PIXL_GUI_Audit/GUI_CONTROL_AUDIT.md`; useful missing controls and exact implementation requirements are recorded in `docs/PIXL_GUI_Audit/NEW_CONTROL_RECOMMENDATIONS.md`.
- Backed up the relevant pre-audit source, shaders, menu/quality files and accepted configurations under `build/active-dev-checkpoints/20260828-042704-gui-control-audit-baseline` before modification. The live `UserGraphics.json` was not rewritten and remains SHA-256 `B874DFBB0A13CE6F6F13BE0DBE909B217773942F890E583D46DCE800964E23CC`. Packaged `SettingsDefault.json` and `PIXL-Renderer-Live-Tested.json` still match exactly at SHA-256 `6A9A29723562859D97EAE483C33DBC7247EBF38D8AF628EDC2B02DC8902953A8`.
- Repaired the Contact Shadows contrast control end-to-end. Its former HLSL fallback promoted every value below `1.0` to maximum contrast `4.0`, producing a discontinuous slider and allowing the screen-space term to black out direct lighting. The shader now clamps continuously to the public `1.0-4.0` range; surface thickness, edge rejection and quality inputs also clamp. Public labels now describe observable behaviour.
- Repaired the CameraSuite Modern Motion Blur toggle. C++ correctly requested the presentation composite and uploaded the appended constants, but `HDROutputCS.hlsl` omitted motion blur from its finishing-effect early-out. Motion blur now runs when it is the only enabled finishing effect; its accepted default remains disabled.
- Repaired quality-profile fidelity and workload mappings. Lighting Ultra again owns two local-contact lights; Materials Ultra again preserves Specular AA `1.34`. Terrain & Vegetation now scales real geometric snow/mud render distance, fade, tessellation and near/far budgets while preserving the user's foliage material and wind choices. Water SSR now derives approximately `24/32/39/48` coarse iterations from Low/Medium/High/Ultra trace distance, with the accepted Ultra path unchanged at 48 iterations.
- Cleaned public labels, ranges and interaction safety across Ambient Probe, Atmosphere, Contact Shadows, Distance Blend, Foliage Dynamics, Image Reconstruction, Interior Daylight, Linear Light Core, Material Layers, Rain Response, Skin Optics, Sky Bounce, Sky Veil, Strand Shading, Thin Surface, Water Optics and WindowLife. Replaced the remaining unsafe integer-as-`bool*` widgets with `Util::UIntCheckbox`; corrected misleading shader-cache tooltips and quality-group descriptions.
- Advanced Mode no longer silently enables Developer Mode. Cubemap authoring, GI buffers/debug, Ground Response field/self-test, reconstruction diagnostics, particle-light visualization, precipitation overrides, terrain/volume buffer viewers, water cache regeneration and Material Forge authoring/calibration/debug tools are gated by explicit Developer Mode. Atmosphere's real performance settings were retained publicly and renamed `Advanced Volumetric Quality` rather than being hidden as debug UI.
- Static settings/reference scans found no exposed serialized renderer setting without its intended CPU/GPU consumer. Thin Surface's differently named CPU/HLSL fields were verified as an intentional per-frame mapping. The disabled FSR 4 informational widget remains classified `DEAD/OBSOLETE` until a real backend exists; it must not be represented as functional.
- Recommended controls were intentionally not fabricated. Safe future candidates include Natural Lighting range/fade, Volume Occlusion resolution/softness, Terrain Occlusion update/softness, Terrain Seam blend distance, World Probe responsiveness, a CPU-capped particle-light budget, WindowLife detail/layout tiers and Pixel Capture memory planning. Automatic correctness/data-provider modules were not given meaningless toggles.
- Final Release build completed successfully with exit code 0. Output is `build/PIXL-12C/Release/PIXLRenderer.dll`, 19,449,344 bytes, SHA-256 `20A6B3E7A4C6ADC181C33221BD2366F84FC667DC597D263DA76FE947D4A157DC`. Only inherited FidelityFX `MSB8028` shared-intermediate warnings remain.
- Targeted FXC validation passed Contact Shadows `RaymarchCS.hlsl`, CameraSuite `HDROutputCS.hlsl` and water `ISReflectionsRayTracing.hlsl`. Canonical/runtime shader copies for those three edited paths are hash-identical. English translation JSON parses and `git diff --check` reports no whitespace errors beyond expected line-ending notices.
- This audit build has **not** been deployed to the game, beta, or external Git repository. No visual success is claimed. The current working tree also contains substantial accepted/pre-existing development changes that were preserved rather than reset.

### Runtime Validation Queue For The GUI Audit Build

1. Deploy only after taking a live DLL/config/shader checkpoint. Launch without deleting the shader cache and confirm selective invalidation, zero failures, normal UI/hotkeys and a warm second launch.
2. A/B all seven quality groups at Low/Medium/High/Ultra, then restore Ultra. Confirm visible/performance scaling, profile persistence and `CUSTOM` detection after one advanced edit; verify user-owned atmosphere/foliage/camera look values do not jump unexpectedly.
3. Contact Shadows: sweep Edge Contrast from `1.0` to `4.0` in the distant Whiterun case under DLAA and DLSS. Direct light must remain, the response must be continuous, near contact must remain and depth-discontinuous/far geometry must not crawl or overlap.
4. CameraSuite: enable only Modern Motion Blur with every other finishing effect disabled and verify it now responds. Then retest DOF/autofocus, bloom, exposure transitions, HDR/SDR, lens weather effects, UI isolation and Photo Mode capture.
5. Validate the visual-sensitive module rows in `GUI_CONTROL_AUDIT.md` across representative interior, exterior, dialogue, water, grass/tree, snow/mud, rain, night, world-map and mod-texture scenes. Vendor-specific reconstruction requires both NVIDIA and AMD paths where hardware is available.
6. Review `PIXLRenderer.log` after a clean exit for shader, resource, device, assertion, serialization and cache errors before deploying to beta or treating this as a release build.

## Checkpoint - 2026-08-28 Material Forge Physical Falloff Blend

- Owner A/B captures confirmed that disabled local-light falloff was too bright/flat while the fully regularized inverse-square path was more physically convincing but too dark for comfortable gameplay. The attenuation endpoints themselves were retained; the repair adds a continuous blend between them rather than inventing another brightness multiplier.
- Backed up the affected source/default set under `build/active-dev-checkpoints/20260828-102029-material-forge-falloff-blend-before` and the immediately previous live DLL, Lighting shaders, defaults, preset and user config under `build/active-dev-checkpoints/20260828-104901-material-forge-falloff-live-before-deploy`.
- Added `Material Forge > Physical Light Quality > Physical Falloff Strength` with a clamped `0.00-1.00` range. `0.00` is the exact Skyrim/Natural Lighting attenuation used when the toggle is off, `1.00` is the existing full PIXL finite-emitter inverse-square path, and `0.65` is the balanced default requested for an in-between result. `Local Emitter Radius` remains independent and the owner's current value of `11` is preserved.
- The value is serialized as `PhysicalLocalLightFalloffStrength` and reaches both ordinary Skyrim local lights and the Natural Lighting/Radiant Grid path, including local-contact-light candidate ranking. Menu/inventory lighting preserves its existing authored quadratic falloff. The toggle remains an exact master bypass.
- Preserved the five-register `MaterialForge::Settings` GPU ABI by assigning semantic ownership to the existing final float padding at byte offset `76`; the structure remains exactly `80` bytes. `SharedData.hlsli` intentionally retains the historic HLSL member name `pad0`, while the C++ field and JSON use the public semantic name. No constant-buffer offset, register, resource binding or following feature data moved.
- Existing user configurations safely acquire `0.65` through default-preserving deserialization when the new key is absent. The live `UserGraphics.json` was not rewritten and remains SHA-256 `7C738103A27C5F0F55929CC46C0377589682E8AB1370AB1598845C436906417E`; it has physical falloff enabled and emitter radius `11.0`.
- Strict Windows SDK FXC `/Ges /WX /O3` validation passed four Lighting pixel permutations: Material Forge base, Natural Lighting + Radiant Grid, Natural Lighting + Radiant Grid + Deferred Contact Shadows, and Natural Lighting + Radiant Grid + WindowLife. Both packaged JSON defaults parse and contain strength `0.65`.
- Release compilation/linking passed. The 36-module audit and scoped `git diff --check` passed; only inherited FidelityFX `MSB8028` shared-intermediate warnings remain. The new build/live DLL is 19,449,344 bytes with SHA-256 `E92778BB6585BEBDCABF7C6BEDB5537AFB66E973DE14B469F279F79D04C04080`.
- Deployed with Skyrim closed. Source/live hashes match for `Common/LightingEval.hlsli` (`05A1768B203108FF717C8AF89EE96494E38BBF78D93E49993D904761DE1B727F`), `Lighting.hlsl` (`CC3D92181124E3ABA3FD4EC228FBA3DAF539A0B735015E4274FC753A4A25D999`), and both packaged defaults (`B0D37F35CE570C3E0DCE53E9555C8A5787C0F5463FE177948A5B28564357F5D7`). The 3,437-file pipeline library was preserved; beta staging remains unchanged pending owner visual acceptance.

### Immediate Runtime Validation For Physical Falloff Strength

1. Launch without clearing the shader cache. Expect Lighting-owned selective work only; record disk-cache, built-now and failed counts and confirm the PIXL UI remains available.
2. Revisit the supplied interior with `Inverse-Square Local Lights` enabled. Compare strength `0.00`, `0.35`, `0.65`, and `1.00`; `0.00` must match the bright toggle-off image and `1.00` must match the darker physical image without a discontinuity.
3. Start at `0.65`, then tune downward if gameplay remains too dark. Confirm candles, torches, hearths and other local lights retain sensible reach with the preserved emitter radius of `11`, and sweep the slider live to check for popping or instability.
4. Exit cleanly and review `PIXLRenderer.log` for compilation, serialization, resource or cache errors before promoting this build to beta. No runtime visual success is claimed by this checkpoint.

## Checkpoint - 2026-08-28 WindowLife Oblique Projection And Duplicate-Room Repair

- Owner capture `codex-clipboard-JJ5ajA.png` established two distinct WindowLife defects while all other tested systems remained nominal: a narrow pane stretched the room at an oblique viewing angle, and a broad pane displayed a large shifted duplicate of its authored room over the primary interior.
- Traced the first defect to every room layer dividing perspective travel directly by a small facing term and then hard-clamping the resulting out-of-range coordinate at the atlas boundary. Traced the second defect to the nearer furniture-depth pass sampling and colour-blending the complete room atlas a second time at a different parallax depth. This was not a texture-load, mask-load, constant-buffer, or resource-binding failure.
- Backed up the pre-change source/live WindowLife shader and descriptor, live DLL, and live user configuration under `build/active-dev-checkpoints/20260828-122824-windowlife-oblique-ghost-before`. No settings were rewritten.
- Added a shared bounded-perspective projection for curtains, reveal/depth, authored room, furniture detail, and occupants. It retains the existing front-facing parallax response, progressively flattens excessive depth at grazing angles, caps angular slope, and smoothly compresses travel into a per-layer elliptical budget instead of allowing an atlas-edge clamp to stretch one texel column across a pane.
- Replaced the shifted full-colour furniture-room blend with restrained scalar occlusion extracted only from dark local valleys in the nearer sample. Furniture can still separate subtly from the back plane during camera motion, but the nearer pass can no longer present as a second reflected/ghosted copy of the whole room.
- WindowLife is revision `0.6.6`. No C++/HLSL constant layout, setting, register, texture binding, room atlas, user calibration, pane detection, automatic room sizing, mask policy, glass response, or resource format changed.
- Windows SDK FXC `/Ges /WX /O3` validation passed five representative WindowLife Lighting pixel-shader combinations, including Material Forge, normal/specular/alpha, and Natural Lighting + Radiant Grid paths. Two rejected synthetic combinations were confirmed to fail identically against the untouched baseline because of pre-existing invalid environment-map register overlap and model-space-normal tangent-basis combinations; they are not regressions from this patch.
- Release compilation/linking passed. The only build diagnostics remain the inherited FidelityFX `MSB8028` shared-intermediate warnings. `tools/AuditPixlRenderer.ps1` passes all 36 modules, and scoped `git diff --check` reports no whitespace defects beyond line-ending notices.
- Deployed with Skyrim and the SKSE loader closed and without clearing the shader cache/library. Build/live DLLs match at 19,449,344 bytes, SHA-256 `59C01CC2B2227AB8AA881210329C26E7AD101ECA94CEA9FB7C6A8A16EC6BBAA0`. Source/live WindowLife HLSL matches at SHA-256 `8301CCCD7C35D5AFD8D883B5825ACA307A0F1134A4BEC98D5C4AC74B0C95BAA1`; the source/live `0.6.6` descriptor matches at `4B7162CE231D6D3902C709DBD14B69E7879EB3EF63CA5BFD41EDF1F0D0B3AB6E`.
- The current live `UserGraphics.json` remains unchanged at SHA-256 `18F1B21AB4B4B706D10C16268AB9B669D53F2614B70F60F3112794F3D075A5F5`. Its accepted WindowLife calibration, including automatic room sizing and `Parallax Depth = 40`, was preserved. Beta staging remains unchanged pending owner visual acceptance.

### Immediate Runtime Validation For WindowLife 0.6.6

1. Launch without clearing the shader cache. Expect selective WindowLife-eligible Lighting compilation only; record disk-cache, built-now, failed count and elapsed time, then perform one warm restart to confirm it does not rebuild again.
2. Revisit the exact narrow left-hand window from the supplied capture. Inspect it front-on, around 30 and 45 degrees, and near grazing. The room should retain useful movement at ordinary angles and flatten gracefully at steep angles without stretching an atlas edge across the pane.
3. Revisit the broad right-hand window and pan laterally. The primary room and subtle furniture depth should remain, but the broad shifted duplicate/reflection-like room layer must be absent and must not pop as the view angle changes.
4. Regression-check accepted front-facing small, large, arched, Whiterun crosshatch, Solitude facade, replacement-texture, and closed-shutter cases. Pane detection, mask clipping, automatic sizing, glass response, occupants, and curtains should remain unchanged.
5. Exit cleanly and review `PIXLRenderer.log` for WindowLife `0.6.6`, shader failures, resource errors, cache finalization, atlas loads, and UI/hotkey stability. Static/build success is established; no visual success is claimed until this live test passes.

## Checkpoint - 2026-08-28 WindowLife World-Aperture And Authored-Layer Repair

- Owner testing of WindowLife `0.6.6` confirmed that its softer duplicate was still visible, room motion still read as camera-following, mullioned sections of one physical window could select unrelated rooms, automatic rooms sat too high, curtains did not share the automatic fit, and people read as translucent black ghosts rather than blurred opaque occupants. The pre-patch log was otherwise clean: all three 4x4 room/occupant/curtain atlases loaded with 12 mips, zero optional exact masks were present, 272 relevant stages were selectively invalidated, and 317 unaffected stages were revalidated.
- Backed up the complete relevant source/live set before modification under `build/active-dev-checkpoints/20260828-135438-windowlife-world-aperture-layers-before`, including the prior DLL, shader, descriptor, accepted user configuration, and all three atlases. Skyrim and the SKSE loader were closed for deployment.
- Activated the previously dead native-glow aperture reconstruction and integrated it into the real WindowLife evaluation path. At a coarse mip it bridges narrow lead/wood mullions, reconstructs one upright world-space centre and extent from the installed window texture, and assigns one quantized world-space room/activity seed to every connected sub-pane. Background fitting accepts a conservative three-edge irregular-window tier; people and curtains require the strict four-edge tier. Uncertain reconstruction retains the calibrated geometry fallback while suppressing misaligned foreground layers.
- Optional external masks remain exact final glass clips only. They cannot promote facade/helper geometry into a window, choose a room, resize an aperture, change parallax, or override a replacement texture. The existing filename rejection for shuttered, closed and boarded windows remains intact.
- Removed both remaining duplicate-room mechanisms. Authored rooms are sampled once at one bounded recessed depth; the shifted full-room furniture resample is gone, and authored rooms no longer receive a second independently shifted transmission/vignette after colour composition. The remaining edge reveal is aperture-anchored and is used only as a missing-room-atlas fallback.
- Reduced and smoothly bounded oblique travel for the room, people and shallow curtain layers. Perspective depth now fades only near genuinely edge-on views, but angular slope and per-layer elliptical travel are capped so a room cannot slide after the camera or stretch an atlas edge across a narrow pane.
- The bundled curtain atlas now composites as its real RGBA cloth cutout at the same aperture fit as the room. Its alpha is controlled by the renamed public `Curtain Opacity` control and is no longer reapplied as a black transmission shadow. The analytic wedge remains only as a missing-asset fallback.
- The bundled occupant atlas now composites as a softly mip-filtered, semi-coloured RGBA person layer. Added public `Occupant Opacity` (`0.00-1.00`, default `1.00`); it controls true layer opacity instead of black-shadow intensity. Softness comes from filtered cutout edges rather than whole-person translucency. Successfully composited authored people no longer also darken the result through the legacy silhouette transmission path. Legacy day/night darkness controls remain Developer-only fallbacks for a missing atlas.
- Preserved the existing 192-byte per-draw GPU payload and all registers/resources. `Presentation0.z`, previously reserved, now carries occupant opacity; no following field, resource slot, descriptor binding, shader register, atlas format or shared FeatureData layout moved. Existing configs acquire the new default through default-preserving deserialization.
- WindowLife is revision `0.7.0`. Strict Windows SDK FXC `/Ges /WX /O3` validation passed four representative Lighting PS permutations under `build/shader-validation/windowlife-v070-20260828`: base Material Forge, specular, normal/alpha-test, and Natural Lighting + Radiant Grid. Compiled sizes are 56,424 bytes for the first three and 61,688 bytes for the GI path; the native-layout integration adds only 176/76 bytes relative to the previous accepted validation artifacts.
- Release compilation/linking passed in 109.3 seconds. Only inherited FidelityFX `MSB8028` shared-intermediate warnings remain. `tools/AuditPixlRenderer.ps1` passes all 36 integrated modules, and scoped `git diff --check` reports no whitespace defects beyond expected line-ending notices.
- Deployed with Skyrim closed and without deleting the shader cache. Build/live DLLs match at 19,450,368 bytes, SHA-256 `29A1383FDD72C17844EB71CE35E51301EFBF24E12C8033E0D6C6800C195D35BB`. Source/live WindowLife HLSL matches at SHA-256 `4A3901BE2CB11587C9BDCD5658F4DF54478A0EF074D2B11C4F34DD7D2B6C4FD3`; source/live descriptor matches at `4D1C1BB253532F8A3A8F0751DE57344289896877704D508EAED4CF2E60DE7EEB`.
- The accepted live `UserGraphics.json` was not rewritten and remains SHA-256 `4F57C781A4232D34FAC13BF278C76148FB67F256752E534BE70888D009B533D3`. Beta staging remains unchanged pending owner visual acceptance. Static/build success is established; no visual success is claimed yet.
- Updated the durable GUI audit to account for the new true-opacity controls and native-texture aperture path. The former automatic-shape-layout recommendation is now recorded as implemented; only optional curated building-specific room-art profiles remain a future recommendation.

### Immediate Runtime Validation For WindowLife 0.7.0

1. Launch without clearing the shader cache. Expect WindowLife-owned selective Lighting compilation only; record disk-cache, built-now, failed count and elapsed time, then perform a warm restart.
2. Revisit the exact duplicate-layer window and pan laterally/front-to-oblique. There must be one room image, no softer shifted copy, no camera-following overlay, and no atlas-edge stretch.
3. Test mullioned/paneled, Whiterun crosshatch and adjacent multi-pane windows. Every pane belonging to one aperture must share one room, curtain, activity sequence and world-space motion; neighbouring physical windows may still vary.
4. With Automatic Room Sizing enabled, inspect small, large, tall, arched and Solitude windows. Rooms must no longer sit high, curtains must align with the same reconstructed aperture, and uncertain textures must fall back safely rather than expose a bad moving layer.
5. Sweep `Occupant Opacity` through `0.00`, `0.50`, and the `1.00` default. Authored people should remain intentionally blurred and coloured but be genuinely opaque at `1.00`, with no black ghost/double-darkening pass. Sweep `Curtain Opacity` similarly and confirm the real atlas cutouts replace the repeated analytic wedge/shadow.
6. Regression-check replacement textures, facade/helper geometry, original-glass visibility, optional exact-mask toggle, closed shutters, interior passers, distance fading and front/grazing angles. Exit cleanly and inspect `PIXLRenderer.log` for shader/resource/cache errors before beta reconciliation.

## Checkpoint - 2026-08-28 WindowLife Smart Aperture Framing

- Owner captures `codex-clipboard-DCuIeN.png` and `codex-clipboard-aqjTx0.png` established the remaining tradeoff precisely. Automatic mode kept rooms inside each physical window but stretched an entire square atlas tile into the aperture, making rooms miniature/flat, placing several floor horizons too high, and still treating some mullioned quadrants independently. Manual `150 x 150` mode produced a more convincing accidental crop/scale in one window but its world grid cut unrelated rooms through the other windows.
- The previous `0.7.0` runtime itself was clean: WindowLife loaded all room, occupant and curtain atlases with 12 mips, zero optional exact masks failed, and no shader/resource errors were logged. Cache metadata selectively invalidated 272 WindowLife-owned stages while revalidating 317 unaffected stages; the long approximately 22-minute compile was WindowLife's large Lighting family, not a 3,459-stage global library rebuild.
- Backed up the complete relevant source/live set and the owner's post-test configuration under `build/active-dev-checkpoints/20260828-162243-windowlife-smart-framing-before`. Skyrim and the SKSE loader were closed throughout deployment.
- Split automatic layout into two explicit responsibilities. Native glow evidence owns only the physical aperture centre/extent and stable room identity; a new independent room-art viewport controls which portion of the authored square tile is visible. This preserves automatic UV/window coherence while reproducing the larger, cropped presentation that made the owner's manual example convincing.
- Expanded native aperture discovery from a partial six-guide-texel search to a complete twelve-texel fractional-mip UV-tile search. Thin centre mullions remain bridged, but broader exterior frames remain separators. A sample originating in any quadrant can now reach the opposite outer edge, so one 2x2 mullioned window shares one centre, extent, room, curtain and activity sequence. World-space identity quantization increased from 16 to 32 units to absorb small per-quadrant reconstruction variance without merging normally spaced neighbouring windows.
- Added aspect-preserving automatic art framing rather than stretching a square room into every window shape. New public `Interior Scale` is serialized and clamped from `1.00x` to `1.80x`, with a default of `1.35x`. It uses the previously reserved `Presentation0.w`; the per-draw structure remains exactly 192 bytes and no resource/register/field offset moved. Manual mode remains byte-for-byte on its original room-coordinate path.
- Measured and encoded the perspective floor boundary for every one of the 16 bundled room tiles. Automatic framing targets a consistent low `0.80` screen-V floor while clamping the crop inside its own atlas cell. This prevents different room artwork from putting its floor halfway up a window and avoids cross-cell filtering/tiling. Room MIP selection now derives from the framed coordinates so magnified rooms retain the correct detail level.
- Curtains continue to fit the physical aperture rather than the cropped background art, but their near-glass parallax depth/travel was reduced to prevent visible detachment from the frame. Occupants use the exact normalized floor produced for their selected room, so their feet follow the room perspective instead of a universal centre-biased height. Manual mode retains its accepted occupant placement.
- WindowLife is revision `0.7.1`. Strict Windows SDK FXC `/Ges /WX /O3` validation passed Material Forge base, specular, normal/alpha-test, and Natural Lighting + Radiant Grid permutations under `build/shader-validation/windowlife-v071-smart-framing-20260828`. Base variants are 56,480 bytes and the GI path is 61,688 bytes: only 56 bytes larger than `0.7.0` despite the added framing metadata and full-tile search.
- Release compilation/linking passed in 111.4 seconds with only inherited FidelityFX `MSB8028` shared-intermediate warnings. `tools/AuditPixlRenderer.ps1` passes all 36 integrated modules; scoped `git diff --check` passes with line-ending notices only.
- Deployed without deleting the shader cache. Build/live DLLs match at 19,450,880 bytes, SHA-256 `42676FC727A36A8D4D7C487454E9848C05273C0CA499681DCA9789CA4CB2B3ED`. Source/live WindowLife HLSL matches at `784E43E29FDB3A4B6709D5BAF0C40B2B624FF9C8B491AE24121573AE42892B7B`; source/live descriptor matches at `A2F53BEF51ACA3852CF062F6B357A87AC703AA2A3EA67F95D40B7D65E6ECE527`.
- The live post-test `UserGraphics.json` was not rewritten and matches its checkpoint at SHA-256 `7E173944552E11144437A602E77FD16D7700AF976BCA64C503B982C7B80635AE`. The owner left `AutomaticRoomSizing=false`, `RoomWidth=150`, `RoomHeight=150`, and `OccupantOpacity=0.05` during the comparison; those deliberate test values remain. `InteriorScale` safely acquires `1.35` when the missing key is deserialized. Beta staging remains unchanged and no visual success is claimed before live acceptance.

### Immediate Runtime Validation For WindowLife 0.7.1

1. Launch without clearing the cache and let only the WindowLife-owned Lighting stages finish. Re-enable `Automatic Room Sizing`; set `Interior Scale` to its `1.35x` default. The last test left automatic mode disabled, so leaving it off intentionally preserves the manual comparison path.
2. Revisit the exact four-window facade in the supplied captures. Each physical 2x2 window must show one continuous room across its mullions; neighbouring windows may use different rooms. The floor should sit near the lower fifth, room objects should read larger, and no room tile may restart at a centre crossbar.
3. Sweep Interior Scale through `1.00`, `1.20`, `1.35`, `1.55`, and `1.80`. It must magnify/crop room art without moving the detected aperture, changing the room seed, creating tiling, or altering pane/frame detection. `1.35` is the intended first-pass balance.
4. Set Curtain Opacity above zero and pan front-to-oblique. The authored curtain must remain aligned to the physical window while the room moves behind it. For a meaningful occupant test, raise the currently saved `Occupant Opacity=0.05` to `1.00`; figures should remain blurred but opaque and stand on the visible room floor.
5. Regression-check the previous narrow oblique, Solitude facade, Whiterun crosshatch, arched/large/small, replacement-texture and closed-shutter cases. Exit cleanly and review `PIXLRenderer.log` for compile, resource, cache or serialization errors before beta reconciliation.

## Accepted Baseline - 2026-08-28 WindowLife 0.7.1 / Final Pre-Release Entry

- The owner accepted the current WindowLife presentation as sufficiently polished for release and directed that the complete current working renderer become the new known-good baseline. WindowLife `0.7.1`, its smart aperture/art-framing split, the current deployed DLL, and the current live PIXL configuration are therefore protected from speculative visual rewrites during the final pre-release pass.
- Created a recoverable pre-polish checkpoint at `build/active-dev-checkpoints/20260828-175615-accepted-windowlife-v071-release-baseline`. It contains every modified/untracked PIXL worktree file, Git/submodule state, the build and live DLLs, the live user configuration, live WindowLife shader/descriptor/assets, pipeline metadata, and SHA-256 manifests.
- Accepted build/live DLL: 19,450,880 bytes, SHA-256 `42676FC727A36A8D4D7C487454E9848C05273C0CA499681DCA9789CA4CB2B3ED`. Accepted WindowLife HLSL: `784E43E29FDB3A4B6709D5BAF0C40B2B624FF9C8B491AE24121573AE42892B7B`. Accepted live `UserGraphics.json`: `7E173944552E11144437A602E77FD16D7700AF976BCA64C503B982C7B80635AE`.
- The final pre-release polish/security/fidelity pass begins from this baseline. Release-safe corrections may proceed, but any visually sensitive change requires static/build validation and remains live-test pending unless the owner directly accepts it. No external push, tag, release, or deployment was authorized by this baseline acceptance.

## Live-Test Checkpoint - 2026-08-28 Pre-Release Correctness Batch 1

- Paused the wider final pre-release audit at the owner's request and produced a fresh Release test build. `tools/AuditPixlRenderer.ps1` passes all 36 integrated modules. The DLL is 19,471,872 bytes with SHA-256 `1FD53F2D113F737024EB071A4083EF4B6136F0B78B550E00C08B011101457DB3`. The only build diagnostics are the inherited FidelityFX `MSB8028` shared-intermediate warnings.
- Release-safe C++ work in this batch hardens configuration persistence and indexing, optional-module error isolation, shader/include loading, profiler/resource failure handling, reconstruction vendor entry-point validation, DX12 swap-chain COM ownership, and null/lifetime/range handling in Linear Light Core, Natural Lighting, Radiant Grid, World Probes, Sky Bounce, Hybrid GI, Water, Foliage Dynamics, Ground Response, Contact Shadows, Atmosphere and Rain Response.
- Release-safe HLSL work adds numerical guards for degenerate normals, zero denominators, invalid homogeneous coordinates, roughness/mip domains, fog/depth reconstruction, water refraction/parallax, thin surfaces, spherical harmonics, GGX helpers, world probes, precipitation and roof runoff. It does not intentionally retune the accepted artistic defaults or WindowLife `0.7.1`.
- Corrected the generated-project shader inventory so all 180 active HLSL/HLSLI files appear in the Visual Studio project; this does not itself create new runtime permutations. Hardened Release compiler/linker protections and prevented incompatible SKSE DLL discovery from executing an arbitrary library merely to inspect it.
- Windows SDK FXC validation passed 12 affected module-owned compute jobs: Atmosphere depth/material/rain-material/base-scattering/full-scattering/integration, all three roof-runoff stages, and all three World Probe stages. The earlier standalone `Color::Ambient` diagnostic was proven to be a harness mismatch caused by omitting PIXL's runtime `COMPUTESHADER` define.
- Deployed with Skyrim closed. The matching DLL and 27 changed shader sources were hash-verified in the live game. The accepted user configuration, presets, shader cache, assets and WindowLife implementation were not changed or deleted. Rollback checkpoint and deployment manifest: `build/active-dev-checkpoints/20260828-233455-pre-polish-live-test`.
- No live visual success is claimed. The wider mandatory file/module/security/performance/UI audit remains in progress and will resume after owner testing.

### Focused Runtime Validation For Correctness Batch 1

1. Confirm the shader-build panel, PIXL menu/hotkeys and all enabled modules initialize normally. Do not clear the cache; note disk-cache, built-now and failed counts, then verify a warm second launch.
2. Check Atmosphere/fog in exterior and interior transitions, clear/rain weather and dawn/dusk. Look for missing fog, black volumes, flicker, NaNs or shader failures.
3. Check Hybrid GI, Sky Bounce, World Probes and Radiant Grid in a bright exterior, dark interior and local-light scene. The intended look should match the accepted baseline; focus on missing output, leaks, flashes or stale history after a cell transition.
4. Check water front-on and at grazing angles, underwater transitions, shore/refraction and reflections. Look specifically for invalid bright pixels, stretched parallax or lost waves.
5. Check normal and complex grass, trees, wind-disabled vegetation, Ground Response snow/mud, Contact Shadows, rain/wetness and roof runoff. Existing artistic response should remain; verify stability rather than expecting a redesign.
6. Exercise DLAA/DLSS and, if available, the installed reconstruction/frame-generation path. Disable/re-enable the optional path once and confirm a missing/unsupported feature degrades locally rather than removing PIXL's UI.
7. Exit cleanly and inspect `PIXLRenderer.log` for shader compile failures, resource errors, module-disable fallbacks, assertions, access violations and cache finalization before this batch is accepted.

## Pending Deployment - 2026-08-29 DLSS Preset F

- Added backward-compatible DLSS Preset F support as serialized `presetDLSS = 5`. Existing values remain unchanged: `0=Default`, `1=J`, `2=K`, `3=L`, `4=M`. The Image Reconstruction menu now exposes `Preset F (DLAA / Ultra Performance)`, the English locale contains the matching label, loaded-setting validation accepts value `5`, and Streamline maps it directly to `sl::DLSSPreset::ePresetF` for all explicit DLSS quality selections.
- The bundled NVIDIA Streamline header identifies Preset F primarily for DLAA and Ultra Performance. PIXL intentionally honors the owner's explicit selection in other modes as well; NVIDIA's signed runtime remains authoritative.
- Pre-change backup: `build/active-dev-checkpoints/20260828-235538-dlss-preset-f-before`.
- Release compilation/linking and the 36-module audit pass. Pending DLL: 19,471,360 bytes, SHA-256 `319F5C79A7C24014A8A230A84A1AAF1B008B3F6342E33EF1C382BA19D9A36440`. Only inherited FidelityFX `MSB8028` warnings remain.
- Deployment is intentionally blocked because `SkyrimSE` was running during the requested change. The live DLL, locale and `UserGraphics.json` have not been replaced. Once Skyrim is closed, deploy the DLL/locale and set the current live `ImageReconstruction.presetDLSS` to `5`. No shader or shader-cache change is required.

## Canonical Build Checkpoint - 2026-08-29 Actor Surface Effects 1.0.0

- Implemented the new generic `ActorSurfaceEffects` Characters module in canonical source. Version 1 supports localized player/nearby-NPC snow, mud and derived wetness while reserving a generalized event/lifecycle architecture for future blood, frost, char, ash, poison, dust and spell residue.
- Ground Response remains authoritative. It shares actor identity, accepted snow/wet-mud receiver, actual raised surface height, contacted body interval, penetration depth, radius and velocity only after the same deformation stamp has passed classification and capacity checks. Disabling or failing Actor Surface Effects does not alter terrain deformation.
- Chose bounded actor-local analytical contact lobes rather than per-actor textures. This keeps contamination attached to actor position/yaw across camera movement, equipment changes and perspective changes without author UV masks. Quality tiers use 4/6/8/12 lobes and 8/14/20/32 nearby actor slots, with a separate user NPC cap and one player reserve.
- Added frame-rate-independent lifecycles: fresh snow -> melting snow -> wetness -> normal; wet mud -> dry mud -> fade. Snow weather slows melt, interiors/rain accelerate melt, rain rewets mud and slows drying, and water washes contamination while leaving temporary wetness. Falling-snow-only deposition and local heat-source coupling remain explicitly deferred.
- Integrated the material into existing Lighting rather than replacing actor shading. Snow uses shared `PIXLAdvancedSnowMaterial` tint, roughness, dielectric F0 and backscatter cues also consumed by Ground Response. Wet/dry mud and wet-film response preserve existing Skin Optics, eye/hair handling, normal maps and the underlying character lighting path.
- Shared character PS `b13` safely with Dialogue Focus: its original 96-byte prefix is immutable, Actor Surface Effects begins at byte 96, twelve 48-byte events begin at byte 176, and the combined payload is 752 bytes. C++ offset/size assertions and FXC reflection verify the ABI. Landscape remains compile-time disjoint and retains Ground Response `b13`.
- Runtime binding uses two shared 752-byte neutral/dialogue buffers plus one lazy buffer per contaminated actor. Clean actors exit on invalid surface magic; non-actor skinned draws receive neutral state to prevent cross-draw leakage. There are no new SRVs, UAVs, samplers, render targets, textures, draw calls or compute dispatches.
- Added functional Characters GUI/configuration controls for enable, snow, mud, quality, persistence, accumulation strength, maximum NPCs, distance, lifecycle rates, mask softness and edge breakup. Developer-only views expose combined/snow/mud/wetness channels, lobe allocation and actor-local coordinates. Characters grouped quality now maps to real actor/lobe budgets.
- Release build/link and `PIXL-Audit` pass with all 37 integrated modules. Strict Windows SDK FXC `/Ges /WX /O3` passed eight affected Lighting pixel permutations covering skinned base, skin, hair, eye, Material Forge PBR, legacy character, landscape Ground Response and static paths. Only inherited FidelityFX shared-intermediate warnings remain.
- Canonical pending DLL: 19,536,384 bytes, SHA-256 `1C7FCB592CDA000ED4EEC44FC9820B218C9D7A76B029338BAC1000F5BD6E3193`.
- Documentation: `docs/ActorSurfaceEffects_Architecture.md`, `docs/ActorSurfaceEffects_Implementation_Report.md`, and `docs/ActorSurfaceEffects_LIVE_TEST_QUEUE.md`.
- No in-game visual/performance success is claimed. Required validation includes shallow/deep contacts, separate boots/body lobes, player/NPC independence, lifecycle/weather/water, first/third-person ownership, equipment changes, material variety, DLAA/DLSS stability and disabled-module regression.
- The game process was no longer running at finalization. Live deployment and cache impact remain pending a recoverable source/live checkpoint and final post-documentation validation; the existing live shaders, DLL, config and shader cache were not modified during implementation.

## Live-Test Checkpoint - 2026-08-29 Frame Generation State / Photo Mode / WindowLife DDS

- Fixed Image Reconstruction frame-generation reporting so the persisted request and the actual runtime state cannot be presented as the same thing. The module now distinguishes `Off`, `Active`, `TemporarilySuspended`, `Starting`, `RestartRequired`, `Unavailable`, and `RuntimeFault`. Opening a pause menu with the default `Frame Generation in Menus=false` policy now reads `ON - TEMPORARILY PAUSED WHILE THIS MENU IS OPEN` instead of looking silently disabled while the checkbox remains enabled.
- Added a fail-safe runtime-fault latch around FidelityFX frame-generation context creation, configuration, UI composition and dispatch. A backend failure disables only generated-frame dispatch for the session, reports `RUNTIME FAULT`, and logs the first actionable failure rather than continuing to claim active generation or generating per-frame log spam.
- Replaced PIXL's broken custom real-time DOF path with a serialized `Skyrim Depth of Field` control that updates Skyrim's native `bDoDepthOfField:Imagespace` setting at runtime. Legacy PIXL DOF fields remain load-compatible but are forced inactive. No custom DOF shader path is presented to users.
- Hardened Director/Photo Mode exit input cleanup to release transient sprint/run and camera movement state without changing the user's persistent autorun/toggle-run preference. Added paused-world time refresh, temporary LUT intensity, direct 20-110 degree world FOV/lens control with exact exit restoration, processed capture motion blur controls, and three persistent photo preset slots. Legacy offline photo DOF is dormant and no longer exposed.
- WindowLife now prefers the authored compressed `RoomAtlas_2k.dds` and `CurtainAtlas_high-fidelity-2k.dds` resources, validates their dimensions/mips/formats, and falls back to the existing PNG assets if DDS loading fails. This avoids runtime mip generation and reduces estimated resident room/curtain atlas memory by approximately 34.7 MiB without changing WindowLife shader ABI or accepted visual defaults.
- Reconciled the combined Actor Surface Effects/Lighting sources and supporting resources into the live game. Canonical and live shader inventories are both 263 files with 263 content matches and zero differences, source-only files, live-only files or collisions. The existing shader cache was preserved at 3,437 files / 203,557,602 bytes; only genuinely affected permutations may compile on the next launch.
- `PIXL-Audit` passes with all 37 integrated modules. Release and live DLLs are both 19,556,352 bytes with SHA-256 `B3765A77DF3108935CF1E2DD71451F54AD4AC60E78EA8BF399C5360299A0DC5D`. Packaged default and live-tested preset JSON parse successfully. `git diff --check` reports no content errors; inherited line-ending notices remain informational.
- Recoverable pre-deployment checkpoint: `build/active-dev-checkpoints/20260829-045100-photo-fg-windowlife-predeploy`. The user's live `UserGraphics.json` was preserved byte-for-byte; no external push, tag, release or beta publication occurred.
- Live validation is pending. In particular, verify frame generation while gameplay is unpaused, its explicit temporary-pause state while the PIXL/menu UI is open, clean Off/On plus required-restart transitions, no FidelityFX runtime fault in `PIXLRenderer.log`, native Skyrim DOF toggling, Photo Mode exit speed, paused time, LUT/FOV/preset restoration, processed photo motion, DDS WindowLife asset loading, and Actor Surface Effects smoke tests. No visual or runtime success is claimed yet.

## Canonical Build Checkpoint - 2026-08-29 Native Seasons Integration

- Audited the installed Seasons of Skyrim 1.9.1 native API, current runtime log, complete installed `Data/Seasons` configuration, Turn of the Seasons ESP/BSA/configuration, official Seasons source at reference commit `89837a27849c97a9f91d01f2911d14b49bef2662`, and PIXL's landscape/Material Forge/Terrain Field/Ground Response paths. No third-party file was modified.
- Confirmed substantial existing compatibility: PIXL already resolves the Seasons runtime TXST stored in `BGSTextureSet::pad12C`; Material Forge and Terrain Field consume it; vanilla and Material Forge Ground Response render classification both preserve the final six `textureIsSnow` values from Skyrim's post-Seasons landscape material.
- Fixed the remaining CPU-contact mismatch. Ground Response movement resistance, receiver probes, loaded-land blend coverage, hard-surface rejection and snow/mud eligibility now classify the resolved seasonal texture set and replacement land record. The authoritative Seasons-hooked `TES::GetLandMaterialType` remains the safe physical-material fallback when a replacement TXST has no unique owning LT.
- Added isolated `SeasonIntegration` with no static dependency or filename/version gate. It capability-detects the documented `GetCurrentSeason` export from already loaded modules, polls it once per second on the game thread, respects Seasons' custom months/permanent modes/overrides, safely maps unknown values to neutral context, and uses a 30-second maximum rediscovery cadence only while absent/late-loaded. The render thread never queries the provider.
- Added a monotonic season generation. A real transition or active-provider game-state refresh clears the rendered texture snow cache, invalidates Terrain Field's material-hash cache, and queues one render-thread clear of the shared compaction/displaced-snow/elemental history. This prevents Winter footprints from floating over or being reinterpreted by thawed terrain. No shader cache, unrelated render history, or configuration is reset.
- Added developer-only read-only Ground Response diagnostics for provider, native status, current season and generation. Normal users receive no new compatibility settings because integration is automatic.
- Turn of the Seasons audit found 9/10/9/1 primary LT swaps for Winter/Spring/Summer/Autumn plus 64 generated Winter variants and Wyrmstooth worldspace entries. No Turn of the Seasons FormID/plugin check is present in PIXL; other packs benefit from the generic resolved-material contract.
- No HLSL, cbuffer, resource/register, shader define, permutation, pass, draw, dispatch or persistent GPU allocation changed. Steady-state GPU overhead is zero; active CPU overhead is one trivial native query per second plus existing material work. Missing Seasons preserves base PIXL behavior.
- Pre-change checkpoint: `build/active-dev-checkpoints/20260829-054912-seasons-integration-prepatch`.
- Final Release build/link and `PIXL-Audit` pass with all 37 integrated modules. Canonical DLL: 19,569,152 bytes, SHA-256 `6F93F35928AA4F23AA959D40D44E9E5F56422091143064E7C388E1F14A3E9041`. PE inspection confirms no hard Seasons import. Only inherited FidelityFX `MSB8028` shared-intermediate warnings remain.
- Documentation: `docs/SEASONS_COMPATIBILITY.md` and `docs/reports/SEASONS_INTEGRATION_REPORT.md`.
- This build has not been deployed and no live visual/runtime success is claimed. Required validation is all four overrides, None/disabled, permanent/custom mapping, Winter-to-Spring and Autumn-to-Winter, save/load/new game, interior/exterior, fast travel, supported/unsupported worldspaces, Turn of the Seasons representative landscapes, and one launch without Seasons installed.

## Live Deployment - 2026-08-29 Native Seasons Integration

- Rebuilt the complete `PIXL-Audit` Release target after the owner elected to treat the isolated Photo Mode exit crash as a possible one-off. Compilation, linking, and the 37-module integrated audit pass; only the inherited FidelityFX `MSB8028` shared-intermediate warnings remain.
- Deployed the Seasons-enabled DLL and matching PDB with Skyrim closed. Build and live DLLs are both 19,569,152 bytes with SHA-256 `6F93F35928AA4F23AA959D40D44E9E5F56422091143064E7C388E1F14A3E9041`.
- Preserved the previous live DLL/PDB under `build/active-dev-checkpoints/20260829-140020-seasons-predeploy-live`. No shaders, shader cache, live user configuration, third-party Seasons/Turn of the Seasons files, staging package, or external Git remote were modified.
- Live validation is pending. The highest-value first checks are provider detection/current-season diagnostics, resolved Winter snow deformation, thaw invalidation after switching to Spring, save/load and interior/exterior transitions, followed by a repeat Photo Mode enter/exit test to determine whether the earlier crash reproduces.

## Live Hotfix - 2026-08-29 Photo Mode Safe Exit / Camera Controls

- The owner reproduced an immediate crash twice by pressing HOME to leave PIXL Director Photo Mode. PIXL/SKSE/Seasons logs contained no shader, device, module, or provider failure; the renderer ran normally until the exit input. Static tracing confirmed `Menu::ProcessInputEventQueue` executes inside the DXGI Present overlay while the old exit path synchronously mutated Skyrim's native free-camera, player input, calendar, weather, FOV, and actor state from that render callback.
- Reworked exit into a two-stage teardown. HOME now records an atomic exit request; SKSE's game-thread task interface owns native free-camera shutdown, transient sprint/run/movement cleanup, world time/weather restoration, FOV restoration, and player-alpha restoration. The Present thread subsequently restores Camera Suite settings/LUT GPU resources and clears Director state. This preserves the speed-boost fix without mutating Skyrim gameplay state re-entrantly from Present.
- Removed the redundant exit-time weather refresh: paused sky time is restored without forcing current weather, then the saved weather is restored exactly once. Added concise request/completion log breadcrumbs for future diagnosis and a fail-safe that leaves Photo Mode active if the SKSE task interface is unexpectedly unavailable.
- Updated Director controls and presentation. UP/DOWN adjusts FOV in 2-degree steps; LEFT/RIGHT adjusts free-camera speed from 0.25x to 2.00x in 0.10x steps; SHIFT retains the fast movement gear. The full Photo Mode page exposes `Free-camera speed`, the viewfinder displays live lens/speed values, the bottom help bar is now a readable two-line guide, and the effects panel uses PAGE UP/PAGE DOWN to select plus `[`/`]` to adjust. These framing controls remain available with the HUD hidden.
- Prepatch rollback checkpoint: `build/active-dev-checkpoints/20260829-142458-photo-mode-exit-crash-prepatch`.
- Release build/link and `PIXL-Audit` pass with all 37 modules. Deployed with Skyrim closed; build/live DLLs are both 19,572,224 bytes with SHA-256 `BF03383EC171DAAA363C25E020FCD666F25D3D927CAEB04A2C22933E42671AAD`. No HLSL, shader cache, user configuration, Seasons files, or staging package changed.
- Live validation required: enter/exit Photo Mode at least three times using HOME, confirm ordinary and SHIFT movement, verify arrow-key lens/speed controls with HUD shown and hidden, change temporary time/weather/LUT/FOV then exit, confirm gameplay movement speed and original look/world state restore, and check the log for both `[PIXL Director]` exit breadcrumbs.

## Live Hotfix Revision - 2026-08-29 Photo Mode Exit Teardown Stage 2

- Live testing disproved the first Photo Mode exit hotfix: the second HOME press still crashed. `PIXLRenderer.log` ended immediately after `Photo Mode exit requested` and never reached the game-thread completion marker, proving the failure occurred inside the queued native teardown rather than Camera Suite/LUT finalization on Present.
- Removed all direct `PlayerControls`, sprint-handler, run-handler and movement-vector mutation from Photo Mode teardown. Those transient gameplay structures are owned by Skyrim and are not safe to rewrite while native TFC is destroying/changing its input state.
- Exit now clears only TFC-owned motion (`FreeCameraState` run/vertical state plus the camera's input accumulators) immediately before calling Skyrim's native free-camera toggle. This retains protection against inherited fast-camera movement without touching persistent or transitional gameplay input handlers.
- Ordinary enter/exit no longer performs unnecessary world or camera writes. Time, weather and FOV are restored only if Photo Mode actually changed them; player alpha is not redundantly written during teardown because Director continuously preserves the incoming alpha while active.
- Added four step-level `[PIXL Director]` exit breadcrumbs around native camera release, conditional world restoration, conditional FOV restoration and task completion. If another failure remains, the final emitted stage identifies the exact operation rather than leaving the whole queued task ambiguous.
- Recoverable prepatch/live checkpoint: `build/active-dev-checkpoints/20260829-144603-photo-mode-exit-teardown-stage2`.
- Release build/link and `PIXL-Audit` pass with all 37 integrated modules. Deployed with Skyrim closed; build/live DLLs are both 19,573,248 bytes with SHA-256 `CE8088F4CCBF5F536C2DF7F486A3039AB0515A70D84F2F6B26E378216C538CEF`. No HLSL, shader cache, user configuration, season data or staging package changed.
- Runtime status remains pending. First test should be an unchanged Photo Mode enter/exit cycle, repeated three times. Then separately test changed FOV, changed time, changed weather and temporary LUT so any remaining teardown fault can be isolated from the new breadcrumbs.

## Live Deployment - 2026-08-29 Unified Renderer Audit Checkpoint

- Paused the broader full-renderer/pipeline polish audit at the owner's request and deployed the current verified checkpoint for live testing. The unfinished audit remains future work; no unverified visual-success claim is made.
- Radiant Grid: matched the cluster light-list capacity to the CPU allocation (128 lights per cluster) and changed cluster-frustum construction to the unjittered inverse projection. This removes a potential structured-buffer overwrite and a known source of view-angle/TAA-driven cluster oscillation.
- Reconstruction: connected the existing reset state to both Streamline DLSS and FidelityFX FSR, with invalidation on loading completion, large camera discontinuities, FOV changes, and significant dynamic-resolution changes. Fixed FidelityFX frame-generation reset state being cleared by an unchanged HDR frame.
- FSR now receives a typed `R32_FLOAT` depth resource produced by the existing encode compute pass instead of passing Skyrim's typeless `R24G8` depth through a backend path that described it as integer data. DLSS depth handling remains unchanged.
- RCAS now guards flat-range reciprocals against NaN/INF and clamps neighborhood loads at display borders, preventing invalid sharpening output and black edge sampling.
- Release build/link passed for `PIXLRenderer`; `tools/AuditPixlRenderer.ps1` passed with all 37 registered modules. Modified Radiant Grid and reconstruction permutations passed FXC validation. Only inherited FidelityFX shared-intermediate `MSB8028` warnings remain.
- Live predeployment rollback checkpoint: `build/active-dev-checkpoints/20260829-184055-renderer-audit-live-predeploy` (previous DLL/PDB, affected live shaders, and deployment manifest).
- Deployed DLL/PDB and the changed Radiant Grid/RCAS shader sources with Skyrim closed. Build/live DLLs are both 19,575,296 bytes with SHA-256 `000C5113046F3F331D7396E021D43430D6BFF89C8EBEDBB1168F8905995C71D6`; all deployment-manifest comparisons report `Match=True`.
- Preserved the live shader cache unchanged at 3,437 files / 204,829,006 bytes. Preserved `Data/SKSE/Plugins/PIXL/Config/UserGraphics.json` unchanged with SHA-256 `59B4A43607E9331FC3C77F5309D9E91B14324A53672D7E81AF4F26A6F77F8B84`. The beta staging package and external Git remote were not modified.
- Runtime validation required: Radiant Grid stability during camera rotation/local-light changes; DLSS/FSR history behavior after loads, FOV changes and camera discontinuities; FSR disocclusion/depth-edge behavior; RCAS border/flat-field stability; and frame-generation state after menus/loading/Photo Mode.

## Canonical Hotfix Pending Deployment - 2026-08-29 Radiant Grid Capacity / Skin Micro-Relief

- Live interior testing of the unified renderer checkpoint exposed hard rectangular lighting/fog transitions while the camera moved. The contemporaneous PIXL log contained no shader, reconstruction, device or resource failure. Static tracing confirmed both direct local lighting and Atmosphere volumetric local-light scattering consume Radiant Grid's 64x64x32 clustered light lists, so reconstruction can preserve the discontinuity but does not originate it.
- Root cause is the audit checkpoint's reduction from the intended 256 lights per cluster to 128. Dense interiors can exceed that cap, and the culler retains the first intersecting lights then truncates the rest; different 64-pixel/depth clusters therefore lose different lights at hard boundaries. The unjittered cluster-frustum matrix remains correct and was retained.
- Restored `CLUSTER_MAX_LIGHTS` / `MAX_CLUSTER_LIGHTS` to 256 on both CPU allocation and HLSL. Unlike the historical unsafe state, CPU structured-buffer storage now matches the shader cap exactly, preventing overflow while restoring dense-scene visual capacity. Prepatch checkpoint: `build/active-dev-checkpoints/20260829-radiant-grid-capacity-prepatch`.
- Audited Skin Optics and Tissue Diffusion from UI/config through C++ constant/resource binding and Lighting/Tissue Diffusion shader consumption. PIXL already has dual GGX lobes, energy balancing, curvature/thickness transmission, peach fuzz, wetness, stable RNM micro-normals, screen-space diffusion and global normal-variance specular AA.
- Added a bounded pore micro-roughness response derived from the already sampled skin detail normal. It broadens highlights slightly on resolved pore relief so dialogue close-ups read less like uniformly polished embossed plastic; it adds no texture fetch, cbuffer field, register, resource, pass or ABI change, and its strength remains naturally controlled by the existing Micro Detail control. Prepatch shader checkpoint: `build/active-dev-checkpoints/20260829-skin-microrelief-prepatch`.
- Windows SDK FXC validation passed Radiant Grid cluster build/cull compute shaders plus Skin/FaceGen, wet-skin, RGB-tint and model-space-normal Lighting pixel permutations. Canonical Release DLL built successfully at 19,575,296 bytes, SHA-256 `32C3F6614D2E9046D87095714CF99C94AB1A92C86BC563B795291A744CD90BE5`.
- Skyrim is currently running and rebuilding the existing live shader cache. Per owner instruction, no live DLL, shader, configuration or cache file was touched. The live DLL remains the previous checkpoint at SHA-256 `000C5113046F3F331D7396E021D43430D6BFF89C8EBEDBB1168F8905995C71D6`. Deployment must wait until Skyrim exits; the Radiant Grid/common Lighting changes will then require affected permutations to compile again.
- Runtime validation remains required: reproduce both supplied interior viewpoints and walk/rotate through the former block boundary; confirm direct and volumetric local lights no longer cull in tiles; inspect dense exterior/candle scenes; then compare skin at dialogue distance across neutral, warm, wet and strong side lighting. Shader-only skin changes cannot synthesize the reference image's altered facial geometry, hair strands or missing source texture detail; those require upgraded assets or the future neural photo-enhancement path.

## Live Deployment - 2026-08-29 Radiant Grid Capacity / Skin Micro-Relief

- The owner completed the prior live compilation and supplied two further interior captures showing the same illumination moving between hard rectangular regions. The completed run log was clean and finalized pipeline metadata at `20:29:45`; there were no PIXL shader, device, reconstruction or resource failures through the screenshot timestamps. This corroborates deterministic clustered-list truncation rather than a compiler/runtime failure.
- After Skyrim and the SKSE loader were confirmed closed, preserved the previous live DLL/PDB plus `Lighting.hlsl` and `RadiantGrid/Common.hlsli` under `build/active-dev-checkpoints/20260829-204000-radiant-grid-skin-predeploy` with a SHA-256 manifest.
- Deployed the matching 256-entry Radiant Grid DLL/HLSL pair and the Skin Optics micro-relief Lighting shader without deleting or clearing any shader-cache files and without changing the live user configuration.
- Build/live DLLs match at 19,575,296 bytes, SHA-256 `32C3F6614D2E9046D87095714CF99C94AB1A92C86BC563B795291A744CD90BE5`. Source/live `RadiantGrid/Common.hlsli` match at `9481DA52C0F5B26E96F06EB8DFFAD30A44AF7DE862AA68BF08071E490CFFE85E`; source/live `Lighting.hlsl` match at `03E9FC13D7EBA2C363BE3E5C3B22AB6CBC310CE2DC698C40165392D83C9D15C4`.
- Live validation is pending. Because a shared Radiant Grid include and Lighting entry point changed, dependency-owned permutations will compile once on the next launch; this is necessary for the correction but the existing unrelated cache remains intact. Revisit the same interior first and move/rotate through both former boundaries before evaluating skin.

## Live Shader Hotfix - 2026-08-29 Radiant Grid Projection Contract

- The owner's annotated orbit sequence disproved the earlier capacity-only diagnosis: the hard rectangular illumination still appeared with the corrected 256-entry cluster storage. The artifact tracks camera rotation, affects both direct local lighting and volumetric scattering, and occurs in interiors and exteriors. It is therefore generated by Radiant Grid cluster membership before reconstruction rather than by DLSS/FSR reconstruction itself.
- Isolated the visual regression to `ClusterBuildingCS.hlsl` changing cluster-frustum construction from Skyrim's current-frame `CameraProjInverse` to the separately cached `CameraProjUnjitteredInverse`. Radiant Grid's screen-space consumers address clusters using the engine's current projected pixel/depth contract; using the alternate inverse can place cluster AABBs and their querying pixels in different frusta during camera rotation, making entire 64-pixel lighting cells appear and vanish.
- Restored `CameraProjInverse`, the accepted baseline projection contract. Retained the independently valid 256-entry CPU/HLSL capacity match and all C++ safety fixes; no DLL, setting, resource binding, cbuffer layout or shader permutation define changed in this hotfix.
- Windows SDK FXC validation passed both `ClusterBuildingCS` and `ClusterCullingCS`. Prepatch source checkpoint: `build/active-dev-checkpoints/20260829-radiant-grid-projection-prepatch`. Previous live shader checkpoint: `build/active-dev-checkpoints/20260829-220055-radiant-grid-projection-live-predeploy`.
- Deployed only `Data/Shaders/RadiantGrid/ClusterBuildingCS.hlsl` with Skyrim closed. Canonical/live source hashes match at `CCF8A3D821E799374F34EC745A261BD54E972C42793662553E4CB42ACAF60A19`. No shader cache was deleted, no configuration was changed, and no DLL/staging/external repository was modified.
- Runtime visual validation remains required. Reproduce the same slow character orbit in the supplied interior and then an exterior local-light scene. Confirm that light and fog remain spatially anchored without rectangular steps. If any discontinuity remains, the next isolated investigation target is room/portal acceptance rather than reconstruction or list capacity.

## Live-Test Build - 2026-08-30 WindowLife Consistency / Fidelity Pass

- The owner live-validated the Radiant Grid projection hotfix: restoring `CameraProjInverse` removed the camera-orbit block lighting artifact. The safe 256-light CPU/HLSL capacity match, renderer-audit stability work, reconstruction corrections and skin micro-relief remain retained. The incompatible unjittered cluster-frustum experiment was not restored.
- Protected the accepted live configuration before WindowLife work at `build/active-dev-checkpoints/20260829-231428-windowlife-20item-prepatch`. `Data/SKSE/Plugins/PIXL/Config/UserGraphics.json` remained byte-for-byte unchanged through deployment, SHA-256 `8C75711C2D085AD4940C3CCD0E8F44543D6B30D76125CAFFA2DA9894B5A10F42`.
- WindowLife now salts every fallback/native room identity with cached material identity plus stable geometry name and quantized instance centre. This prevents evenly spaced facade instances from aliasing the full room/curtain/occupant sequence while remaining stable across frames and distance.
- Reworked automatic-layout stability without introducing GPU readback or a new pass. Material classification now caches native-layout eligibility; the actual aperture remains correctly draw-local because it depends on UV/world derivatives. Native confidence uses a `0.35-0.65` continuous dead-zone blend instead of a binary `0.5` switch. The debug overlay retains red rejected, cyan background-only and green occupant-safe states.
- Added matching CPU/HLSL per-family fallback calibration for Nordic, noble, Riften, Windhelm, Dwemer and trade architecture. Geometry radius is now the primary aperture/facade signal; NIF-name substrings only break ties in the ambiguous radius band.
- Curtains now select family-aware atlas tiles and share a stable room/night warmth band. Room, curtain and occupant atlas insets grow with mip and grazing angle up to a safe `0.18`. Close curtain/occupant alpha receives derivative feathering while distant mip filtering remains unchanged.
- Added a real Schlick-weighted World Probe glass layer, an exterior-only tight solar glint, time-of-day interior color temperature, restrained live ambient/sun/rain response, sun-directional reveal shading, stable neighboring-room brightness/color variation and a mild room-height falloff. New public controls are Environment Reflection, Weathered Glass Response and Interior Lighting Response; advanced controls are Sun Glint, Directional Reveal, Neighboring Room Variation and Close Cutout Feather.
- Replaced high-frequency analytic grime with an original PIXL-owned mip-filtered `1024x1024` neutral glass-grime asset at private PS `t122`, with a low-frequency missing-asset fallback. Rain adds subtle vertical response and active snow weather adds restrained frost/condensation. Asset origin and processing are recorded in `pipeline/WindowLife/WINDOW_LAYER_ATLAS_PROVENANCE.md`.
- B4 exterior light spill is intentionally deferred: the current window-material pixel shader cannot shade unrelated wall/ground receiver pixels. A correct solution requires a bounded emitter list and receiver/deferred-light pass with visibility and reconstruction validation; faking it on pane pixels was rejected.
- WindowLife's private per-draw structured payload grew in lockstep from 192 to 240 bytes. C++ `static_assert`, exact HLSL field order and an SRV binding audit protect the ABI. WindowLife owns contiguous PS `t122-t127`; no competing active `t122` declaration was found.
- Release `PIXL-Audit` build and renderer audit pass with all 37 integrated modules. Representative base, World Probes + SkyBounce, and Material Forge + World Probes WindowLife Lighting pixel permutations compile successfully with Windows SDK FXC. The only shader warning was a pre-existing AmbientProbe division warning in one synthetic permutation.
- Deployed with Skyrim closed without clearing the shader cache. A final rebuild after synchronizing the hook diagnostic recompiled WindowLife and its registration consumers; build/live DLLs match at SHA-256 `4DA263684598169DD734C813807F473774076B64AF982BE6A25B2D4B7D4908C9`. Source/live `Lighting.hlsl` match at `1C17A3599E1CB3B6D91A18FEF5AF67554C89BB32C836DB22708FCFE426E61301`; source/live WindowLife include match at `17141021F09675C7C3F6FD6C3CE3B49F8C2D4470D5D740576A13A2B62C4E5520`; source/live grime asset match at `06CA7B30F4C8E8EA9E1DEA8CDFB3503F016B7805629D273B97C795B8722D02A9`.
- Full item-by-item diagnosis, mechanism, patch, regression analysis and observable acceptance criteria are in `docs/WindowLife_20Item_Implementation_Report.md`. No visual success is claimed until the owner performs its ordered live-test checklist.
- Added the seven new WindowLife keys to the shipped default and live-tested preset JSON using the same conservative in-code values. Both JSON documents parse successfully; the accepted live `UserGraphics.json` remains untouched and retains SHA-256 `8C75711C2D085AD4940C3CCD0E8F44543D6B30D76125CAFFA2DA9894B5A10F42`.

## Live Fix Deployment - 2026-08-30 Seasonal Snow / Photo Controls / Actor Equipment / Precipitation / Window Depth

- Live logs prove Seasons of Skyrim integration remained active and healthy: the native provider was detected, current season/generation were published, Turn of the Seasons replacement texture sets were indexed, and Terrain Field/Ground Response caches were invalidated. The defect was generic resolved-material classification: `snow01landscape` and `snowrocks01` were visibly resolved snow textures but carried `authoredSnow=0`, so Ground Response encoded them as ordinary soft terrain while `grasssnow01` alone became snow.
- Ground Response now treats the final resolved diffuse path (`snow`, `snw`, or `glacier`, with explicit `nosnow`/`snowless` exclusions) as snow evidence when the authored flag is absent. Structural hard-surface exclusion remains authoritative. This aligns render/deformation eligibility with the already working gameplay contact fallback and supports seasonal packs without Turn of the Seasons FormIDs or a Winter-equals-snow assumption. No GPU ABI or snow threshold/default changed.
- Director Photo Mode retains ordinary arrow keys for lens FOV and camera speed. Numpad 8/2 now selects quick-panel controls and Numpad 4/6 adjusts them, automatically opening the quick panel; Page Up/Down and bracket bindings remain compatible. The visible help text reflects the numpad navigation.
- Actor Surface Effects now resolves the owning actor through the live geometry/skeleton parent chain rather than requiring the actor to be stored directly on each geometry. Skinned armour and clothing therefore bind the same actor-local contamination payload as exposed skin, while equipment changes remain safe because raw mesh pointers are not cached. Unsupported/non-actor skinned draws still receive the neutral `b13` payload.
- Snow precipitation retains directional/ambient readability but compresses the extreme local-light term that made flakes appear emissive near NPC, dialogue and carried-light regions. Rain uses the unchanged local-light path. No new texture, pass, register, cbuffer or user setting was added.
- WindowLife's existing Interior Scale now frames authored room art in both manual and Automatic Room Sizing modes; previously the shader returned early and ignored it in manual mode. Its user range is now `1.0x-2.5x`. Interior Parallax Depth now exposes `0-216` (uploaded safely through the existing field with a `240` guard), three times the old UI range. Existing saved/default values remain unchanged and the private 240-byte C++/HLSL payload is unchanged.
- Pre-edit checkpoint: `checkpoints/live-fixes-20260830-031734`. Predeployment live rollback checkpoint: `build/active-dev-checkpoints/20260830-034906-five-live-fixes-predeploy`.
- Release build/link passed; `tools/AuditPixlRenderer.ps1` passed all 37 modules. Strict Windows SDK FXC `/Ges /WX /O3` passed WindowLife base and Natural Lighting/Radiant Grid Lighting permutations plus modified snow-particle and unchanged rain-control permutations. Only inherited FidelityFX shared-intermediate `MSB8028` warnings remain.
- Deployed with Skyrim closed without deleting shader cache or altering the beta staging package. Build/live DLLs match at 19,584,000 bytes, SHA-256 `C4FCF1667D2270F3C0ED3E46D1AADB49F3C92085D6B8B7736A51B9C5A71E1185`; source/live `Particle.hlsl` match at `1456C262D063AC6515D1DAC8DA28A49D8C7E8DE56A6C254644FDB05EE652B6CC`; source/live WindowLife include match at `86D016A2345CB1B00F9D567481FDBCEB1195F0565F946616E4FC7C4F6A32E53E`.
- Preserved the current owner-tested live configuration byte-for-byte through deployment at SHA-256 `4CCE99D5CDE9115C91E5DA87A47F71E8BDCA58B8AC68E68D74B106903A1DF9AA`. Runtime validation remains required; no live visual success is claimed.

## Live Fix Deployment - 2026-08-30 SkyBounce Boundary / Snow Stability / Actor Equipment / Camera Shell / Held-Light Shadows

- The supplied exterior capture exposed the finite SkyBounce volume boundary rather than the previously fixed Radiant Grid projection defect. `GetFadeOutFactor` evaluated unscrolled model-space coordinates and saturated them before measuring the edge, while probe sampling used the camera-scrolled `PosOffset` coordinate. The fade now uses the same scrolled coordinate, rejects samples outside the volume, and blends through a broad `0.18` inner band so the finite probe extent does not appear as a moving lighting line. The accepted Radiant Grid `CameraProjInverse` contract and 256-light capacity remain unchanged.
- The dark-snow motion pattern was traced to four finite-difference samples of the 512-square snow microsurface texture being forced to mip 0. Those samples now use `SampleGrad` with derivatives of the stable absolute-world UV, allowing the GPU to minify the detail during motion/distance instead of relying on TAA/DLSS to hide subpixel contour aliasing after the camera stops.
- Actor Surface Effects ownership now checks both the visible scene-parent chain and `NiSkinInstance::rootParent`. Equipped armour/clothing commonly reaches the actor only through the latter skeleton link; skin already worked through the former. Ownership is resolved per draw and no raw equipment pointer is cached, preserving safe equipment changes and the shared 752-byte actor payload ABI.
- Added a fail-closed `ThirdPersonState::GetTranslation` correction for shader-raised snow. It reuses Ground Response's existing resolved-material CPU surface probe at the camera XY and raises only the normal third-person camera when it lies in the narrow gap between Skyrim's Havok terrain and the active PIXL snow shell. It does not run for photo/free camera, interiors, non-snow, disabled geometric snow, underground/teleport states, or correction distances outside the current snow depth envelope.
- Held lights within 280 camera-relative units now receive a 112-unit minimum local contact-shadow ray while ordinary world emitters retain the user's configured length. This is the existing bounded six-tap, depth-aware screen-space shadow path—not a new omnidirectional shadow-map pass—so it improves nearby Flames/torch grounding without adding draw calls, cubemaps, resources, registers, constants or global shadow cost.
- Prepatch checkpoint: `build/active-dev-checkpoints/20260830-052709-five-visual-bugs-prepatch`.
- Release build/link passed and `tools/AuditPixlRenderer.ps1` passed all 37 integrated modules. Only inherited FidelityFX shared-intermediate `MSB8028` warnings remain. Build/live/staging DLLs are 19,586,048 bytes; build/live SHA-256 is `E22C5752D9B750D5108DEA279BA04094E0304D9DE787EBABC554B1F642A9CECD`.
- Deployed with Skyrim closed. Canonical/live `Lighting.hlsl`, `SkyBounce.hlsli`, and `ContactShadows.hlsli` are hash-identical; the same DLL/shader set was reconciled into the current beta staging directory. The shader cache and owner-tested live configuration were not deleted or edited.
- Runtime visual validation remains required. Test the former moving outdoor lighting frontier, snow while strafing/rotating in low light, snow/mud on several equipped armour pieces, ordinary third-person camera contact with deep raised snow, and held Flames near hands/walls/objects. No visual success is claimed from compilation alone.

## Architecture Review - 2026-08-30 PIXL Dynamic Fire / Unity Reference

- Completed a documentation-only reverse-engineering and feasibility review of all 53 files under `_REFERENCE/PIXL-Flame-Unity`. The Unity reference is an object-scoped visual burn controller: one expanding world-space ignition sphere per flammable object, localized extinguish input, neighbour heating through periodic collider overlap, global material-to-char blending, VFX Graph flames/smoke/embers/steam, and conventional child-light flicker. It is not a persistent thermodynamic or world-scale fire simulation.
- Traced the relevant current PIXL facilities directly: RenderModule lifecycle/hooks, Deferred ordering, DX11 buffer/resource helpers, Material Forge physical-material IDs/descriptors, Ground Response events/classification, Actor Surface Effects' bounded actor-local event pattern, Weather/Rain/Seasons context, world-space foliage wind precedent, Radiant Grid light upload/consumers, Hybrid GI, particle shaders, and Image Reconstruction depth/motion/reactive/transparency encoding.
- Recommended a bounded hybrid native architecture rather than a Unity API port: stable reference-local analytical `FirePatch` state for surface heat/char, optional sparse near-field masks only for validated UVs, pooled GPU flame/smoke/ember/steam particles, aggregate renderer-native Radiant Grid light candidates, conservative material classification, weather/wetness/wind/Ground Response integration, and explicit reconstruction annotations.
- Final decision: **PROTOTYPE FIRST - architecture viable but major unknowns remain**. The two hard prototype gates are stable per-reference/local-surface identity for arbitrary shared Skyrim geometry and a proven custom transparent render/motion/reactive-mask contract across DLAA, DLSS, FSR and supported frame generation. If the latter fails, a surface-only burn system remains a safe fallback.
- Added `docs/DynamicFire/UNITY_FIRE_REVERSE_ENGINEERING.md`, `PIXL_FIRE_FEASIBILITY.md`, `PIXL_FIRE_ARCHITECTURE.md`, `PIXL_FIRE_IMPLEMENTATION_PLAN.md`, and `PIXL_FIRE_RISKS.md`. These contain the file-by-file reference audit, Unity-to-PIXL mapping, alternative state-representation comparison, selected architecture/data flows, phased go/no-go implementation plan, quality controls, three scope proposals, and risk/mitigation register.
- No C++, HLSL, build configuration, runtime asset, shader cache, live configuration, DLL, beta staging file, or external repository was changed. No build was required or run because this task intentionally produced analysis/design documentation only. The existing live PIXL baseline and outstanding visual-validation queue remain unchanged.

## Live Fix Deployment - 2026-08-30 Actor Surface Effects Equipment Bone Ownership

- Live testing confirmed snow/mud accumulation state remained correct on exposed skin but equipped armour and clothing still received the neutral character payload. The prior ownership fallback checked the visible geometry chain and `NiSkinInstance::rootParent`; dismember armour can keep an armour-instance root there while only its remapped `bones[]` point into the owning actor's live skeleton.
- Extended `ResolveOwningActor` with a current-draw, bounded skin-bone hierarchy fallback. It inspects at most 256 engine-owned bone pointers and stops at the first actor-owned hierarchy. No mesh/actor raw pointer is cached, so equipment changes and unloaded geometry retain the existing safe behavior. Unknown/non-actor skinned draws still bind the neutral payload.
- This is a C++ ownership fix only. The shared 752-byte DialogueFocus/Actor Surface Effects `b13` ABI, HLSL, registers, shader defines, settings, runtime assets, and cache dependencies are unchanged.
- Prepatch source checkpoint: `build/active-dev-checkpoints/20260830-actor-equipment-bone-owner-prepatch`. Live rollback checkpoint: `build/active-dev-checkpoints/20260830-actor-equipment-bone-owner-live-predeploy`.
- Canonical Release build/link passed and `tools/AuditPixlRenderer.ps1` passed all 37 integrated modules. Only inherited FidelityFX shared-intermediate `MSB8028` warnings remain.
- Deployed DLL/PDB with Skyrim closed. Build/live DLLs match at 19,586,560 bytes, SHA-256 `910D71B5776571B66E44091D707AABB4A07D365EE92B11C45CB7447EC39ADF78`. The shader cache, owner configuration, shader files, beta staging package, and external repository were not changed.
- Runtime visual validation remains required: contaminate the player/NPC while wearing several skinned boots, trousers, robes and dismember armour pieces; verify exposed skin and equipment share the same actor-local snow/mud state; then equip/unequip items and switch first/third person to confirm no stale or inherited contamination.

## Live Fix Revision - 2026-08-30 Actor Surface Effects Armour Pixel Permutations

- The follow-up live capture disproved actor ownership as the only remaining problem: exposed skin evaluated Actor Surface Effects, while common tangent-normal boots/armour did not. The apparent sole-only boundary was not an actor-local axis error; most of the equipment pixels never entered the Actor Surface Effects pixel-shader path.
- Traced the cause to `BSLightingShader_GetPixelTechnique`: PIXL historically removed the `Skinned` flag from every non-model-space-normal pixel descriptor because skinning is normally vertex-only. FaceGen/model-space-normal skin therefore retained a character pixel permutation, but ordinary skinned armour and clothing collapsed onto the matching static pixel permutation and compiled out `ActorSurfaceEffects::Evaluate`/the shared character `b13` payload.
- `MaterialForge.cpp` now retains the `Skinned` pixel-permutation bit only while Actor Surface Effects is loaded. Equipped tangent-normal geometry therefore receives the existing character-only HLSL path, while ordinary static geometry, landscape, trees, and the historical fail-safe path remain unchanged. The prior visible-parent/root-parent/bone ownership resolution remains in place for binding the correct actor payload.
- No HLSL source, cbuffer field, register, resource format, draw/dispatch, user setting, live shader file, or existing cache entry was edited. The change creates distinct skinned Lighting pixel permutations when requested, so the next launch may compile equipment permutations without requiring deletion of the unrelated shader cache.
- Source checkpoint: `build/active-dev-checkpoints/20260830-actor-equipment-pixel-permutation-prepatch`. Live rollback checkpoint: `build/active-dev-checkpoints/20260830-actor-equipment-pixel-permutation-live-predeploy`.
- Canonical Release build/link passed and `tools/AuditPixlRenderer.ps1` passed all 37 integrated modules. Windows SDK FXC also compiled a representative live `Lighting.hlsl` pixel permutation with `PSHADER`, `SKINNED`, and `ACTOR_SURFACE_EFFECTS` enabled.
- Deployed DLL/PDB with Skyrim closed. Build/live DLLs match at 19,586,560 bytes, SHA-256 `573DE1F8C00FA91293E1D687A9C19C91F9E91932F19AD96614EF464440FDC3DC`. Live configuration, shader files, shader cache, beta staging package, and external repository were not changed.
- Runtime visual validation remains required. Re-test the same boots first, then trousers/robes and one NPC armour set. Confirm contamination covers the visible equipment surface rather than only exposed skin/soles; equip/unequip an item and switch first/third person to verify ownership remains stable. No visual success is claimed from compilation alone.

## Release Handoff Build - 2026-08-30 Stable Actor Masks / Ground Tuning / Roof Runoff

- Follow-up captures showed two separate Actor Surface Effects defects: armour draws could now enter the pixel path, but the payload owner could still be missed on dismember geometry, and contamination was evaluated from animated world position so gait/jump motion moved the boundary. Prepass now indexes live skeleton nodes for active contaminated actors, bounded bone lookup maps skinned equipment back to its actor, and skinned shading evaluates persistent lobes from stable pre-skin model coordinates. Repeated contact widens/accumulates a lobe without dragging its centre every step.
- The existing 752-byte C++/HLSL actor payload, `b13` binding, resource formats and register layout remain unchanged. A representative `SKINNED + ACTOR_SURFACE_EFFECTS` Lighting pixel permutation passed Windows SDK FXC validation.
- Ground Response now exposes Mud Darkening through `1.50x` and Interaction / Compaction Strength through `3.0x`; saved defaults are unchanged. Static tracing reconfirmed the existing grass-collision current/previous displacement path remains active.
- Roof runoff uses a one-pixel authoritative bead footprint, smaller emitter apertures and jitter, tighter distance behavior, reduced shoulder dilation, and a narrower composite threshold/highlight. Both modified compute shaders passed FXC validation. This is expected to reduce fuzzy/dithered DLSS/TAA breakup, but camera-motion behavior still requires live testing.
- Reconfirmed global Contact Shadows are an independent active compute module bound at `t45` and consumed by Lighting, grass and distant trees. World Probes water SSR and Water Optics enhanced SSR remain active. PIXL does not currently ship a second generic opaque SSR pass; opaque reflections continue through Hybrid/reflection/world-probe paths to avoid double contribution.
- Canonical Release build/link and `tools/AuditPixlRenderer.ps1` passed all 37 integrated modules. Build/live/staged DLL SHA-256 is `21F6F3864422243D4F2970A978112CEDB23C3D92C7981E49D61C36FC139A7C07` (19,589,120 bytes). Only inherited FidelityFX shared-intermediate `MSB8028` warnings remain.
- Deployed the DLL and only affected Lighting/ActorSurfaceEffects/RoofRunoff shader sources with Skyrim closed. No shader cache was deleted. Rebuilt the current beta staging directory from canonical source and copied the current live `UserGraphics.json`; staged Image Reconstruction is `qualityMode=1`, `upscaleMethod=1` (TAA), frame generation off. Package audit passes and staged DLL matches the build.
- Updated `.gitignore` to exclude checkpoints, the Unity reference tree, and exact loose root runtime/debug copies while retaining canonical source/assets. Updated the public README for 37 modules, Actor Surface Effects, native Seasons compatibility, current WindowLife behavior and the Medium + TAA compatibility baseline.
- Runtime visual validation remains required: armour/clothing accumulation stability while walking/running/jumping, equip/unequip and first/third-person ownership, mud/compaction control ranges, roof runoff size and temporal behavior, plus a quick Contact Shadows/grass-collision/water-SSR smoke test. No visual success is claimed from build validation.

## Public Source Publication - 2026-08-30

- Committed the complete private engineering state through `2ebdd06ca0889a1d449addd528c5c45d401e5971`. The intentionally dirty FidelityFX submodule still matches the committed reproducible `FidelityFX-DX11-Short-Output.patch` exactly and its public gitlink remains pinned at `054f0ade7bd443710644ecb2564936281821f532`.
- Generated the audited GPL source snapshot with `tools/ExportPixlPublicSource.ps1`: 855 archive entries, SHA-256 `A38FC22E5054A4FEB60AEF744A918E45E85CCEAE81851077165AC3283F38E7C5`. Private ledgers/instructions, `PIXL_AUTONOMOUS`, PixDiT research, Unity reference assets, checkpoints, build output, runtime mirrors, caches and loose debug copies are excluded.
- Fixed `tools/AuditPixlRenderer.ps1` so a public clone correctly skips absent private instruction roots. The public-only source worktree then passed the 37-module audit against the verified Release DLL, plus credential-pattern, private-path, forbidden-path, submodule-pin and GitHub file-size checks.
- Updated public GitHub `main` by fast-forward only from `8d9011be` to source commit `746f627f` (`Update PIXL Renderer source and module documentation`). Push to `git@github.com:pixlmusic/PIXL-Renderer.git` completed successfully; no force-push, tag or release was created. GitHub emitted a non-fatal recommendation for the existing 70.77 MiB NVIDIA DLSS runtime binary, which remains below GitHub's 100 MiB hard limit.

## Experimental Four-Preset Configuration Set - 2026-08-30

- Positively identified the active baseline through `Util::PathHelpers::GetSettingsUserPath()` and `State::Load`: `Data/SKSE/Plugins/PIXL/Config/UserGraphics.json`. Its SHA-256 is `CB7EFF2FBCC8BEB0226345E1624DC9BE253B6F267CB2132A61DDBCA54DF1490C`; the current beta-staging copy is byte-identical. The live baseline was not modified.
- Added the reproducible generator `tools/GeneratePixlPresetExperiments.ps1` and generated `PIXL_Preset_Experiments/PIXL_Baseline_REFERENCE.json`, `PIXL_Complete.json`, `PIXL_Extreme_Fidelity.json`, `PIXL_Minimum.json`, and `PIXL_Linear_Diffuse.json`.
- The profiles use the actual seven-group contracts from `Renderer/QualityProfiles.cpp`: Complete uses High plus DLAA/profile F, Extreme uses Ultra plus DLAA/profile F and maximum validated Photo Finish sampling, Minimum uses the Low contracts plus normal runtime feature gates and FSR Ultra Performance, and Linear Diffuse uses High/TAA with a coordinated Linear Light Core, exposure, ambient, GI/AO and bloom calibration. Frame generation remains off in every profile for deterministic A/B testing.
- Added `PIXL_Preset_Experiments/CONFIGURATION_MAP.md` for every serialized baseline leaf/array, `PRESET_DIFFS.md` for semantic baseline differences only, and `PRESET_REPORT.md` for loader evidence, hashes, rationale, known disconnected/compatibility-only fields and the ordered live-test plan.
- Independent validation passed: all JSON documents parse; recursive key ordering, arrays and 900 leaf paths match the baseline; no duplicate JSON members or non-finite numbers exist; changed enum/workload/camera/actor/ground/Linear Light Core fields fall within source loader/UI ranges. The reference is byte-for-byte identical to the baseline, and the baseline hash was verified again after generation.
- Linear Light Core was traced through `LinearLightCore.cpp/.h` and `distribution/Shaders/Common/Color.hlsli` plus Lighting/Grass/Water/Effect consumers. No disconnected binding was found, so no separate `LINEAR_DIFFUSE_FINDINGS.md` was warranted. The fixed-false `isDirLightLinear` input assumption is documented as a runtime calibration limitation rather than asserted to be a defect.
- This was configuration/documentation tooling only. No C++, HLSL, build system, live configuration, DLL, staging package, shader cache or external repository was changed. No build was required. All four visual outcomes remain pending live Skyrim testing; no visual success is claimed.

## Hair Reconstruction Module - 2026-08-31

- Added the 38th integrated PIXL module, `Hair Reconstruction`, as a release-safe hybrid card reconstruction path. It requires no hair mod, NIF/XML/groom patch, FormID list or external physics dependency and augments the existing PIXL Strand Shading BRDF rather than duplicating it.
- Runtime classification uses Skyrim's native Hair technique as authoritative, then conservatively upgrades only ordinary skinned-alpha Lighting materials with strong resolved texture/geometry-hierarchy hair evidence. Rejected plausible candidates keep original rendering and are visible through diagnostics. PIXL-owned descriptor bits 6/7 carry automatic/candidate state without colliding with vanilla flags.
- The Lighting vertex path infers root/tip flexibility from stable UV/card evidence and applies bounded actor inertia plus existing world-space PIXL wind to both current and previous skinned positions before the established motion-vector calculation. Distance, quality, wet weight, pause delta and teleport/rebase guards constrain the effect.
- The pixel path preserves authored flow maps, otherwise reconstructs strand direction from robust world-position/UV derivatives and the existing tangent frame. It reuses Kajiya-Kay/Marschner direct/indirect/self-shadow lighting, adds bounded transmission, derivative-filtered virtual fibre detail, optional card-edge silhouette breakup, rain/water darkening and wet roughness, and hair snow compatibility through Actor Surface Effects.
- Added a 96-byte, six-register aligned C++/HLSL settings block after Strand Shading. No existing field was reordered; no SRV/UAV/sampler/cbuffer register, render target, pass, draw, compute dispatch or persistent per-actor resource was added.
- Added connected Low/Medium/High/Ultra Characters quality mapping, complete serialized GUI/reset behavior, conservative Medium clean-install/default settings, Ultra settings, English module metadata, and seven debug views: classification, direction, root/tip, flexibility, motion, temporal confidence and virtual fibres.
- Prepatch checkpoint: `build/active-dev-checkpoints/20260831-hair-reconstruction-prepatch`.
- Windows SDK FXC passed native Hair, automatic Hair and candidate/debug pixel permutations plus isolated native/automatic skinned vertex motion permutations. Native/automatic accepted paths produced identical bytecode per stage. The temporary vertex-validation shim only bypassed the stock standalone-FXC Lighting `b12` declaration collision and was deleted; shipping register source was unchanged.
- Canonical Release build/link and `PIXL-Audit` pass with all 38 integrated modules. DLL: 19,609,600 bytes, SHA-256 `B52A6981B31CD7E8663BC729DA55E5BB64091D831229CD105526EED5ECBD251B`. Only inherited FidelityFX shared-intermediate `MSB8028` warnings remain.
- Documentation: `docs/HairReconstruction/PipelineAudit.md`, `README.md`, and `ImplementationReport.md`.
- Runtime/visual status: **pending**. No visual, DLSS/FSR, frame-generation, alpha-order, wet-hair, wind or snow success is claimed from static validation. Medium should be validated first; High/Ultra procedural edge fibres remain a separate stress test. True topology caching, generated ribbon geometry, semantic lobe springs, collision primitives and hair-only OIT are documented future work rather than unstable release prerequisites.
- Live deployment was verified with Skyrim closed. The Release DLL, `Lighting.hlsl`, shared data ABI, Hair Reconstruction kernel, Strand Shading hair kernel, Actor Surface Effects integration, module manifest, renderer defaults and English locale are hash-identical to canonical source. Live rollback checkpoint: `build/active-dev-checkpoints/20260831-hair-reconstruction-live-predeploy`.
- The owner configuration `Data/SKSE/Plugins/PIXL/Config/UserGraphics.json` remained byte-identical through deployment (SHA-256 `827CDF4ACCE18B8438362A64B08C7D07EFAEB534E99BC1FE7CDB09868BC6726E`). `Data/ShaderCache` remains present and was not deleted; only affected/new permutations may compile on launch.

## Hair Reconstruction Deployment Correction - 2026-08-31

- The first Hair deployment placed the new DLL at the incorrect nested data path `Data/SKSE/Plugins/PIXL/PIXLRenderer.dll`. SKSE actually loads only the root plugin `Data/SKSE/Plugins/PIXLRenderer.dll`, which remained the preceding 19,590,144-byte build (SHA-256 `703940D67C0F2096586E2A068014A38E46F526DC5FC1BC499F1E40877B265495`).
- The newer shared HLSL layout had already inserted the 96-byte Hair Reconstruction settings block while the loaded old DLL still uploaded the pre-Hair `FeatureData` layout. The resulting CPU/GPU ABI offset mismatch explains the owner-captured full-frame lavender/normal-like output. The log independently confirmed the mismatch by reporting `Found orphaned unknown feature INI: HairReconstruction` and compiling no permutation with `HAIR_RECONSTRUCTION`.
- With Skyrim closed, backed up the actual loaded DLL/PDB to `build/active-dev-checkpoints/20260831-hair-reconstruction-correct-root-live-predeploy`, deployed the Release DLL/PDB to the correct root SKSE plugin path, and verified SHA-256 `B52A6981B31CD7E8663BC729DA55E5BB64091D831229CD105526EED5ECBD251B`. The misleading nested DLL/PDB were moved into that rollback checkpoint and no longer remain in the live PIXL data folder.
- The shader cache was not deleted. The user configuration saved during the mismatched run was preserved (post-run SHA-256 `2EF48B8C10F0C28F9DED315EAC5A7F04941F88856CBADF3AEAD583737D34F43A`); it contains no enabled global debug view and will receive the missing Hair Reconstruction section from safe defaults on the next correct startup.
- Next-launch acceptance: startup must log `Loading RenderModule: 'HairReconstruction'` and `HairReconstruction.ini 1-0-0 successfully loaded`, with no orphan warning. Lighting compile lines should include `HAIR_RECONSTRUCTION`; the normal scene must replace the lavender frame. Runtime hair quality remains unvalidated until then.

## Hair Reconstruction Shared-Buffer ABI Hotfix - 2026-08-31

- The corrected root DLL allowed Hair Reconstruction to load, but the next live capture rendered a mid-grey scene with black geometry. The log contained no shader or module initialization failure and the saved configuration had all global debug modes disabled. The image matched PIXL's MaterialForge roughness diagnostic, proving cached shaders were reading the wrong shared settings rather than an enabled user diagnostic.
- Root cause: the new 96-byte `HairReconstructionSettings` block had been inserted between `StrandShadingSettings` and `TerrainDetailSettings` in global `FeatureData`. Existing cached shaders compiled before Hair Reconstruction therefore read every later block (Terrain Detail through Skin Optics/MaterialForge) at a 96-byte-shifted offset. Hair's descriptor-selective cache invalidation could not repair non-hair consumers of the globally shifted layout.
- Fixed the ABI without deleting the shader cache: moved Hair Reconstruction to the end of the C++ packed pipeline tuple and the end of the HLSL `FeatureData` structure. All pre-existing field offsets are now byte-identical; the new field is an append-only extension. Added explicit append-only comments on both sides and bumped `HairReconstruction.ini` from `1-0-0` to `1-0-1` so only Hair-affected Lighting permutations are invalidated/recompiled.
- Prepatch rollback checkpoint: `build/active-dev-checkpoints/20260831-hair-featuredata-append-prepatch` (canonical CPU/HLSL/manifest plus exact live shader, manifest and root DLL/PDB).
- Canonical Release build/link passed and `PIXL-Audit` passed all 38 integrated modules. Representative Hair Reconstruction and ordinary non-hair MaterialForge Lighting pixel permutations both passed Windows SDK FXC. Build/live DLLs match at 19,609,600 bytes, SHA-256 `95C4A76E7D96392D268ED917EDB45A7C65021025E44F91030879D45B14225C1B`.
- Deployed only the corrected root DLL/PDB, append-only `SharedData.hlsli`, Hair manifest `1-0-1`, and corrected English locale with Skyrim closed. No nested DLL exists, `Data/ShaderCache` was preserved, and the user graphics configuration was not modified.
- Reworded the unknown-module UI warning from "newer CS versions" to "newer PIXL Renderer versions" in canonical/live English locale; both JSON files parse and are hash-identical. Beta locale contains the corrected wording but the beta package itself was intentionally not partially restaged during this live hotfix.
- Runtime/visual status remains pending. Next launch should compile only affected Hair permutations, log `HairReconstruction.ini 1-0-1 successfully loaded`, and render the normal scene rather than the grey/black diagnostic frame. No visual success is claimed until that live check passes.

## Hair Reconstruction Contaminated Pipeline Cache Quarantine - 2026-08-31

- The first append-only live test still rendered almost-black materials while emissive fire/light sprites survived. The latest log showed Hair Reconstruction `1-0-1` loading without compile errors and the current `UserGraphics.json` remained identical to the live/default PIXL baseline for exposure, debug views and lighting settings.
- The remaining cause was historical bytecode, not the current source/config: 696 `.pixlbin` entries (27,499,008 bytes) had timestamps between the original middle-layout `SharedData.hlsli` deployment (07:06:44) and the append-only replacement (11:10:44). These Water/Lighting shaders were compiled against the transient shifted `FeatureData` ABI and remained invalid even though older pre-Hair and newer append-only entries were correct.
- With Skyrim closed, removed exactly that timestamp-bounded generation from the active `Data/PIXL/PipelineLibrary`. No full cache reset was performed: 2,801 compatible pipeline entries remain active, including corrected append-only Hair entries generated after 11:10:44. The active contaminated count is now zero.
- Recovery location: `build/active-dev-checkpoints/20260831-hair-featuredata-contaminated-cache-quarantine`. Because pipeline shards in different directories can share a filename, the quarantine retains 348 unique generated shard snapshots; all removed content is reproducible by PIXL's normal shader compiler and no source, configuration, savegame or authored asset was removed.
- Next launch will rebuild the missing affected Lighting/Water permutations once. Runtime/visual status remains pending until a normal lit scene is observed after that compile completes.

## Hair Reconstruction Runtime Rollback - 2026-08-31

- Further live captures showed mixed shader-family failure: ordinary opaque geometry was black while selected skin/fur and emissive paths remained visible. Selectively rebuilding the transient-layout cache was insufficient, so Hair Reconstruction is no longer considered safe for the live baseline.
- With Skyrim closed, restored the exact pre-Hair root plugin from `20260831-hair-reconstruction-correct-root-live-predeploy`: DLL SHA-256 `703940D67C0F2096586E2A068014A38E46F526DC5FC1BC499F1E40877B265495` (19,590,144 bytes) and its matching PDB.
- Restored the exact pre-Hair live `Lighting.hlsl`, `Common/SharedData.hlsli`, `Hair/Hair.hlsli`, and `ActorSurfaceEffects/ActorSurfaceEffects.hlsli` from `20260831-hair-reconstruction-live-predeploy`. Restored shared-data SHA-256 is `69FE8FD3875D7BD1C3FF86FB98428F815E2DD54BF212C4DFD0C9AAC1E0FEB6C8`.
- Removed the live Hair Reconstruction manifest and kernel from the shader/module discovery paths and retained them in `build/active-dev-checkpoints/20260831-hair-runtime-rollback-current-broken/removed-live-hair-files`. The current broken runtime set was backed up first under the same checkpoint.
- Quarantined all 1,349 pipeline shards (163,850,436 bytes) compiled at or after the first Hair shared-shader deployment. Their relative cache paths are preserved under `post-hair-pipeline-cache`; 2,149 pre-Hair pipeline entries remain active. The next launch will rebuild only missing pre-Hair permutations.
- The current user configuration and savegames were not overwritten or deleted. `UserGraphics.json` remained SHA-256 `A04AF783B01977FA7137FCB2095A88345F9FDDFB16B9633A7BD66C0812D3B21D`; any unknown Hair JSON block is inert under the restored DLL.
- Canonical source still contains the Hair implementation for offline diagnosis, but it must not be rebuilt/deployed as the live baseline until its shared ABI, descriptor classification and cross-family cache behavior pass an isolated validation gate. Runtime status of the restored pre-Hair baseline is pending one live launch after the necessary partial cache rebuild.
- A post-rollback launch at 12:04 loaded exactly the 37 pre-Hair render modules, contained no `HairReconstruction` log entry or live Hair kernel/manifest, and reported no module/shader initialization error. It reached the foreground compiler gate but was closed before creating replacement cache entries; the active known-good cache remains 2,149 shards. The next launch must be allowed to finish the partial rebuild before visual validation.
- Owner subsequently completed the rollback compile and confirmed the renderer looked correct in game. The foreground queue reached `remaining=0`, the active pipeline library rebuilt to 3,453 shards (209,262,748 bytes), and Skyrim shut down cleanly. Log entries containing the ordinary `HAIR` permutation define are vanilla lighting shader variants, not the removed Hair Reconstruction module. This exact pre-Hair live runtime is now the current known-good visual baseline; Hair Reconstruction remains offline-only pending redesign.

## Crysis 3 Pipeline Foundation Batch - 2026-08-31

- Reconstructed the requested CryEngine 3 techniques against PIXL's actual current pipeline. The central finding is that PIXL already owned most of the proposed renderer foundations: HybridGI produces SSDO-style bent direction/visibility; Deferred owns a thin auxiliary scene buffer; Atmosphere owns shadowed temporal froxels; Material Forge performs conservative legacy-material normalization; particles receive sun/sky/local lighting; reflections already use screen/history/probe/sky fallback; Camera Suite owns a modern post-reconstruction bloom pyramid; and WindowLife already implements the principal advanced-glass response. No duplicate global passes were added.
- Upgraded the existing HybridGI Directional Visibility path to `4-3-0`. History filtering now decodes octahedral bent normals before spatial accumulation, confidence derives from actual angular/sample coverage, geometry changes drive temporal rejection, near-opposite vectors use a finite fallback, and half/quarter reconstruction adds normal-aware weighting through the existing normal pyramid. The 400-byte HybridGI constant ABI, render-target formats and deferred `t17` consumer remain unchanged; the upsample dispatch adds only the existing normal-pyramid SRV at local `t6`.
- Stabilized particle-derived local lighting in `RadiantGrid` with CPU-only per-frame owner aggregation. Layered/repeated submissions from one fire/torch/brazier billboard now retain one brightest/largest representative rather than multiplying radiance and cluster occupancy. Non-finite payloads are rejected and queues drain safely while disabled. No GPU light-data ABI, shader define, register or cache version changed.
- Added a bounded Water-family caustic prototype. The existing Water pixel path now derives a Jacobian focusing term from the final live wave + flowmap + rain-ripple normal and the visible refracted receiver distance. It modulates only exterior above-water refracted receiver colour while preserving reflection, fog, tint, alpha/Fresnel and the authored receiver-caustic path. It adds no pass, resource, binding, texture sample or CPU/GPU ABI field.
- Hardened Atmosphere temporal validity. Froxel history now rejects FOV/projection changes, exterior worldspace identity changes and changes in directional-shadow/depth/IBL/SkyBounce/local-light input availability, in addition to the existing cut/resolution/scene/resource resets. This is CPU-only and adds no shader permutation, pass, resource or GPU ABI change.
- Preserved Material Forge's accepted visual tuning while fixing the automatic-hair `chair`/furniture hierarchy false positive and adding collapsed developer-only registry health diagnostics. Documented, but deliberately deferred, the global `AUTO_FUR` roughness/F0 write-order defect because a safe fix requires a separately scheduled Lighting permutation rebuild and visual A/B.
- Added a source-only deterministic `WorldWind` sampling contract and adapted Foliage Dynamics through an exact compatibility wrapper. The active grass vertex permutation remained byte-identical, proving no current visual change. This file was not deployed; it is the future shared wind input for foliage, hair, smoke and particles, not a speculative 3D force-grid simulation.
- Designed, but did not ship, a no-ABI finite-area GGX approximation for extended emitters. Full rectangle/disc/tube/LTC lights, new auxiliary MRTs, constraint-simulated foliage, persistent world decal atlases, thick-glass refraction and automatic clipping volumes remain explicit prototype/future work because they require broad ABI, cache or baseline changes.
- Audited Camera Suite optical finishing. PIXL bloom is already a scene-linear, exposure-aware, Karis/firefly-guarded soft-knee prefilter with half/quarter/eighth/sixteenth levels, rotational downsampling, energy-limited tent upsampling and post-reconstruction composition. Corrected one compatibility defect: when Physical Camera is disabled, the bloom prefilter now uses unity exposure rather than the persistent texture's last adapted value, preventing a bloom jump after toggling Physical Camera off. Camera Suite advanced to `1-6-4`; bloom remains default-off and was not visually retuned. Future ghosts/diffraction/starburst require coherent bright-source extraction and occlusion rather than a full-screen haze pass.
- Strict Windows SDK FXC validation passed the changed HybridGI full/half/quarter, disocclusion and upsample variants; five representative Water variants; all twelve Atmosphere variants; active foliage/Ground Response/precipitation/current-previous wind harnesses; representative Lighting/material/particle paths; and all three Camera Suite bloom compute stages. The final canonical Release build/link passed and `PIXL-Audit` passed all 38 installed modules. Build/live DLL: 19,611,648 bytes, SHA-256 `626A06C60757C4D95471822277A8DF524A43A3EE34F795CB55CF2AE220540CC2`. Only inherited FidelityFX shared-intermediate `MSB8028` warnings remain.
- The current live runtime was rechecked before deployment and was already the later 38-module Hair Reconstruction `1-0-2` baseline used by the owner's recent clean session (pre-batch DLL SHA-256 `5A44DE4514A1B2228DDE29672BB14AF3B0C493E9FA0E8E503A905A7960E77497`), not the older 37-module rollback described by the preceding stale ledger entry. This batch preserved that active module set; it did not redeploy SharedData, Lighting, Hair files or the Hair descriptor.
- With Skyrim closed, backed up the exact live DLL/PDB, Water shader, three HybridGI kernels, HybridGI descriptor, Camera Suite bloom prefilter and Camera Suite descriptor to `build/active-dev-checkpoints/20260831-crysis-foundation-live-predeploy`, then deployed only the new DLL/PDB, Water shader, HybridGI source/descriptor set, and the isolated Camera Suite bloom prefilter/descriptor. Source/live hashes were verified. The user configuration, presets, Hair/Lighting files, Foliage/WorldWind files, beta staging tree and external repository were not modified. The shader cache was not deleted.
- Reports: `docs/Crysis3Pipeline/CRYSIS3_TECHNIQUE_REVIEW.md`, `PIXL_CROSS_REFERENCE.md`, `PIXL_PIPELINE_ARCHITECTURE.md`, `IMPLEMENTATION_ROADMAP.md`, `DIRECTIONAL_VISIBILITY_REPORT.md`, `PARTICLE_RESPONSE_REPORT.md`, `DYNAMIC_WATER_CAUSTICS_REPORT.md`, `WORLD_FORCE_FIELD_FOUNDATION.md`, `EXTENDED_EMITTERS_REPORT.md`, `ATMOSPHERIC_VISIBILITY_REPORT.md`, `MATERIAL_INTELLIGENCE_REPORT.md`, `REFLECTION_HIERARCHY_REPORT.md`, `TRANSMISSION_AND_SSS_REPORT.md`, and `OPTICAL_GLARE_REPORT.md`.
- Runtime/visual status remains pending. On first launch, only HybridGI `4-3-0`, Water-dependent permutations and Camera Suite `1-6-4` bloom compute should rebuild; no global Lighting rebuild is intended. Validate bent-visibility motion at railings/eaves/caves, water caustic stability, fire/torch energy, Atmosphere across FOV/worldspace changes, bloom with Physical Camera toggled, and a warm second launch. No visual or performance measurement is claimed from static validation.

## Incandescent Geometry Lighting - 2026-08-31

- The owner supplied interior captures where candle/fire pixels were emissive and bloomed but nearby stone, furniture, floors and actors received little corresponding local or indirect illumination.
- Root cause: Radiant Grid's dynamic emitter extraction accepted supported soft-effect billboard particles only. Installed candle meshes such as the SMIM imperial candle family use ordinary `BSLightingShader` glow-map materials (`Candles01.dds` + `Candles01_g.dds`), so they never created a local-light record.
- Added a conservative cached incandescent-geometry classifier in `engine/Modules/RadiantGrid.{h,cpp}`. Only real glow-map materials whose resolved diffuse/glow paths identify candle/wick/torch/sconce/brazier/bonfire/campfire/hearth/fire/flame/ember/burning families are promoted. Generic glow maps, windows, magic and unrelated emissives remain unchanged.
- Accepted sources use the geometry world-bound centre, family-bounded radius, warm physical tint, authored-colour preservation for coloured flames and finite emissive-multiplier scaling. They reuse the existing Radiant Grid buffer, so opaque materials, particles, water, Atmosphere scattering and later HybridGI reuse see the same emitter without a new pass.
- Classification is cached per geometry plus material identity, invalidates on material changes and is removed by the existing geometry-destruction hook. Developer statistics separately show particle and glow-mapped emitter counts.
- No GPU ABI, HLSL, shader register, resource, pass, descriptor, permutation define, config file or shader cache changed. Existing `Enable Particle Lights` remains the real-time master fallback.
- Prepatch checkpoint: `build/active-dev-checkpoints/20260831-incandescent-geometry-prepatch`. Live rollback checkpoint: `build/active-dev-checkpoints/20260831-incandescent-geometry-live-predeploy`.
- Canonical Release build/link PASS and `PIXL-Audit` PASS with all 38 integrated modules. Build/live DLL: 19,617,792 bytes, SHA-256 `22EC5BD78E33672D6F3F435F6339882719476D20BB4A273CC2268AD3EF5F68A0`. Only inherited FidelityFX shared-intermediate `MSB8028` warnings remain.
- Deployed only the root SKSE DLL/PDB with Skyrim closed. User configuration, live shaders, shader cache and staging were not modified.
- Runtime visual status: **pending**. Revisit the supplied dungeon and verify warm surface illumination, fog response, camera stability and no false emissive sources/light leakage. No visual success is claimed from build validation alone.
- Detailed report: `docs/Crysis3Pipeline/INCANDESCENT_EMITTERS_REPORT.md`.

## Seasonal Snow / Actor Accumulation / Particle Energy Fix - 2026-08-31

- Owner captures isolated three defects: explicit snow-covered mountain/cliff landscape layers were excluded from Ground Response, actor contamination detached during jump/root motion, and overlapping local lights drove snow particles into emissive white blocks near NPCs.
- Ground Response now lets resolved `snow`/`snw` texture evidence override structural path tokens while preserving explicit no-snow names and all existing slope/receiver/blocker checks. This repairs generic Seasons of Skyrim / Turn of the Seasons landscape naming without hardcoded forms or season-equals-snow behavior.
- Actor Surface Effects now converts contacts and publishes the shader origin from the live actor 3D root transform, falling back to `Actor::GetPosition()` only when unavailable. The 752-byte character ABI, event storage, material composition and lifecycle remain unchanged.
- Particle Response now applies one bounded soft shoulder to the aggregate snow-only Radiant Grid local-light energy instead of compressing each light before summation. Rain, directional/ambient precipitation lighting and opaque lighting are unchanged.
- Source checkpoint: `build/active-dev-checkpoints/20260831-snow-actor-particle-prepatch`. Live rollback checkpoint: `build/active-dev-checkpoints/20260831-snow-actor-particle-live-predeploy`.
- Release build PASS; `PIXL-Audit` PASS with 38 modules. Strict FXC `/Ges /WX /O3` PASS for snow/RadiantGrid/NaturalLighting, rain/RadiantGrid/NaturalLighting and base Particle pixel permutations.
- Build/live DLL: 19,618,816 bytes, SHA-256 `9BE104774C50DCEC0E17835198AE146B1CD7EF537D06DC0D456F5B7A7068D027`. Canonical/live `Particle.hlsl`: SHA-256 `E3770E91E9839C1B94465AB77CFD8F83B16B7DFEE0FCEFF2A4C3F76CC46212A2`.
- Deployed only the root DLL/PDB and `Particle.hlsl` with Skyrim closed. User configuration remained SHA-256 `83C58754CD6EB45B7429E9248891FF6E70BD093395454A3F964D4D0A057BCB69`; no shader cache was deleted.
- Runtime visual status is pending. Validate seasonal mountain/cliff snow coverage, accumulation through walk/sprint/jump/landing, and snowfall beside a torch-bearing NPC. Detailed report: `docs/Crysis3Pipeline/SNOW_SURFACE_STABILITY_REPORT.md`.

## Native DLSS Neural Rendering Sidecar Integration - 2026-09-01

- Added an opt-in, version-gated DLSS Neural Rendering bridge for the installed NVIDIA `nvngx_dlssnr.dll` 310.8.x runtime. It dynamically resolves NGX Feature 18 and introduces no DLL import dependency; absence, unsupported versions, missing exports, initialization failures, or evaluation failures retain the ordinary DLSS frame.
- Reused PIXL's existing D3D11-to-D3D12 swap-chain sidecar, device, queue, command allocators, command lists, fences, and shared resources. No second D3D12 device or competing presentation wrapper was introduced.
- Added a dedicated shared NR output and evaluates it between the D3D11/D3D12 synchronization point and the final swap-chain copy. The runtime consumes PIXL's typed depth and post-`EncodeTexturesCS` dilated DLSS motion guide with the actual dynamic-resolution subrect; frame generation retains its existing raw pre-upscale guide copy.
- Added Image Reconstruction configuration and GUI controls: master enable, Natural/Balanced/Detail/Strong/Custom presets, 0-2 intensity/local-tone/local-structure/skin-structure controls, model style 0-3, automatic character mask, UI correction, and read-only runtime/version/detail/successful-frame diagnostics. Defaults remain disabled and Natural is the restrained starting preset.
- The first validation policy is DLSS + SDR + Frame Generation Off. PIXL bypasses native NR if HDR or Frame Generation is configured, or if `renodx-dlss.addon64` is loaded, preventing double processing. Enabling/disabling requires restart because the swap-chain sidecar is selected at device creation.
- The 310.8 runtime's signed-core identity check is handled by an immediately restored, runtime-module-local import-slot scope during vendor calls. If that scoped operation cannot be installed, the optional feature disables safely.
- No HLSL, shader descriptor, permutation define, register, render-target format, or C++/HLSL shared constant ABI changed; no shader-cache rebuild is required for this feature.
- Canonical Release build PASS and `PIXL-Audit` PASS with 38 modules. Final source build DLL: 19,645,440 bytes, SHA-256 `45E7FDA808905E1822F1C60E7AA8F901F536DB525E206730202F9D8C2B1B5B22`. Only inherited FidelityFX shared-intermediate `MSB8028` warnings remain.
- Source report: `docs/ImageReconstruction/DLSS_NEURAL_RENDERING.md`. With Skyrim closed, backed up the exact prior live DLL/PDB and RenoDX addon to `build/active-dev-checkpoints/20260901-dlssnr-native-sidecar-live-predeploy`, deployed the root DLL/PDB, and recoverably renamed `renodx-dlss.addon64` to `renodx-dlss.addon64.disabled-by-pixl-native`. Build/live DLL hashes match at SHA-256 `45E7FDA808905E1822F1C60E7AA8F901F536DB525E206730202F9D8C2B1B5B22`; no shader, cache, preset, or user configuration was modified. Runtime/visual status is **pending** until the feature is enabled in the GUI and Skyrim is restarted.

## Neural Photo Finish Capture / NR -> Frame Generation - 2026-09-01

- Confirmed the owner's capture concern: Photo Finish selected the D3D11 framebuffer before the DX12 sidecar evaluated DLSS Neural Rendering, so a saved temporal stack was not guaranteed to contain the neural result even though live presentation did.
- Added a Photo Mode-only `Neural Photo Reconstruction` toggle, default off and capability-gated. Unsupported/non-DLSS/TAA/FSR users retain the existing universal Photo Finish path without an NR dependency; unavailable neural capture falls back safely rather than cancelling the photo.
- Neural outputs are now double-buffered and published with a monotonically increasing completed-frame serial. Photo Finish waits for a fresh completed neural frame for each deterministic projection-jitter phase and never copies the texture D3D12 is currently writing.
- Frame Generation may now follow Neural Rendering. Dedicated neural depth/motion shared resources preserve the post-DLSS encoded guide contract while FSR frame generation retains its raw pre-upscale guide resources, preventing the previous resource overwrite that made simultaneous operation unsafe.
- The existing Photo Finish 1/4/8/16/24-frame accumulation and Native/2x/4x final output remain the quality controls. PIXL does not expose a fake private-runtime 2x/4x transformer resolution: the validated vendor evaluation remains display resolution, then the real jitter-aware Photo Finish resolve creates the selected larger final image.
- Source checkpoint: `build/active-dev-checkpoints/20260901-photo-neural-capture-prepatch`. Release build PASS and `PIXL-Audit` PASS with 38 integrated modules. No HLSL, shader descriptor, permutation define, register, GPU constant ABI or shader cache changed.
- With Skyrim closed, backed up the exact prior live DLL/PDB to `build/active-dev-checkpoints/20260901-photo-neural-capture-live-predeploy` and deployed only the rebuilt root DLL/PDB. Build/live DLL: 19,648,512 bytes, SHA-256 `6B451C0BC9852B13A69E87F1A78ACC749F6FFF5E1E2F7A74FE547A478D95E8D3`. Prior live DLL SHA-256 `45E7FDA808905E1822F1C60E7AA8F901F536DB525E206730202F9D8C2B1B5B22` remains recoverable. Runtime visual/capture/combined-FG validation is pending.

## Scaled DLSS Neural Contract / Photo Composite Test Build - 2026-09-01

- Owner runtime testing established that Feature 18 looked coherent at DLAA/native resolution but produced offset or shadowed detail when ordinary DLSS scaling was active. Source audit found a contradictory creation contract: the color/output was display resolution and depth/motion subrects were render resolution, but the generic and `DLSSNR.Width/Height` fields incorrectly advertised display resolution. DLAA concealed the defect because both domains were equal.
- `NeuralRendering.cpp` now publishes `Width/Height` and `DLSSNR.Width/Height` as the real guide/render extent, while `OutWidth/OutHeight` and explicit output fields remain the display extent. The post-DLSS color remains display resolution, guide subrects remain render resolution, and the runtime logs the complete two-extent contract on feature creation.
- Local Streamline documentation confirmed PIXL motion vectors are normalized UV, so `mvecScale={1,1}` remains correct. `EncodeTexturesCS` already produces a closest/longest 5x5 dilated motion field; `Streamline.cpp` now correctly declares `motionVectorsDilated=true` rather than misdescribing that input.
- Photo Finish's neural preference is now default-on but remains capability-gated and user-toggleable. The Photo Mode label is `Capture Neural Final Composite`, remains editable before the first completed neural frame, and clearly reports whether it is waiting for NR or using the universal TAA/FSR/non-DLSS capture path. The current live config was intentionally set to `PhotoFinishNeuralEnabled=true`; previous logs proved all earlier captures had explicitly used `neural source off`.
- Source/live rollback checkpoint: `build/active-dev-checkpoints/20260901-neural-scaled-photo-live-predeploy`. Release build PASS; `PIXL-Audit` PASS with all 38 modules. Build/live DLL: 19,650,048 bytes, SHA-256 `AA7CD935E0191C21AD825ED7FAC2D709747A060B8D394EDC6E54538B20D745EB`. Only inherited FidelityFX shared-intermediate `MSB8028` warnings remain.
- No HLSL, shader descriptor, permutation define, register or shader-cache version changed; no cache rebuild is expected. Runtime validation is pending: test DLSS Quality and Balanced for removal of the neural shadow/double image, then Photo Finish and confirm the log reports `neural source on`. The larger neural input/motion/memory/frame-pacing audit remains active after this focused test build.

## Neural Rendering Processing Resolution - 2026-09-01

- Owner confirmed the corrected DLAA/scaled-DLSS contract works, but Feature 18 remains prohibitively expensive on RTX 3060 Ti-class hardware. Inspection of the installed RenoDX reference exposed no supported architecture-specific fast switch; the meaningful private-runtime workload control is its output/evaluation extent.
- Added `NR Processing Resolution`: Automatic (Recommended), Full 100%, High 85%, Balanced 75%, and Performance 67%. The choice is serialized as `ImageReconstruction.neuralRenderingResolutionMode` and requires restart because the shared DX11/DX12 output resource is created with the sidecar. Automatic selects 75% for <=8.5 GiB dedicated VRAM and 4K output, otherwise 85%.
- Safety policy never lets the reduced Feature 18 output fall below the active DLSS guide/render extent. DLAA therefore remains full resolution; use DLSS Quality or Balanced to obtain the intended 3060 Ti speedup. A read-only GUI line reports the actual allocated NR extent and percentage.
- Feature 18 writes the reduced output to a dedicated shared surface. A small embedded DX12 compute pass bilinearly reconstructs it into PIXL's existing full-resolution, double-buffered neural presentation outputs. This preserves frame-generation order and Photo Finish's completed full-resolution neural-composite source. If pipeline creation is unavailable, PIXL logs a warning and reverts to full-resolution NR; if Feature 18 evaluation fails, ordinary DLSS remains the presentation source.
- The full-resolution presentation pair remains allocated for race-free Photo Finish. Reduced mode therefore adds one 4-byte-per-pixel model surface (approximately 7.9 MiB at 75% of 2560x1440), trading modest PIXL-owned VRAM for a substantially smaller vendor evaluation domain. No performance gain is claimed until runtime measurement; 75% contains 56.25% as many output pixels and 67% contains 44.89%, but private-runtime scaling and the final bilinear pass determine the real result.
- Release build PASS; `PIXL-Audit` PASS with all 38 modules. Build/live DLL: 19,656,704 bytes, SHA-256 `EB7C4A27AC18E46485CEEDAB866B45D8BCDE563C66BCFCB1628CA3DD5B52878A`. Rollback checkpoint: `build/active-dev-checkpoints/20260901-neural-resolution-selector-live-predeploy`. No shipped HLSL/permutation/cache version changed; the new upscale compute shader is compiled once from an embedded, isolated DX12 source string when the sidecar is created.
- Runtime validation pending: select DLSS Quality or Balanced, choose Automatic/Balanced/Performance NR, restart, verify log `Processing extent`, compare real render FPS/GPU frametime, inspect fine foliage/hair/text and motion, run Photo Finish and confirm `neural source on`, then test NR followed by FSR frame generation.

## Feature 18 Reduced-Output Rejection / Safe Rollback - 2026-09-01

- Runtime testing rejected the experimental independent Feature 18 output extent. At 1920x1080 display resolution the private 310.8 runtime returned `0xBAD00005` for a 1440x808 Feature 18 output, while ordinary DLSS remained the fallback presentation source. The log also exposed an initial Present before valid guide dimensions were published (`guides=1x1`).
- Removed the reduced-output resource, embedded bilinear upscale pipeline, automatic percentage policy, serialization field, restart state and GUI selector from canonical source. PIXL no longer advertises an unsupported or no-op neural-resolution control.
- Preserved the validated scaled-DLSS contract fix (`Width/Height` are the render/guide domain and `OutWidth/OutHeight` are display output), truthful dilated-motion declaration, full-resolution double-buffered neural presentation, and final-composite Photo Finish capture.
- Added a hard evaluation gate: Feature 18 is not called until the encoded depth/motion guide extent is larger than 1x1. This prevents a startup Present from latching the optional runtime into a session fault.
- Rejected runtime build/config preserved at `build/active-dev-checkpoints/20260901-neural-resolution-selector-rejected-runtime`. Previous working live binary was immediately restored from `build/active-dev-checkpoints/20260901-neural-resolution-selector-live-predeploy` while canonical source was corrected.
- Corrected Release build PASS and `PIXL-Audit` PASS with all 38 modules. With Skyrim closed, deployed the corrected root DLL/PDB; build/live DLL is 19,650,048 bytes with SHA-256 `925BC48B0262B2CE0663CB7ADBB0760AF1B06F7D1480A0CC0BB876A834D00D91`. Runtime verification remains required; no performance improvement is claimed. A future 3060 Ti workload control must use a vendor-supported Feature 18 quality/performance contract or reduce upstream DLSS render resolution, not invent an independent output extent.

## Neural Rendering Creation Controls / DLSS Preset Validation - 2026-09-01

- Audited the installed `nvngx_dlssnr.dll` 310.8.0.0, RenoDX reference addon, Streamline 2.10.3 headers/changelog and PIXL's real option path. Feature 18 demonstrably accepts `PerfQualityValue` plus `DLSSNR.Hint.Render.Preset`; both are now serialized, clamped and exposed under Advanced NR Tuning.
- `NR Quality Contract` provides Follow Current DLSS, DLAA, Quality, Balanced, Performance, Ultra Performance and Ultra Quality. Follow Current DLSS is the safe default. `NR Output Preset` provides Runtime Default and private Presets 1-3. Both are creation-time fields and the GUI clearly requires restart when changed.
- Feature-creation logs now include the resolved performance-quality value and output preset, so runtime behavior is verifiable rather than inferred.
- INT4 was not added: the installed 310.8 runtime contains FP8/FP16 paths but exposes neither INT4 kernels nor an application precision parameter. PIXL reports vendor automatic precision in the UI rather than writing an invented key.
- DLSS preset E was not added: NVIDIA removed E from Streamline 2.10.3 and unsupported E resolves to Default. Existing L/M UI entries were also removed because this SDK resolves them to Default. PIXL now exposes only honored SR presets: Default, J, K and F; legacy stored L/M values migrate safely to Default.
- Release build PASS and `PIXL-Audit` PASS with all 38 integrated modules. With Skyrim closed, backed up the previous live DLL/PDB/config to `build/active-dev-checkpoints/20260901-neural-creation-controls-live-predeploy` and deployed the rebuilt root DLL/PDB plus the two explicit default-valued user-config keys. Build/live DLL is 19,653,120 bytes with SHA-256 `D83F17D6DD472DA14DA8A7F8DCBFF8263B12C8F987D8C048422EA13036054338`. No HLSL, shader descriptor, permutation define, register, render-target format or shader-cache version changed. Runtime validation remains required.

## Director Neural Final Composite / Accumulation Controls - 2026-09-01

- Added `Neural Final Composite` directly to the in-game Photo Mode `Insert` quick panel. Numpad 8/2 selects it and Numpad 4/6 toggles it. The readout reports `READY` when Feature 18 has published a usable completed frame, `ARMED` when the saved preference is waiting for a compatible session, and `OFF` when disabled.
- Enabling neural capture from either Director UI raises legacy one/four-frame settings to eight. The quick-panel `Neural Accumulation` row cycles 8/16/24 fresh completed frames; non-neural Photo Finish retains the full 1/4/8/16/24 range.
- The capture backend independently enforces an eight-frame minimum only when the neural source is genuinely active. Every deterministic jitter phase still waits for a new completed-frame serial, so a saved photo cannot silently reuse the same transformer output. Memory safety may reduce the burst for exceptionally large captures and remains explicitly logged.
- Accumulation is a single jitter-aware convergence resolve, not recursive Feature 18 feedback. This improves stable final detail without repeatedly amplifying model hallucinations or violating the private runtime contract.
- Canonical Release build PASS and `PIXL-Audit` PASS with all 38 integrated modules. With Skyrim closed, backed up the prior live DLL/PDB to `build/active-dev-checkpoints/20260901-photo-neural-insert-accumulation-live-predeploy` and deployed only the rebuilt root DLL/PDB. Build/live DLL: 19,653,120 bytes, SHA-256 `7A4DDCC70C90F415F51828ED43199C92B89BA5C4D25C64BB340D5BDCEC2C83EE`. Only inherited FidelityFX `MSB8028` shared-intermediate warnings remain. No HLSL, shader descriptor, permutation define, GPU ABI, shader cache, or user configuration changed. Runtime capture validation is pending.

## Transactional Offline Photo Finish / Recursive Neural Refinement - 2026-09-01

- Photo Finish now owns a complete capture transaction: exact native TFC translation/rotation and every keyboard, mouse, controller and PIXL-menu input are locked before the clean-frame delay and remain locked until reconstruction, encode and file save complete. A second request is rejected while request, sampling, worker processing or save state is active.
- Added capture-only internal render override (current, >=85%, native 100%) plus 0/4/8/16 fully rendered lighting/post convergence frames. The override resets affected DLSS/NR/FSR/HybridGI temporal histories on entry and restoration. Normal gameplay scale is restored immediately after GPU sampling.
- Added bounded 1/2/3-pass Feature 18 refinement. The first pass consumes the live PIXL/DLSS composite; later passes ping-pong the prior neural output back into Feature 18 with reset history. Optional recursive failure preserves the last valid output, drops back to one pass and does not poison normal live Neural Rendering.
- Final Native/2x/4x Photo Finish reconstruction remains the supported higher saved-image output domain. The vendor Feature 18 output remains at its validated display extent; PIXL does not reintroduce the previously rejected independent NR extent that failed with `0xBAD00005`.
- Added full Photo page and Insert quick-panel controls for internal render quality, lighting/post convergence and neural refinement count. Capture started from the full PIXL workspace now closes that workspace and uses the same clean immutable-camera path.
- No HLSL, shader descriptor, permutation define, GPU constant ABI, register, resource format, shader cache or user config was modified. Release build PASS and `PIXL-Audit` PASS with 38 modules. With Skyrim closed, backed up the prior live DLL/PDB to `build/active-dev-checkpoints/20260901-photo-offline-neural-transaction-live-predeploy` and deployed only the rebuilt root plugin binaries. Build/live DLL: 19,662,336 bytes, SHA-256 `C67FF6E8F866E7D103A5F98582157D5D197035867CE9ABD9C66A49387A8B2695`. Runtime validation remains pending. Detailed report: `docs/ImageReconstruction/PHOTO_FINISH_OFFLINE_NEURAL_PIPELINE.md`.

## Photo-Only Neural Bootstrap / Fresh-Frame Convergence - 2026-09-01

- Runtime testing proved `ARMED` was misleading: Photo Finish tested the real-time Neural Rendering master and required an already published Feature 18 output before starting, so a user with gameplay NR disabled could not invoke the model for a still image.
- DLSS sessions now provision PIXL's DX12 sidecar and Feature 18 guide/output resources at device creation independently of the real-time master. `Capture Neural Final Composite` can temporarily activate Feature 18 during the frozen transaction while ordinary gameplay NR remains off. TAA/FSR/non-DLSS/HDR/conflicting-addon sessions still fall back safely.
- The capture override now exposes effective DLAA quality mode 0 to Streamline and forces native 100% internal input without mutating or saving the user's gameplay DLSS quality or real-time NR setting. DLSS/NR/FSR/HybridGI histories reset on entry and exit; the original gameplay path is restored after the save completes.
- Removed recursive Feature 18 color feedback after runtime testing showed progressive blur. Feature 18 is a temporal reconstruction model, not a documented iterative image generator. The former 1/2/3 choice is now `Neural temporal convergence`: it requires 8/16/24 independent, newly rendered, correctly jittered and guided neural frames before PIXL's offline still resolve.
- Photo UI now reports `PHOTO READY` only when the sidecar/resources can service a capture and `UNAVAILABLE` otherwise; it no longer calls a saved preference `ARMED`. Full-page help text explains the temporary DLAA/native transaction and fresh-frame accumulation.
- Release build PASS; `PIXL-Audit` PASS with all 38 modules. Only inherited FidelityFX `MSB8028` shared-intermediate warnings remain. No HLSL, shader descriptor, permutation define, GPU ABI, shader-cache version or user configuration changed.
- With Skyrim closed, backed up the prior live DLL to `build/active-dev-checkpoints/20260901-photo-nr-bootstrap-predeploy` and deployed the rebuilt DLL to the live plugin and current beta staging. Build/live/staging DLL: 19,663,360 bytes, SHA-256 `D8017E98EA3F3A0A19D6B2FC6F797F25787F91B50895D5C6DE9180C4F20D8EB9`.
- Runtime validation pending: launch with DLSS and real-time NR off, confirm Photo page says `PHOTO READY`, capture at 8-frame convergence, verify the log reports temporary DLAA/native neural override and the saved file contains the NR composite, then confirm gameplay returns to its prior DLSS mode with NR still off. Detailed report updated: `docs/ImageReconstruction/PHOTO_FINISH_OFFLINE_NEURAL_PIPELINE.md`.
