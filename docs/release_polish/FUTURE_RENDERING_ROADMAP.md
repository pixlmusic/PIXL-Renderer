# Future Rendering Roadmap

## Visual research

1. Vendor-documented semantic or material guides for Neural Rendering, if NVIDIA exposes them publicly.
2. GPU-side robust Photo Finish resolve to reduce capture latency and CPU memory traffic.
3. Motion-compensated particle accumulation for unlocked or animated cinematic captures.
4. Validated vendor precision selection only if a supported Feature 18 API appears.
5. Per-material temporal confidence shared by DLSS, GI, reflections and sharpening.

## Performance research

1. Pool Photo Finish conversion surfaces and stream robust statistics without repeated conversion.
2. Profile the NR scene/guide quality contracts on RTX 3060 Ti, 4060, 4070 and 4090.
3. Add measured VRAM accounting for every sidecar surface to Pulse Profiler.
4. Investigate asynchronous photo resolve without weakening input/camera locking.
5. Validate clean-cache permutation warm-up tooling independently of user runtime caches.

These are post-release modular investigations, not hidden prerequisites for the current build.

## Per-module research backlog

Each cell contains five deliberately unimplemented directions. They are proposals, not promises or measured improvements.

| Module | 5 future visual improvements | 5 future performance improvements | 5 future feature / research ideas |
| --- | --- | --- | --- |
| Actor Surface Effects | finer mask edges; material-specific snow colour; layered mud drying; equipment transfer blending; improved creature coverage | sparse mask tiles; GPU actor culling; dirty-region uploads; lower-cost normal rebuild; adaptive update cadence | save-safe persistence; blood/magic layers; footprint sharing; first-person body adapters; creature topology classification |
| AmbientProbe | better directional lobes; HDR authoring validation; interior probe sets; sky/weather interpolation; roughness-aware diffuse | compressed lookup study; immutable upload path; sampler sharing; unused-mip removal; cache locality audit | automatic probe baking; mod-supplied probes; worldspace metadata; SH comparison; artist diagnostics |
| Atmosphere | multi-order colour fit; improved height haze; cloud/fog coupling; cave transition model; aurora-compatible scattering | froxel occupancy skip; checkerboard study; tiled light injection; adaptive Z slices; async update experiments | weather-profile authoring; local fog volumes; portal-aware interiors; lightning scattering; cloud shadow feedback |
| Camera Suite | lens-model bokeh; flare source occlusion; chromatic lens profiles; better autofocus subjects; HDR display tuning | pooled pyramids; fused post passes; histogram wave reductions; adaptive DOF tiles; GPU photo resolve | camera presets; path recording; focus tracking; exposure metering modes; capture metadata |
| Contact Shadows | multi-light confidence; penumbra growth; foliage-aware bias; alpha-tested blockers; better thin geometry | temporal reuse; half-resolution mode; tile light masks; adaptive ray steps; wave-friendly traversal | ray-query sidecar study; material thickness metadata; debug heatmaps; per-light budgets; capture-based tuning |
| Distance Blend | automatic tone match; snow LOD chroma; weather-aware distant wetness; sky-horizon coherence; improved object LOD normals | LUT consolidation; branch reduction; shared LOD constants; lower-frequency updates; half precision study | asset calibration tool; per-worldspace profiles; mod metadata; histogram diagnostics; seasonal presets |
| Foliage Dynamics | species stiffness; branch hierarchy; leaf torsion; rain loading; snow shedding | distance frequency reduction; vertex early-outs; species batching; normal reuse; adaptive flutter | shared world force field; author tags; collision impulses; storm damage cues; animation-mod API |
| Ground Response | layered crust; drainage channels; ice compaction; improved imprint rims; subsurface snow tint | sparse clipmap fields; indirect interactions; dirty-tile normals; lower-rate far updates; compressed histories | persistent tracks; vehicle/creature profiles; terrain-mod metadata; avalanche research; seasonal depth seeding |
| Hair Reconstruction (retired) | card-flow authoring; conservative silhouettes; wet clumps; snow strands; scalp transition | descriptor-only permutations; compact settings; distance culling; shared wind; cache-isolated prototype | isolated plugin prototype; cache ABI harness; mod hair corpus; topology confidence; explicit opt-in metadata |
| HorizonBlend | atmospheric horizon match; water horizon continuity; snow LOD blend; city silhouette handling; sunrise colour continuity | branchless blend; shared depth terms; per-pass scoping; constant caching; distant-only execution | worldspace profiles; diagnostic overlay; automated seam capture; LOD metadata; weather-aware tuning |
| Hybrid GI | emissive-aware bounce; better leak prevention; glossy GI; transparent confidence; improved disocclusion fill | tile classification; adaptive rays; reservoir reuse study; async probe updates; bandwidth format trials | probe/SSGI fusion; radiance cache clipmaps; material confidence API; hardware RT sidecar; offline reference captures |
| ImageReconstruction | material reactive masks; particle transparency masks; water disocclusion; hair motion confidence; HDR exposure contract | guide packing; copy fusion; resource aliasing; backend-specific resolution; sidecar VRAM accounting | vendor semantic guides; XeSS study; latency instrumentation; FG pacing model; automated backend matrix |
| Interior Daylight | portal-shaped daylight; coloured glazing; room bounce; cave mouth transition; window shadow softness | visible-portal culling; dirty cell state; shared shadow capture; distance scheduling; compact portal data | interior metadata API; cell authoring tool; weather penetration; time-of-day profiles; mod compatibility registry |
| Light Volumes | anisotropic media; emitter shapes; coloured density; shadow softness; interior fog volumes | empty-froxel skip; indirect dispatch; tile light lists; adaptive resolution; temporal update scheduling | volume authoring; portal volumes; smoke coupling; fire density fields; profiler overlays |
| Linear Light Core | calibrated white points; better legacy light conversion; emissive calibration; moon spectra; HDR paper-white policy | shared conversion LUT; cached light transforms; packed constants; branch audit; precision validation | photometric metadata; light-mod profiles; authoring diagnostics; HDR calibration tool; reference-scene suite |
| Material Layers | horizon-stable POM; anisotropic materials; better self-shadow; layered wet parallax; terrain macro relief | cone-step study; material tile skip; sample sharing; derivative cache; quality-by-distance | author material metadata; virtual textures; displacement bake tool; shader permutation analytics; mod validation corpus |
| MaterialForge | improved dielectric inference; oxide/coating layers; stone porosity; wood anisotropy; metal patina | registry caching; texture metadata batching; permutation pruning; material hot-path profiling; compact lookup data | material sidecars; authoring UI; asset scanner; per-mod overrides; calibration library |
| Natural Lighting | area-light approximation; improved shadow energy; interior bounce balance; candle spectra; weather attenuation | per-light classification cache; shared attenuation; light list culling; constant packing; SIMD CPU setup | light metadata API; photometric presets; extended emitters; compatibility profiles; capture comparison tool |
| Pixel Capture | HDR output; EXR support; motion-compensated samples; alpha/mask exports; improved particle resolve | GPU resolve; async encoding; pooled readbacks; streaming statistics; tiled supersampling | metadata sidecars; comparison captures; automated scene suite; cubemap capture; render-layer export |
| Pulse Profiler | clearer pass visualization; hitch annotations; VRAM graphs; quality comparison; frame-pacing views | query pooling; lower sampling cadence; ring buffers; deferred readback; zero-cost disabled scopes | capture export; PIX markers; benchmark automation; regression thresholds; hardware database |
| Radiant Grid | area emitters; better flame colour; emissive geometry coupling; shadowed particles; volumetric spill | GPU submission; tighter clusters; indirect lists; temporal light stability; buffer compaction | LTC lights; emissive extraction; magic profiles; mod API; light heatmaps |
| Rain Response | roof-edge sheets; puddle ripples; splash normals; snow/rain mixing; wind-driven mist | runoff tile culling; adaptive precipitation density; event compaction; lower-rate roof detection; shared weather fields | drainage graph; indoor occlusion volumes; splash decals; thunder coupling; weather-mod API |
| SkinOptics | melanin/hemoglobin tuning; better ear transmission; pore normal filtering; makeup handling; eye-skin transition | dialogue-only quality boost; sample reuse; material eligibility masks; lower-rate wetness; packed constants | character profiles; race metadata; complexion calibration; face-light diagnostics; skin-mod test corpus |
| Sky Continuity | eclipse handling; moon colour; latitude profiles; aurora interaction; improved twilight | cached ephemeris; update-on-time-change; shared directions; compact constants; shadow refresh scheduling | calendar API; multi-moon research; worldspace astronomy; weather-mod contract; diagnostic sky paths |
| Sky Veil | multi-layer clouds; better cloud edges; precipitation shafts; cloud types; mountain occlusion | adaptive ray steps; temporal checkerboard; cloud tile skip; lower-rate shadow maps; compressed history | weather cloud profiles; storm cells; cloud authoring tool; lightning integration; volumetric cloud prototype |
| SkyBounce | cascaded irradiance; better visibility; probe relocation; interior/exterior blending; directional radiance | toroidal updates; priority queues; sparse probes; lower precision validation; occupancy scheduling | DDGI comparison; probe classification; worldspace budgets; author debug mode; baked/dynamic fusion |
| Strand Shading | dual scattering; melanin model; flyaway response; wet lobes; better alpha edge lighting | lobe approximation study; LUT evaluation; descriptor scoping; distance simplification; half precision validation | hair metadata; card direction maps; mod corpus; author preview; groom sidecar research |
| Terrain Detail | blue-noise variation; biome macro colour; strata direction; slope-aware detail; snow-aware tiling | sample sharing; far-distance simplification; texture gather study; branch reduction; packed weights | biome metadata; landscape bake tool; virtual texture study; terrain capture suite; mod author API |
| Terrain Field | richer runtime classification; material transitions; seasonal ground identity; cave terrain handling; shore material cues | streamed clipmaps; dirty cell uploads; compact classifications; lookup caching; worldspace budgets | terrain metadata; season API expansion; material scanner; custom-worldspace registry; author diagnostics |
| Terrain Occlusion | soft penumbrae; snow height shadows; city/worldspace maps; sunrise stability; distant silhouette occlusion | clipmap updates; sun-angle thresholds; compressed heightmaps; tile culling; async preprocessing | runtime height capture; mod heightmap format; authoring tool; worldspace downloads excluded; terrain GI coupling |
| Terrain Seam | material-aware contacts; snow seam fill; wet edge blending; vegetation roots; rock-ground AO | adaptive capture bounds; half-resolution study; tile masks; dirty updates; fused depth capture | mesh contact metadata; author debug view; worldspace profiles; object-class exclusions; GI seam coupling |
| Thin Surface | acrylic model; leaf-specific transmission; paper/parchment; ice sheets; fabric weave | material eligibility masks; LUT transmission; distance simplification; branch scoping; packed settings | material tags; author profiles; layered transmission; spectral study; compatibility corpus |
| Tissue Diffusion | profile calibration; ear/cartilage response; better depth edges; coloured transmission; wet-skin coupling | bilateral tap reduction; temporal reuse study; half-resolution masks; tile skin detection; kernel packing | race profiles; author UI; diffusion reference captures; subsurface metadata; hardware RT comparison |
| Volume Occlusion | coloured translucent shadows; cascade blending; softer effects; particle self-occlusion; decal depth awareness | adaptive resolution; shared blur; cascade tile skip; update cadence; compact moments | per-effect shadow budgets; author controls; volumetric coupling; diagnostics; EVSM comparison |
| Water Optics | wave spectra; shoreline foam shapes; turbidity layers; underwater particulate; ice-water transition | adaptive SSR tiles; half-resolution reflection; temporal caustics; normal sample reuse; flow mip streaming | lake/river profiles; bathymetry metadata; FFT prototype; rain-wave coupling; water-mod API |
| Waterbody | shoreline flow; waterfalls; eddies; depth-aware colour; worldspace water types | streamed flow tiles; mip residency; mesh batching; cache compaction; cell-priority updates | flow authoring tool; water metadata; navmesh/current gameplay API; ocean prototype; seasonal ice support |
| WindowLife | richer room depth; better curtains; night occupancy variation; rain-on-glass; interior light spill | atlas residency; pane tile culling; distance fallback; branch scoping; sample reuse | authored window tags; room packs; time schedules; weather interaction; portal-render prototype |
| World Probes | local probe blending; visibility weighting; parallax correction; interior probes; glossy temporal stability | prioritized faces; incremental filtering; sparse probe sets; BC6H scheduling; resource pooling | probe volumes; author placement; baked/dynamic fusion; worldspace budgets; hardware capture comparison |
| Build/UI/runtime integration | clearer recovery UI; richer previews; accessibility contrast modes; startup progress detail; safe diagnostics | deterministic build outputs; parallel shader audit; cache telemetry kept local; faster manifest hashing; incremental package checks | ABI schema generator; automated control tracing; crash-safe optional modules; CI package verification; reproducible public source export |
